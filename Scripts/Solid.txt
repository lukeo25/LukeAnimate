/* Luke Animate Solid Type Tool v1.0 */
(function(){
  "use strict";
  const PANEL_ID="lukeanimate-solid-panel",STYLE_ID="lukeanimate-solid-style",TOOL_ID="solid";
  const GROUP_ID=String(group&&group.id?group.id:"type"),ITEM_ID=String(item&&(item.id||item.label)?(item.id||item.label):"Solid"),ICON=String(item&&item.iconUrl?item.iconUrl:"https://lukeo25.github.io/LukeAnimate/SVGs/Solid.svg");
  if(!LukeAnimate||Number(LukeAnimate.version||0)<122)throw new Error("Solid requires Luke Animate v122 or later.");
  const selectTool=active=>{if(LukeAnimate.setConfiguredToolActive)LukeAnimate.setConfiguredToolActive(GROUP_ID,ITEM_ID,!!active);};
  const old=document.getElementById(PANEL_ID);if(old){old.style.display="flex";old.classList.add("open");selectTool(true);LukeAnimate.bringFloatingPanelToFront(old);LukeAnimate.clampFloatingPanelToViewport(old);return;}
  if(!document.getElementById(STYLE_ID)){const style=document.createElement('style');style.id=STYLE_ID;style.textContent=`
    #${PANEL_ID}{position:fixed;z-index:10000;left:28px;top:18px;width:374px;max-width:calc(100vw - 16px);display:flex;flex-direction:column;background:#f1f3f6;border:1px solid #bfc6cf;border-radius:14px;box-shadow:0 20px 60px rgba(0,0,0,.3);color:#27313d;font:13px Inter,-apple-system,BlinkMacSystemFont,Segoe UI,sans-serif;overflow:hidden} #${PANEL_ID} *{box-sizing:border-box}
    #${PANEL_ID} .so-head{display:flex;align-items:center;gap:9px;padding:8px 10px;background:#fff;border-bottom:1px solid #d3d8df;cursor:move;user-select:none} #${PANEL_ID} .so-head img{width:27px;height:27px;object-fit:contain;filter:grayscale(1) brightness(.72)} #${PANEL_ID} .so-title{flex:1} #${PANEL_ID} .so-title strong{display:block;font-size:14px;color:#29434a} #${PANEL_ID} .so-title span{font-size:9px;letter-spacing:.11em;text-transform:uppercase;color:#7b8491} #${PANEL_ID} .so-close{width:32px;height:32px;border:1px solid #bbc3cd;border-radius:8px;background:#fff;font-size:20px;color:#566270;cursor:pointer}
    #${PANEL_ID} .so-body{padding:8px 7px 46px;min-height:260px} #${PANEL_ID} .so-selected,#${PANEL_ID} .so-card{padding:9px;margin-bottom:8px;border:1px solid #d2d7de;border-radius:10px;background:#fff} #${PANEL_ID} .so-selected strong{color:#0e8f7c} #${PANEL_ID} .so-check{display:flex;align-items:flex-start;gap:8px} #${PANEL_ID} input{accent-color:#0e9c86} #${PANEL_ID} .so-muted{color:#74808d;font-size:11px;line-height:1.4}
    #${PANEL_ID} .so-actions{display:grid;grid-template-columns:1fr 1fr;gap:8px} #${PANEL_ID} .so-btn{padding:7px;border:1px solid #b8c0ca;border-radius:8px;background:#eceff2;color:#4b5766;font-size:10px;cursor:pointer} #${PANEL_ID} .so-btn.primary{border-color:#0e9c86;background:#0e9c86;color:#fff;font-weight:700} #${PANEL_ID} .so-btn:disabled{opacity:.45;cursor:not-allowed} #${PANEL_ID} .so-status{position:absolute;left:7px;right:7px;bottom:7px;min-height:26px;border:1px solid #cbd2da;border-radius:8px;background:#fff;padding:8px 10px;font-size:10px;color:#596573} #${PANEL_ID} .so-status.good{border-color:#0e9c86;color:#117d6d} #${PANEL_ID} .so-status.bad{border-color:#c66a6a;color:#a33d3d}`;document.head.appendChild(style);}
  const panel=document.createElement('section');panel.id=PANEL_ID;panel.className='open';panel.innerHTML=`<div class="so-head" data-floating-panel-drag><img src="${ICON.replace(/"/g,'&quot;')}" alt=""><div class="so-title"><strong>Solid</strong><span>Collision surface</span></div><button class="so-close" type="button" title="Close">×</button></div><div class="so-body"><div class="so-selected"><strong>Selected:</strong> <span data-so-selected>No Clip selected</span></div><div class="so-card"><label class="so-check"><input data-so-oneway type="checkbox"><span><strong>One-way platform</strong><br><span class="so-muted">Characters land on top but can pass upward through it.</span></span></label></div><p class="so-muted">Apply Solid to a Clip instance. Side Runner characters—and Directions objects with “Collide with Solid Clips” enabled—will collide with its visible bounds.</p><div class="so-actions"><button class="so-btn" data-so-remove type="button">Remove</button><button class="so-btn primary" data-so-apply type="button">Apply</button></div><div class="so-status" data-so-status></div></div>`;
  document.body.appendChild(panel);LukeAnimate.registerFloatingPanel(panel);LukeAnimate.bringFloatingPanelToFront(panel);LukeAnimate.clampFloatingPanelToViewport(panel);selectTool(true);
  const get=q=>panel.querySelector(q),status=get('[data-so-status]'),apply=get('[data-so-apply]'),remove=get('[data-so-remove]');let selected=null,key='';const setStatus=(m,k)=>{status.textContent=String(m||'');status.className='so-status'+(k?' '+k:'');};
  function selectedClip(){const list=LukeAnimate.getSelectedDrawObjects()||[],clips=LukeAnimate.getClips?LukeAnimate.getClips():{};return list.length===1&&list[0]&&list[0].type==='clipinstance'&&clips[list[0].clipId]?list[0]:null;} function nameOf(o){const clips=LukeAnimate.getClips?LukeAnimate.getClips():{};return o?String(o.name||(clips[o.clipId]&&clips[o.clipId].name)||'Clip'):'No Clip selected';}
  function refresh(force){const next=selectedClip(),nextKey=next?String(next.id||next.clipId):'';if(!force&&nextKey===key)return;selected=next;key=nextKey;get('[data-so-selected]').textContent=nameOf(next);apply.disabled=!next;const settings=next?LukeAnimate.getTypeTool(next,TOOL_ID):null;remove.disabled=!settings;get('[data-so-oneway]').checked=!!(settings&&settings.oneWay);setStatus(next?(settings?'This Clip is Solid.':'Ready to mark this Clip Solid.'):'Select one Clip instance on the stage.',next?'good':'');}
  apply.addEventListener('click',()=>{refresh(false);if(!selected){setStatus('Select one Clip first.','bad');return;}LukeAnimate.setTypeTool(selected,TOOL_ID,{enabled:true,oneWay:get('[data-so-oneway]').checked});remove.disabled=false;setStatus('Solid applied.','good');});remove.addEventListener('click',()=>{refresh(false);if(!selected||!LukeAnimate.removeTypeTool(selected,TOOL_ID)){setStatus('The selected Clip is not Solid.','bad');return;}remove.disabled=true;setStatus('Solid removed.','good');});
  const timer=setInterval(()=>refresh(false),250);get('.so-close').addEventListener('click',()=>{clearInterval(timer);panel.remove();selectTool(false);});refresh(true);
})();
