/* Luke Animate Button Behaviour v1.1 — ActionScript-style Clip buttons and event actions */
(function(){
  "use strict";
  const PANEL_ID="lukeanimate-button-panel",STYLE_ID="lukeanimate-button-style",TOOL_ID="button";
  const GROUP_ID=String(group&&group.id?group.id:"behaviours"),ITEM_ID=String(item&&(item.id||item.label)?(item.id||item.label):"Button");
  const ICON=String(item&&item.iconUrl?item.iconUrl:"https://lukeo25.github.io/LukeAnimate/SVGs/ButtonBehaviour.svg");
  const DEFAULT_CODE=`// Optional ActionScript-style button hook.
// context.event is rollover, rollout, press, release, click or releaseoutside.
return {};`;
  const DEFAULT_ACTION={event:"click",delay:0,target:"self",action:"gotoStop",value:"1",value2:"10",variable:"score",integer:true};
  if(!LukeAnimate||Number(LukeAnimate.version||0)<135||typeof LukeAnimate.setTypeTool!=="function"||typeof LukeAnimate.ensureClipStateFrames!=="function")throw new Error("Button Behaviour requires the current Luke Animate runtime.");
  const selectTool=active=>LukeAnimate.setConfiguredToolActive&&LukeAnimate.setConfiguredToolActive(GROUP_ID,ITEM_ID,!!active);
  const old=document.getElementById(PANEL_ID);if(old){old.style.display="flex";old.classList.add("open");selectTool(true);LukeAnimate.bringFloatingPanelToFront(old);return;}
  if(!document.getElementById(STYLE_ID)){
    const style=document.createElement("style");style.id=STYLE_ID;style.textContent=`
      #${PANEL_ID}{position:fixed;z-index:10000;left:30px;top:18px;width:470px;max-width:calc(100vw - 16px);height:min(780px,calc(100vh - 36px));display:flex;flex-direction:column;background:#f1f3f6;border:1px solid #bfc6cf;border-radius:14px;box-shadow:0 20px 60px rgba(0,0,0,.3);color:#27313d;font:13px Inter,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;overflow:hidden}
      #${PANEL_ID} *{box-sizing:border-box} #${PANEL_ID} .bt-head{display:flex;align-items:center;gap:9px;padding:8px 10px;background:#fff;border-bottom:1px solid #d3d8df;cursor:move;user-select:none} #${PANEL_ID} .bt-head img{width:27px;height:27px;object-fit:contain;filter:grayscale(1) brightness(.72)}
      #${PANEL_ID} .bt-title{flex:1} #${PANEL_ID} .bt-title strong{display:block;font-size:14px;color:#29434a} #${PANEL_ID} .bt-title span{font-size:9px;letter-spacing:.11em;text-transform:uppercase;color:#7b8491} #${PANEL_ID} .bt-close{width:32px;height:32px;border:1px solid #bbc3cd;border-radius:8px;background:#fff;font-size:20px;color:#566270;cursor:pointer}
      #${PANEL_ID} .bt-body{flex:1;min-height:0;padding:8px 7px 48px;overflow:auto} #${PANEL_ID} .bt-selected,#${PANEL_ID} .bt-card{padding:9px;margin-bottom:7px;border:1px solid #d2d7de;border-radius:10px;background:#fff} #${PANEL_ID} .bt-selected strong{color:#0e8f7c}
      #${PANEL_ID} h3{margin:0 0 4px;font-size:12px;color:#3e4c5a} #${PANEL_ID} .bt-help{margin:2px 0 9px;color:#74808d;font-size:10.5px;line-height:1.4} #${PANEL_ID} .bt-grid{display:grid;grid-template-columns:1fr 1fr;gap:7px}
      #${PANEL_ID} .bt-field{display:flex;flex-direction:column;gap:4px;font-size:10.5px;font-weight:700;color:#596674} #${PANEL_ID} input[type=text],#${PANEL_ID} input[type=number],#${PANEL_ID} select{width:100%;height:34px;padding:6px 7px;border:1px solid #b9c3ce;border-radius:7px;background:#fff;color:#263442} #${PANEL_ID} .bt-check{display:flex;align-items:center;gap:7px;margin-top:8px;padding:7px;border:1px solid #d5dbe2;border-radius:7px;background:#f8fafb;font-size:11px} #${PANEL_ID} input[type=checkbox]{accent-color:#0e9c86}
      #${PANEL_ID} .bt-program-head{display:flex;align-items:center;gap:8px} #${PANEL_ID} .bt-program-head h3{flex:1} #${PANEL_ID} .bt-add{height:30px;padding:0 10px;border:1px solid #0e9c86;border-radius:7px;background:#fff;color:#0e8f7c;font-weight:750;cursor:pointer}
      #${PANEL_ID} .bt-action-list{display:flex;flex-direction:column;gap:7px;margin-top:8px} #${PANEL_ID} .bt-action-card{padding:8px;border:1px solid #cbd4dc;border-radius:9px;background:#f7f9fa} #${PANEL_ID} .bt-action-top{display:grid;grid-template-columns:1fr 1.15fr 30px;gap:6px;align-items:end} #${PANEL_ID} .bt-remove-action{width:30px;height:34px;border:1px solid #d7b8b8;border-radius:7px;background:#fff;color:#b54d4d;font-size:18px;cursor:pointer} #${PANEL_ID} .bt-action-fields{display:grid;grid-template-columns:1fr 1fr;gap:6px;margin-top:6px} #${PANEL_ID} .bt-action-empty{padding:12px;border:1px dashed #c5cdd5;border-radius:8px;text-align:center;color:#77838e;font-size:10.5px}
      #${PANEL_ID} details{margin-top:7px;border:1px solid #d2d7de;border-radius:10px;background:#fff;overflow:hidden} #${PANEL_ID} summary{padding:8px 9px;cursor:pointer;font-size:11px;letter-spacing:.08em;text-transform:uppercase;font-weight:700;color:#66717e} #${PANEL_ID} .bt-code{padding:9px} #${PANEL_ID} textarea{width:100%;min-height:110px;resize:vertical;padding:8px;border:1px solid #aeb8c4;border-radius:6px;background:#17212b;color:#e7f5f2;font:12px/1.45 Consolas,monospace}
      #${PANEL_ID} .bt-actions{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:10px} #${PANEL_ID} .bt-btn{padding:8px;border:1px solid #b8c0ca;border-radius:8px;background:#eceff2;color:#4b5766;cursor:pointer} #${PANEL_ID} .bt-btn.primary{border-color:#0e9c86;background:#0e9c86;color:#fff;font-weight:700} #${PANEL_ID} .bt-btn:disabled{opacity:.45;cursor:not-allowed}
      #${PANEL_ID} .bt-status{position:absolute;left:7px;right:7px;bottom:7px;min-height:28px;max-height:42px;overflow:auto;border:1px solid #cbd2da;border-radius:8px;background:rgba(255,255,255,.97);padding:8px 10px;font-size:10px;color:#596573} #${PANEL_ID} .bt-status.good{border-color:#0e9c86;color:#117d6d} #${PANEL_ID} .bt-status.bad{border-color:#c66a6a;color:#a33d3d}`;
    document.head.appendChild(style);
  }
  const panel=document.createElement("section");panel.id=PANEL_ID;panel.className="open";panel.innerHTML=`<div class="bt-head" data-floating-panel-drag><img src="${ICON.replace(/"/g,"&quot;")}" alt=""><div class="bt-title"><strong>Button</strong><span>ActionScript-style Clip interaction</span></div><button class="bt-close" type="button" title="Close">×</button></div><div class="bt-body"><div class="bt-selected"><strong>Selected:</strong> <span data-selected>No Clip selected</span></div><div class="bt-card"><h3>Button frames</h3><p class="bt-help">Named frames inside the selected Clip. Missing frames are created automatically so each state can be drawn separately.</p><datalist id="la-button-frame-labels"></datalist><div class="bt-grid"><label class="bt-field">Normal / Up<input data-frame="normal" list="la-button-frame-labels" value="Normal"></label><label class="bt-field">Hover / Over<input data-frame="hover" list="la-button-frame-labels" value="Hover"></label><label class="bt-field">Mouse Down<input data-frame="down" list="la-button-frame-labels" value="Down"></label><label class="bt-field">Selected<input data-frame="selected" list="la-button-frame-labels" value="Selected"></label></div><label class="bt-check"><input data-latch type="checkbox" checked> Click toggles the Selected state</label></div><div class="bt-card"><div class="bt-program-head"><h3>Event programming</h3><button class="bt-add" data-add-action type="button">+ Add action</button></div><p class="bt-help">ActionScript-style: choose an event, then what it does. Hover and Hold can wait for a specific number of seconds.</p><div class="bt-action-list" data-action-list></div></div><details><summary>View / edit code</summary><div class="bt-code"><p class="bt-help">Optional advanced hook. Node Coder also exposes every mouse event as a connection port.</p><textarea data-code spellcheck="false"></textarea><button class="bt-btn" data-reset type="button">Reset code</button></div></details><div class="bt-actions"><button class="bt-btn" data-remove type="button">Remove</button><button class="bt-btn primary" data-apply type="button">Apply</button></div><div class="bt-status" data-status></div></div>`;
  document.body.appendChild(panel);LukeAnimate.registerFloatingPanel(panel);LukeAnimate.bringFloatingPanelToFront(panel);LukeAnimate.clampFloatingPanelToViewport(panel);selectTool(true);
  const get=s=>panel.querySelector(s),status=get("[data-status]"),apply=get("[data-apply]"),remove=get("[data-remove]"),actionList=get("[data-action-list]");let selected=null,key="",actionCounter=0;
  const esc=value=>String(value??"").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;");
  const setStatus=(message,kind="")=>{status.textContent=String(message||"");status.className="bt-status"+(kind?" "+kind:"");};
  function selectedClip(){const list=LukeAnimate.getSelectedDrawObjects()||[];return list.length===1&&list[0]&&list[0].type==="clipinstance"?list[0]:null;}
  function clipName(object){const clips=LukeAnimate.getClips?LukeAnimate.getClips():{};return object?String(object.name||(clips[object.clipId]&&clips[object.clipId].name)||"Clip"):"No Clip selected";}
  function availableClips(){return (LukeAnimate.getDrawObjects?LukeAnimate.getDrawObjects():[]).filter(object=>object&&object.type==="clipinstance");}
  function targetOptions(preferred){let html='<option value="self">This Clip</option><option value="timeline">Main Timeline</option>';availableClips().filter(object=>object!==selected).forEach(object=>{html+=`<option value="${esc(object.id)}">${esc(clipName(object))}</option>`;});if(preferred&&!['self','timeline'].includes(preferred)&&!availableClips().some(object=>String(object.id)===preferred))html+=`<option value="${esc(preferred)}">Saved Clip (not on this frame)</option>`;return html;}
  function populateLabels(object){const labels=object&&LukeAnimate.getClipFrameLabels?LukeAnimate.getClipFrameLabels(object.clipId):[];get("#la-button-frame-labels").innerHTML=(labels||[]).map(label=>`<option value="${esc(label.name||"")}"></option>`).join("");}
  function newAction(value){return Object.assign({},DEFAULT_ACTION,value||{},{id:String(value&&value.id||("button-action-"+(++actionCounter)))});}
  function actionCard(value){const data=newAction(value),card=document.createElement("div");card.className="bt-action-card";card.dataset.actionId=data.id;card.innerHTML=`<div class="bt-action-top"><label class="bt-field">When<select data-action-event><option value="rollover">Roll over</option><option value="rollout">Roll out</option><option value="hover">Hover for time</option><option value="press">Mouse down</option><option value="release">Mouse release</option><option value="click">Click</option><option value="hold">Hold for time</option><option value="releaseoutside">Release outside</option></select></label><label class="bt-field">Do<select data-action-kind><option value="gotoStop">Go to frame and stop</option><option value="gotoPlay">Go to frame and play</option><option value="stop">Stop</option><option value="play">Play</option><option value="setVariable">Set variable</option><option value="randomVariable">Random number</option><option value="toggleSelected">Toggle Selected state</option><option value="select">Select state</option><option value="deselect">Normal state</option></select></label><button class="bt-remove-action" type="button" title="Delete this action">×</button></div><div class="bt-action-fields"><label class="bt-field" data-target-field>On<select data-action-target>${targetOptions(data.target)}</select></label><label class="bt-field" data-value-field><span data-value-label>Frame / label</span><input data-action-value type="text" value="${esc(data.value)}"></label><label class="bt-field" data-value2-field><span>Maximum</span><input data-action-value2 type="number" step="any" value="${esc(data.value2)}"></label><label class="bt-field" data-variable-field><span>Variable name</span><input data-action-variable type="text" value="${esc(data.variable)}"></label><label class="bt-field"><span data-delay-label>Delay (seconds)</span><input data-action-delay type="number" min="0" max="3600" step="0.05" value="${esc(data.delay)}"></label><label class="bt-check" data-integer-field><input data-action-integer type="checkbox" ${data.integer!==false?"checked":""}> Whole number</label></div>`;card.querySelector("[data-action-event]").value=data.event;card.querySelector("[data-action-kind]").value=data.action;card.querySelector("[data-action-target]").value=data.target;card.querySelector(".bt-remove-action").onclick=()=>{card.remove();renderEmpty();};card.querySelector("[data-action-kind]").onchange=()=>updateActionCard(card);card.querySelector("[data-action-event]").onchange=()=>updateActionCard(card);updateActionCard(card);return card;}
  function updateActionCard(card){const kind=card.querySelector("[data-action-kind]").value,event=card.querySelector("[data-action-event]").value,playback=['gotoStop','gotoPlay','stop','play'].includes(kind),goto=['gotoStop','gotoPlay'].includes(kind),variable=['setVariable','randomVariable'].includes(kind),random=kind==='randomVariable';card.querySelector("[data-target-field]").style.display=playback?"flex":"none";card.querySelector("[data-value-field]").style.display=(goto||kind==='setVariable'||random)?"flex":"none";card.querySelector("[data-value-label]").textContent=goto?"Frame number or label":(random?"Minimum":"Value");card.querySelector("[data-value2-field]").style.display=random?"flex":"none";card.querySelector("[data-variable-field]").style.display=variable?"flex":"none";card.querySelector("[data-integer-field]").style.display=random?"flex":"none";card.querySelector("[data-delay-label]").textContent=event==='hold'?"Hold time (seconds)":(event==='hover'?"Hover time (seconds)":"Delay (seconds)");}
  function renderEmpty(){let empty=actionList.querySelector(".bt-action-empty");if(actionList.querySelector(".bt-action-card")){if(empty)empty.remove();}else if(!empty){empty=document.createElement("div");empty.className="bt-action-empty";empty.textContent="No programmed actions. Click + Add action.";actionList.appendChild(empty);}}
  function renderActions(actions){actionList.innerHTML="";(actions||[]).forEach(action=>actionList.appendChild(actionCard(action)));renderEmpty();}
  function readActions(){return Array.from(actionList.querySelectorAll(".bt-action-card")).map(card=>({id:card.dataset.actionId,event:card.querySelector("[data-action-event]").value,delay:Number(card.querySelector("[data-action-delay]").value)||0,target:card.querySelector("[data-action-target]").value,action:card.querySelector("[data-action-kind]").value,value:card.querySelector("[data-action-value]").value,value2:card.querySelector("[data-action-value2]").value,variable:card.querySelector("[data-action-variable]").value,integer:card.querySelector("[data-action-integer]").checked}));}
  function load(value){const settings=value||{};get('[data-frame="normal"]').value=String(settings.normalFrame||"Normal");get('[data-frame="hover"]').value=String(settings.hoverFrame||"Hover");get('[data-frame="down"]').value=String(settings.downFrame||"Down");get('[data-frame="selected"]').value=String(settings.selectedFrame||"Selected");get("[data-latch]").checked=settings.latchSelected!==false;get("[data-code]").value=String(settings.code||DEFAULT_CODE);renderActions(Array.isArray(settings.actions)?settings.actions:(value?[]:[DEFAULT_ACTION]));}
  function settings(){return {enabled:true,normalFrame:get('[data-frame="normal"]').value.trim()||"Normal",hoverFrame:get('[data-frame="hover"]').value.trim()||"Hover",downFrame:get('[data-frame="down"]').value.trim()||"Down",selectedFrame:get('[data-frame="selected"]').value.trim()||"Selected",latchSelected:get("[data-latch]").checked,actions:readActions(),code:get("[data-code]").value};}
  function refresh(force){const next=selectedClip(),nextKey=next?String(next.id||next.clipId):"";if(!force&&nextKey===key)return;selected=next;key=nextKey;get("[data-selected]").textContent=clipName(next);apply.disabled=!next;populateLabels(next);const value=next?LukeAnimate.getTypeTool(next,TOOL_ID):null;remove.disabled=!value;load(value);setStatus(next?(value?"Button is attached. Edit its event actions, then click Apply.":"Choose what the button does, then click Apply."):"Select one Clip instance on the stage.",next?"good":"");}
  get("[data-add-action]").onclick=()=>{actionList.appendChild(actionCard(DEFAULT_ACTION));renderEmpty();actionList.lastElementChild.scrollIntoView({block:"nearest"});};
  get("[data-reset]").onclick=()=>{get("[data-code]").value=DEFAULT_CODE;setStatus("Code reset. Click Apply to save it.","good");};
  apply.onclick=()=>{refresh(false);if(!selected)return setStatus("Select one Clip first.","bad");const value=settings(),names=[value.normalFrame,value.hoverFrame,value.downFrame,value.selectedFrame].filter(Boolean);LukeAnimate.ensureClipStateFrames(selected.clipId,names);LukeAnimate.setTypeTool(selected,TOOL_ID,value);populateLabels(selected);remove.disabled=false;setStatus("Button programmed with "+value.actions.length+" action"+(value.actions.length===1?"":"s")+". Press Play to test it.","good");};
  remove.onclick=()=>{refresh(false);if(!selected||!LukeAnimate.removeTypeTool(selected,TOOL_ID))return setStatus("The selected Clip does not have Button behaviour.","bad");remove.disabled=true;load(null);setStatus("Button behaviour removed.","good");};
  const timer=setInterval(()=>refresh(false),250);get(".bt-close").onclick=()=>{clearInterval(timer);panel.remove();selectTool(false);};refresh(true);
})();
