/* Luke Animate Button Behaviour v1.0 — ActionScript-style Clip buttons */
(function(){
  "use strict";
  const PANEL_ID="lukeanimate-button-panel",STYLE_ID="lukeanimate-button-style",TOOL_ID="button";
  const GROUP_ID=String(group&&group.id?group.id:"behaviours"),ITEM_ID=String(item&&(item.id||item.label)?(item.id||item.label):"Button");
  const ICON=String(item&&item.iconUrl?item.iconUrl:"https://lukeo25.github.io/LukeAnimate/SVGs/ButtonBehaviour.svg");
  const DEFAULT_CODE=`// Optional ActionScript-style button hook.
// context.event is rollover, rollout, press, release, click or releaseoutside.
return {};`;
  if(!LukeAnimate||typeof LukeAnimate.setTypeTool!=="function"||typeof LukeAnimate.ensureClipStateFrames!=="function")throw new Error("Button Behaviour requires the current Luke Animate runtime.");
  const selectTool=active=>LukeAnimate.setConfiguredToolActive&&LukeAnimate.setConfiguredToolActive(GROUP_ID,ITEM_ID,!!active);
  const old=document.getElementById(PANEL_ID);if(old){old.style.display="flex";old.classList.add("open");selectTool(true);LukeAnimate.bringFloatingPanelToFront(old);return;}
  if(!document.getElementById(STYLE_ID)){
    const style=document.createElement("style");style.id=STYLE_ID;style.textContent=`
      #${PANEL_ID}{position:fixed;z-index:10000;left:30px;top:18px;width:410px;max-width:calc(100vw - 16px);height:min(700px,calc(100vh - 36px));display:flex;flex-direction:column;background:#f1f3f6;border:1px solid #bfc6cf;border-radius:14px;box-shadow:0 20px 60px rgba(0,0,0,.3);color:#27313d;font:13px Inter,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;overflow:hidden}
      #${PANEL_ID} *{box-sizing:border-box} #${PANEL_ID} .bt-head{display:flex;align-items:center;gap:9px;padding:8px 10px;background:#fff;border-bottom:1px solid #d3d8df;cursor:move;user-select:none} #${PANEL_ID} .bt-head img{width:27px;height:27px;object-fit:contain;filter:grayscale(1) brightness(.72)}
      #${PANEL_ID} .bt-title{flex:1} #${PANEL_ID} .bt-title strong{display:block;font-size:14px;color:#29434a} #${PANEL_ID} .bt-title span{font-size:9px;letter-spacing:.11em;text-transform:uppercase;color:#7b8491} #${PANEL_ID} .bt-close{width:32px;height:32px;border:1px solid #bbc3cd;border-radius:8px;background:#fff;font-size:20px;color:#566270;cursor:pointer}
      #${PANEL_ID} .bt-body{flex:1;min-height:0;padding:8px 7px 48px;overflow:auto} #${PANEL_ID} .bt-selected,#${PANEL_ID} .bt-card{padding:9px;margin-bottom:7px;border:1px solid #d2d7de;border-radius:10px;background:#fff} #${PANEL_ID} .bt-selected strong{color:#0e8f7c}
      #${PANEL_ID} h3{margin:0 0 4px;font-size:12px;color:#3e4c5a} #${PANEL_ID} .bt-help{margin:2px 0 9px;color:#74808d;font-size:10.5px;line-height:1.4} #${PANEL_ID} .bt-grid{display:grid;grid-template-columns:1fr 1fr;gap:7px}
      #${PANEL_ID} .bt-field{display:flex;flex-direction:column;gap:4px;font-size:11px;font-weight:700;color:#596674} #${PANEL_ID} input[type=text]{width:100%;padding:7px;border:1px solid #b9c3ce;border-radius:7px;background:#fff;color:#263442} #${PANEL_ID} .bt-check{display:flex;align-items:center;gap:7px;margin-top:8px;padding:7px;border:1px solid #d5dbe2;border-radius:7px;background:#f8fafb;font-size:11px} #${PANEL_ID} input[type=checkbox]{accent-color:#0e9c86}
      #${PANEL_ID} details{margin-top:7px;border:1px solid #d2d7de;border-radius:10px;background:#fff;overflow:hidden} #${PANEL_ID} summary{padding:8px 9px;cursor:pointer;font-size:11px;letter-spacing:.08em;text-transform:uppercase;font-weight:700;color:#66717e} #${PANEL_ID} .bt-code{padding:9px} #${PANEL_ID} textarea{width:100%;min-height:110px;resize:vertical;padding:8px;border:1px solid #aeb8c4;border-radius:6px;background:#17212b;color:#e7f5f2;font:12px/1.45 Consolas,monospace}
      #${PANEL_ID} .bt-actions{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:10px} #${PANEL_ID} .bt-btn{padding:8px;border:1px solid #b8c0ca;border-radius:8px;background:#eceff2;color:#4b5766;cursor:pointer} #${PANEL_ID} .bt-btn.primary{border-color:#0e9c86;background:#0e9c86;color:#fff;font-weight:700} #${PANEL_ID} .bt-btn:disabled{opacity:.45;cursor:not-allowed}
      #${PANEL_ID} .bt-status{position:absolute;left:7px;right:7px;bottom:7px;min-height:28px;max-height:42px;overflow:auto;border:1px solid #cbd2da;border-radius:8px;background:rgba(255,255,255,.97);padding:8px 10px;font-size:10px;color:#596573} #${PANEL_ID} .bt-status.good{border-color:#0e9c86;color:#117d6d} #${PANEL_ID} .bt-status.bad{border-color:#c66a6a;color:#a33d3d}`;
    document.head.appendChild(style);
  }
  const panel=document.createElement("section");panel.id=PANEL_ID;panel.className="open";panel.innerHTML=`<div class="bt-head" data-floating-panel-drag><img src="${ICON.replace(/"/g,"&quot;")}" alt=""><div class="bt-title"><strong>Button</strong><span>ActionScript-style Clip interaction</span></div><button class="bt-close" type="button" title="Close">×</button></div><div class="bt-body"><div class="bt-selected"><strong>Selected:</strong> <span data-selected>No Clip selected</span></div><div class="bt-card"><h3>Button frames</h3><p class="bt-help">These are named frames inside the selected Clip. Missing frames are added automatically so you can enter the Clip and draw each state.</p><datalist id="la-button-frame-labels"></datalist><div class="bt-grid"><label class="bt-field">Normal / Up<input data-frame="normal" list="la-button-frame-labels" value="Normal"></label><label class="bt-field">Hover / Over<input data-frame="hover" list="la-button-frame-labels" value="Hover"></label><label class="bt-field">Mouse Down<input data-frame="down" list="la-button-frame-labels" value="Down"></label><label class="bt-field">Selected<input data-frame="selected" list="la-button-frame-labels" value="Selected"></label></div><label class="bt-check"><input data-latch type="checkbox" checked> Click toggles the Selected state</label></div><div class="bt-card"><h3>ActionScript events</h3><p class="bt-help">Node Coder exposes Roll Over, Roll Out, Hover, Mouse Down, Mouse Up, Click and Release Outside. Connect these ports to goto, stop, Rando, variables or your own code.</p></div><details><summary>View / edit code</summary><div class="bt-code"><p class="bt-help">Optional advanced hook. It runs when the button state changes.</p><textarea data-code spellcheck="false"></textarea><button class="bt-btn" data-reset type="button">Reset code</button></div></details><div class="bt-actions"><button class="bt-btn" data-remove type="button">Remove</button><button class="bt-btn primary" data-apply type="button">Apply</button></div><div class="bt-status" data-status></div></div>`;
  document.body.appendChild(panel);LukeAnimate.registerFloatingPanel(panel);LukeAnimate.bringFloatingPanelToFront(panel);LukeAnimate.clampFloatingPanelToViewport(panel);selectTool(true);
  const get=s=>panel.querySelector(s),status=get("[data-status]"),apply=get("[data-apply]"),remove=get("[data-remove]");let selected=null,key="";
  const setStatus=(message,kind="")=>{status.textContent=String(message||"");status.className="bt-status"+(kind?" "+kind:"");};
  function selectedClip(){const list=LukeAnimate.getSelectedDrawObjects()||[];return list.length===1&&list[0]&&list[0].type==="clipinstance"?list[0]:null;}
  function clipName(object){const clips=LukeAnimate.getClips?LukeAnimate.getClips():{};return object?String(object.name||(clips[object.clipId]&&clips[object.clipId].name)||"Clip"):"No Clip selected";}
  function populateLabels(object){const labels=object&&LukeAnimate.getClipFrameLabels?LukeAnimate.getClipFrameLabels(object.clipId):[];get("#la-button-frame-labels").innerHTML=(labels||[]).map(label=>`<option value="${String(label.name||"").replace(/"/g,"&quot;")}"></option>`).join("");}
  function load(value){const settings=value||{};get('[data-frame="normal"]').value=String(settings.normalFrame||"Normal");get('[data-frame="hover"]').value=String(settings.hoverFrame||"Hover");get('[data-frame="down"]').value=String(settings.downFrame||"Down");get('[data-frame="selected"]').value=String(settings.selectedFrame||"Selected");get("[data-latch]").checked=settings.latchSelected!==false;get("[data-code]").value=String(settings.code||DEFAULT_CODE);}
  function settings(){return {enabled:true,normalFrame:get('[data-frame="normal"]').value.trim()||"Normal",hoverFrame:get('[data-frame="hover"]').value.trim()||"Hover",downFrame:get('[data-frame="down"]').value.trim()||"Down",selectedFrame:get('[data-frame="selected"]').value.trim()||"Selected",latchSelected:get("[data-latch]").checked,code:get("[data-code]").value};}
  function refresh(force){const next=selectedClip(),nextKey=next?String(next.id||next.clipId):"";if(!force&&nextKey===key)return;selected=next;key=nextKey;get("[data-selected]").textContent=clipName(next);apply.disabled=!next;populateLabels(next);const value=next?LukeAnimate.getTypeTool(next,TOOL_ID):null;remove.disabled=!value;load(value);setStatus(next?(value?"Button is attached to this Clip.":"Set the state frames, then click Apply."):"Select one Clip instance on the stage.",next?"good":"");}
  get("[data-reset]").onclick=()=>{get("[data-code]").value=DEFAULT_CODE;setStatus("Code reset. Click Apply to save it.","good");};
  apply.onclick=()=>{refresh(false);if(!selected)return setStatus("Select one Clip first.","bad");const value=settings(),names=[value.normalFrame,value.hoverFrame,value.downFrame,value.selectedFrame].filter(Boolean);LukeAnimate.ensureClipStateFrames(selected.clipId,names);LukeAnimate.setTypeTool(selected,TOOL_ID,value);populateLabels(selected);remove.disabled=false;setStatus("Button applied. Enter the Clip to edit Normal, Hover, Down and Selected frames, then press Play to test it.","good");};
  remove.onclick=()=>{refresh(false);if(!selected||!LukeAnimate.removeTypeTool(selected,TOOL_ID))return setStatus("The selected Clip does not have Button behaviour.","bad");remove.disabled=true;load(null);setStatus("Button behaviour removed.","good");};
  const timer=setInterval(()=>refresh(false),250);get(".bt-close").onclick=()=>{clearInterval(timer);panel.remove();selectTool(false);};refresh(true);
})();
