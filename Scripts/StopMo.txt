/* Luke Animate Stop Motion Import v1.4 — webcam capture to native layer keys.
   Every Stop Mo session creates and selects its own normal numbered layer.
   The layer receives an explicit blank key at the target frame immediately.
   If that layer is deleted during capture, Stop Mo recreates it automatically. */
(function(){
  "use strict";
  const PANEL_ID="lukeanimate-stopmo-panel",STYLE_ID="lukeanimate-stopmo-style";
  const GROUP_ID=String(group&&group.id?group.id:"import"),ITEM_ID=String(item&&(item.id||item.label)?(item.id||item.label):"Stop Mo");
  const ICON=String(item&&item.iconUrl?item.iconUrl:"https://lukeo25.github.io/LukeAnimate/SVGs/StopMo.svg");
  document.getElementById(PANEL_ID)?.remove();
  if(!document.getElementById(STYLE_ID)){
    const style=document.createElement("style");style.id=STYLE_ID;style.textContent=`
      #${PANEL_ID}{position:fixed;z-index:10000;left:32px;top:28px;width:min(430px,calc(100vw - 24px));height:min(760px,calc(100vh - 24px));display:flex;flex-direction:column;overflow:hidden;background:#eef1f4;border:1px solid #b9c2cc;border-radius:14px;box-shadow:0 22px 70px rgba(0,0,0,.3);color:#273442;font:13px Inter,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}
      #${PANEL_ID} *{box-sizing:border-box} #${PANEL_ID} button,#${PANEL_ID} input,#${PANEL_ID} select{font:inherit}
      #${PANEL_ID} .sm-head{height:55px;flex:0 0 55px;display:flex;align-items:center;gap:10px;padding:8px 11px;background:#fff;border-bottom:1px solid #cfd5dc;cursor:move;user-select:none}
      #${PANEL_ID} .sm-head img{width:32px;height:32px;padding:4px;border-radius:8px;background:#66717c} #${PANEL_ID} .sm-title{flex:1;min-width:0} #${PANEL_ID} .sm-title strong{display:block;font-size:16px;color:#28414a} #${PANEL_ID} .sm-title span{display:block;font-size:9px;letter-spacing:.13em;text-transform:uppercase;color:#7c8793}
      #${PANEL_ID} .sm-close{width:34px;height:34px;border:1px solid #bbc4ce;border-radius:9px;background:#fff;color:#536170;font-size:19px;cursor:pointer}
      #${PANEL_ID} .sm-body{flex:1;min-height:0;overflow:auto;padding:11px} #${PANEL_ID} .sm-card{margin-bottom:10px;padding:11px;border:1px solid #cbd3db;border-radius:11px;background:#fff} #${PANEL_ID} .sm-card h4{margin:0 0 11px;color:#65727e;font:800 10px Consolas,monospace;letter-spacing:.1em;text-transform:uppercase}
      #${PANEL_ID} .sm-preview{position:relative;aspect-ratio:16/9;overflow:hidden;border:1px solid #c1cad3;border-radius:9px;background:#101418} #${PANEL_ID} .sm-preview canvas{display:block;width:100%;height:100%} #${PANEL_ID} .sm-empty{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;color:#89949e;font-size:11px;pointer-events:none} #${PANEL_ID} .sm-badge{position:absolute;left:7px;top:7px;padding:3px 7px;border-radius:999px;background:rgba(19,34,38,.82);color:#72e0cd;font:750 10px Consolas,monospace;pointer-events:none} #${PANEL_ID} .sm-flash{position:absolute;inset:0;background:#fff;opacity:0;pointer-events:none;transition:opacity .08s}
      #${PANEL_ID} .sm-capture{width:100%;height:43px;margin-top:9px;border:1px solid #0d9c86;border-radius:9px;background:#0d9c86;color:#fff;font-weight:800;cursor:pointer} #${PANEL_ID} .sm-capture:disabled{opacity:.45;cursor:not-allowed}
      #${PANEL_ID} .sm-actions{display:grid;grid-template-columns:1fr 1fr;gap:8px} #${PANEL_ID} .sm-btn{height:36px;border:1px solid #bdc6d0;border-radius:8px;background:#fff;color:#536170;cursor:pointer} #${PANEL_ID} .sm-btn.primary{border-color:#0d9c86;background:#0d9c86;color:#fff;font-weight:750} #${PANEL_ID} .sm-btn.danger{color:#a34141}
      #${PANEL_ID} .sm-row{display:grid;grid-template-columns:105px minmax(0,1fr);align-items:center;gap:9px;margin-bottom:9px;color:#64717d;font-size:12px} #${PANEL_ID} .sm-row:last-child{margin-bottom:0} #${PANEL_ID} .sm-row input,#${PANEL_ID} .sm-row select{width:100%;height:35px;padding:0 9px;border:1px solid #c3ccd5;border-radius:8px;background:#f7f9fa;color:#293843}
      #${PANEL_ID} .sm-frame{display:grid;grid-template-columns:36px 1fr 36px;gap:7px} #${PANEL_ID} .sm-frame button{height:35px;border:1px solid #bdc6d0;border-radius:8px;background:#fff;color:#536170;cursor:pointer} #${PANEL_ID} .sm-frame input{text-align:center;font-weight:750}
      #${PANEL_ID} .sm-check{display:flex;align-items:center;gap:7px;margin:8px 0;color:#5d6b78;font-size:12px} #${PANEL_ID} .sm-check input{accent-color:#0d9c86}
      #${PANEL_ID} .sm-slider{display:grid;grid-template-columns:105px minmax(0,1fr) 42px;align-items:center;gap:8px;margin:8px 0;color:#64717d;font-size:12px} #${PANEL_ID} .sm-slider input{width:100%;accent-color:#0d9c86} #${PANEL_ID} .sm-slider output{text-align:right;color:#74818c;font:10px Consolas,monospace}
      #${PANEL_ID} .sm-status{flex:0 0 auto;margin:0 10px 10px;padding:8px 9px;min-height:31px;border:1px solid #cbd3db;border-radius:8px;background:#fff;color:#64717d;font-size:10.5px} #${PANEL_ID} .sm-status.good{border-color:#0d9c86;color:#0b806e} #${PANEL_ID} .sm-status.bad{border-color:#c56d6d;color:#a34141}`;document.head.appendChild(style);
  }
  const panel=document.createElement("section");panel.id=PANEL_ID;panel.innerHTML=`
    <div class="sm-head" data-floating-panel-drag><img src="${ICON}" alt=""><div class="sm-title"><strong>Stop Motion Import</strong><span>Webcam capture · timeline frames</span></div><button class="sm-close" type="button" title="Close">×</button></div>
    <div class="sm-body">
      <div class="sm-card"><h4>Camera preview</h4><div class="sm-preview"><canvas data-preview></canvas><div class="sm-empty" data-empty>Start the camera to preview</div><div class="sm-badge" data-badge>Frame 1</div><div class="sm-flash" data-flash></div></div><button class="sm-capture" data-capture type="button" disabled>Capture Frame</button></div>
      <div class="sm-card"><h4>Camera</h4><div class="sm-actions"><button class="sm-btn primary" data-start type="button">Start camera</button><button class="sm-btn danger" data-stop type="button">Stop camera</button></div><div class="sm-row" style="margin-top:9px"><span>Device</span><select data-device><option value="">Default camera</option></select></div><label class="sm-check"><input data-flip type="checkbox"> Flip captured image horizontally</label></div>
      <div class="sm-card"><h4>Timeline</h4><div class="sm-row"><span>Target frame</span><div class="sm-frame"><button data-minus type="button">−</button><input data-frame type="number" min="1" step="1" value="1"><button data-plus type="button">+</button></div></div><label class="sm-check"><input data-auto type="checkbox" checked> Auto-advance after capture</label><div class="sm-row"><span>Layer</span><select data-layer></select></div></div>
      <div class="sm-card"><h4>Image controls</h4><div class="sm-slider"><span>Onion skin</span><input data-onion type="range" min="0" max="1" step="0.05" value="0.4"><output data-onion-out>40%</output></div><div class="sm-slider"><span>Brightness</span><input data-bright type="range" min="0.5" max="2.5" step="0.05" value="1.3"><output data-bright-out>130%</output></div><div class="sm-slider"><span>Contrast</span><input data-contrast type="range" min="0.5" max="2.5" step="0.05" value="1.1"><output data-contrast-out>110%</output></div></div>
      <div class="sm-card"><h4>Session</h4><div class="sm-actions"><button class="sm-btn" data-use-current type="button">Use current frame</button><button class="sm-btn" data-reset type="button">Reset count</button></div></div>
    </div><div class="sm-status" data-status>Press Start camera to begin.</div>`;
  document.body.appendChild(panel);LukeAnimate.registerFloatingPanel(panel);LukeAnimate.bringFloatingPanelToFront(panel);LukeAnimate.clampFloatingPanelToViewport(panel);LukeAnimate.setConfiguredToolActive(GROUP_ID,ITEM_ID,true);
  const q=s=>panel.querySelector(s),canvas=q("[data-preview]"),context=canvas.getContext("2d"),video=document.createElement("video");video.muted=true;video.playsInline=true;video.autoplay=true;
  let stream=null,deviceId="",frameTarget=Math.max(1,LukeAnimate.getCurrentFrame()),captured=0,onionCanvas=null,raf=0,captureLayerId="";
  const status=q("[data-status]"),capture=q("[data-capture]"),frameInput=q("[data-frame]");
  function setStatus(message,kind=""){status.textContent=message;status.className="sm-status"+(kind?" "+kind:"");}
  function setFrame(value){frameTarget=Math.max(1,Math.round(Number(value)||1));frameInput.value=frameTarget;q("[data-badge]").textContent="Frame "+frameTarget;}
  function resizePreview(){const rect=canvas.parentElement.getBoundingClientRect(),w=Math.max(1,Math.round(rect.width)),h=Math.max(1,Math.round(rect.height));if(canvas.width!==w)canvas.width=w;if(canvas.height!==h)canvas.height=h;}
  function nextLayerName(layers){const used=new Set((layers||[]).map(layer=>String(layer&&layer.name||"").trim().toLowerCase()));let number=1;while(used.has("layer "+number)||used.has("layer"+number))number++;return "Layer "+number;}
  function ensureCaptureLayer(){let layers=LukeAnimate.getDrawingLayers()||[],layer=layers.find(entry=>String(entry&&entry.id||"")===captureLayerId);if(layer)return layer;layer=LukeAnimate.createDrawingLayer(nextLayerName(layers),{pushUndo:true,refresh:false});if(!layer||!layer.id)throw new Error("Stop Mo could not create its timeline layer.");captureLayerId=String(layer.id);LukeAnimate.ensureTimelineFrameCount(frameTarget,{pushUndo:false,refresh:false});LukeAnimate.setDrawingLayerFrameObject(layer.id,frameTarget,null,{pushUndo:false,refresh:false});LukeAnimate.setFrame(frameTarget);return layer;}
  function refreshLayers(){const select=q("[data-layer]"),automatic=ensureCaptureLayer(),listed=LukeAnimate.getDrawingLayers()||[],layers=listed.some(layer=>String(layer&&layer.id||"")===String(automatic.id))?listed:listed.concat(automatic),wanted=layers.some(layer=>String(layer&&layer.id||"")===String(select.value||""))?String(select.value):String(automatic.id);select.innerHTML="";layers.forEach((layer,index)=>{const option=document.createElement("option");option.value=String(layer.id);option.textContent=String(layer.name||("Layer "+(index+1)));option.selected=String(layer.id)===wanted;select.appendChild(option);});select.value=wanted||String(automatic.id);}
  function drawPreview(){resizePreview();const w=canvas.width,h=canvas.height;context.clearRect(0,0,w,h);if(onionCanvas&&Number(q("[data-onion]").value)>0){const scale=Math.min(w/onionCanvas.width,h/onionCanvas.height),dw=onionCanvas.width*scale,dh=onionCanvas.height*scale;context.globalAlpha=Number(q("[data-onion]").value);context.drawImage(onionCanvas,(w-dw)/2,(h-dh)/2,dw,dh);context.globalAlpha=1;}if(stream&&video.readyState>=2){const vw=video.videoWidth||w,vh=video.videoHeight||h,scale=Math.max(w/vw,h/vh),dw=vw*scale,dh=vh*scale,dx=(w-dw)/2,dy=(h-dh)/2;context.filter=`brightness(${Number(q("[data-bright]").value)}) contrast(${Number(q("[data-contrast]").value)})`;if(q("[data-flip]").checked){context.save();context.translate(w,0);context.scale(-1,1);context.drawImage(video,dx,dy,dw,dh);context.restore();}else context.drawImage(video,dx,dy,dw,dh);context.filter="none";q("[data-empty]").style.display="none";}else q("[data-empty]").style.display="flex";raf=requestAnimationFrame(drawPreview);}
  async function populateDevices(){try{const devices=(await navigator.mediaDevices.enumerateDevices()).filter(entry=>entry.kind==="videoinput"),select=q("[data-device]");select.innerHTML='<option value="">Default camera</option>';devices.forEach((entry,index)=>{const option=document.createElement("option");option.value=entry.deviceId;option.textContent=entry.label||("Camera "+(index+1));option.selected=entry.deviceId===deviceId;select.appendChild(option);});}catch(error){}}
  function stopCamera(silent=false){if(stream)stream.getTracks().forEach(track=>track.stop());stream=null;video.srcObject=null;capture.disabled=true;if(!silent)setStatus("Camera stopped.");}
  async function startCamera(){stopCamera(true);try{stream=await navigator.mediaDevices.getUserMedia({video:{deviceId:deviceId?{exact:deviceId}:undefined,width:{ideal:1280},height:{ideal:720}},audio:false});video.srcObject=stream;await video.play();capture.disabled=false;setStatus("Camera ready — capture into Frame "+frameTarget+".","good");await populateDevices();}catch(error){setStatus("Camera could not start: "+(error.message||String(error)),"bad");}}
  function loadImage(dataUrl){return new Promise((resolve,reject)=>{const image=new Image();image.onload=()=>resolve(image);image.onerror=()=>reject(new Error("Captured image could not be decoded."));image.src=dataUrl;});}
  async function captureFrame(){if(!stream||video.readyState<2){setStatus("Start the camera before capturing.","bad");return;}capture.disabled=true;try{const capturedFrame=frameTarget,width=Math.max(1,LukeAnimate.width),height=Math.max(1,LukeAnimate.height),output=document.createElement("canvas");output.width=width;output.height=height;const out=output.getContext("2d"),vw=video.videoWidth||width,vh=video.videoHeight||height,scale=Math.max(width/vw,height/vh),dw=vw*scale,dh=vh*scale,dx=(width-dw)/2,dy=(height-dh)/2;out.filter=`brightness(${Number(q("[data-bright]").value)}) contrast(${Number(q("[data-contrast]").value)})`;if(q("[data-flip]").checked){out.translate(width,0);out.scale(-1,1);out.drawImage(video,dx,dy,dw,dh);}else out.drawImage(video,dx,dy,dw,dh);out.setTransform(1,0,0,1,0,0);out.filter="none";onionCanvas=output;const dataUrl=output.toDataURL("image/png"),name="Stop Motion "+String(capturedFrame).padStart(3,"0");let layerId=q("[data-layer]").value,layer=(LukeAnimate.getDrawingLayers()||[]).find(entry=>String(entry.id)===layerId);if(!layer){layer=ensureCaptureLayer();refreshLayers();layerId=String(layer.id);q("[data-layer]").value=layerId;}const originalLayerName=String(layer.name||"");const object=LukeAnimate.createImageObject({src:dataUrl,name},{name,src:dataUrl,naturalWidth:width,naturalHeight:height,width,height,x:0,y:0,mediaRole:"overlay"});LukeAnimate.ensureTimelineFrameCount(capturedFrame,{pushUndo:false,refresh:false});LukeAnimate.setDrawingLayerFrameObject(layer.id,capturedFrame,object,{refresh:false});if(originalLayerName)layer.name=originalLayerName;LukeAnimate.setFrame(capturedFrame);LukeAnimate.refreshUI();captured++;q("[data-flash]").style.opacity=".65";setTimeout(()=>q("[data-flash]").style.opacity="0",90);setStatus("Captured Frame "+capturedFrame+" on "+(layer.name||"the selected layer")+". Session total: "+captured+".","good");if(q("[data-auto]").checked)setFrame(capturedFrame+1);LukeAnimate.setFrame(capturedFrame);}catch(error){setStatus(error.message||String(error),"bad");}finally{capture.disabled=!stream;}}
  q("[data-start]").onclick=startCamera;q("[data-stop]").onclick=()=>stopCamera();capture.onclick=captureFrame;
  q("[data-device]").onchange=event=>{deviceId=event.target.value||"";if(stream)startCamera();};q("[data-minus]").onclick=()=>setFrame(frameTarget-1);q("[data-plus]").onclick=()=>setFrame(frameTarget+1);frameInput.onchange=()=>setFrame(frameInput.value);q("[data-use-current]").onclick=()=>setFrame(LukeAnimate.getCurrentFrame());q("[data-reset]").onclick=()=>{captured=0;setStatus("Session capture count reset.");};
  [["[data-onion]","[data-onion-out]"],["[data-bright]","[data-bright-out]"],["[data-contrast]","[data-contrast-out]"]].forEach(([input,output])=>q(input).oninput=()=>q(output).textContent=Math.round(Number(q(input).value)*100)+"%");
  panel.addEventListener("keydown",event=>{if(event.code==="Space"&&!/INPUT|SELECT|BUTTON/.test(document.activeElement&&document.activeElement.tagName||"")){event.preventDefault();captureFrame();}});
  q(".sm-close").onclick=()=>{cancelAnimationFrame(raf);stopCamera(true);panel.remove();LukeAnimate.setConfiguredToolActive(GROUP_ID,ITEM_ID,false);};
  window.addEventListener("resize",resizePreview);setFrame(frameTarget);refreshLayers();setStatus("Created "+(ensureCaptureLayer().name||"a new layer")+" automatically. Press Start camera to begin.","good");LukeAnimate.refreshUI();populateDevices();raf=requestAnimationFrame(drawPreview);
})();
