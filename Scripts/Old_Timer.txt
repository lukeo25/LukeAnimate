/* Luke Animate Old Timer Type Tool v1.2 */
(function(){
  "use strict";
  const PANEL_ID="lukeanimate-oldtimer-panel",STYLE_ID="lukeanimate-oldtimer-style",TOOL_ID="oldtimer";
  const GROUP_ID=String(group&&group.id?group.id:"type"),ITEM_ID=String(item&&(item.id||item.label)?(item.id||item.label):"Old Timer");
  const ICON=String(item&&item.iconUrl?item.iconUrl:"https://lukeo25.github.io/LukeAnimate/SVGs/Timer.svg");
  const DEFAULT_CODE=`const { config, value, done } = context;
return {
  value,
  text: context.format(value),
  fontFamily: config.fontFamily,
  characterDie: config.setCharacterDie && done ? config.characterDieValue : 0
};`;
  const SIDE_RUNNER_STATE_FRAMES=['Idle_Right','Idle_Left','Run_Right','Run_Left','Jump_Right','Jump_Left','Fire_Right','Fire_Left','Fight_Right','Fight_Left','Death'];
  if(!LukeAnimate||Number(LukeAnimate.version||0)<124||typeof LukeAnimate.setTypeTool!=="function")throw new Error("Old Timer requires Luke Animate v124 or later.");
  const selectTool=active=>{if(LukeAnimate.setConfiguredToolActive)LukeAnimate.setConfiguredToolActive(GROUP_ID,ITEM_ID,!!active);};
  const old=document.getElementById(PANEL_ID);
  if(old){old.style.display="flex";old.classList.add("open");selectTool(true);LukeAnimate.bringFloatingPanelToFront(old);LukeAnimate.clampFloatingPanelToViewport(old);return;}
  if(!document.getElementById(STYLE_ID)){
    const style=document.createElement("style");style.id=STYLE_ID;style.textContent=`
      #${PANEL_ID}{position:fixed;z-index:10000;left:28px;top:18px;width:390px;max-width:calc(100vw - 16px);height:min(735px,calc(100vh - 36px));display:flex;flex-direction:column;background:#f1f3f6;border:1px solid #bfc6cf;border-radius:14px;box-shadow:0 20px 60px rgba(0,0,0,.3);color:#27313d;font:13px Inter,-apple-system,BlinkMacSystemFont,Segoe UI,sans-serif;overflow:hidden}
      #${PANEL_ID} *{box-sizing:border-box} #${PANEL_ID} .ot-head{display:flex;align-items:center;gap:9px;padding:8px 10px;background:#fff;border-bottom:1px solid #d3d8df;cursor:move;user-select:none}
      #${PANEL_ID} .ot-head img{width:27px;height:27px;object-fit:contain;filter:grayscale(1) brightness(.72)} #${PANEL_ID} .ot-title{flex:1} #${PANEL_ID} .ot-title strong{display:block;font-size:14px;color:#29434a} #${PANEL_ID} .ot-title span{font-size:9px;letter-spacing:.11em;text-transform:uppercase;color:#7b8491}
      #${PANEL_ID} .ot-close{width:32px;height:32px;border:1px solid #bbc3cd;border-radius:8px;background:#fff;font-size:20px;color:#566270;cursor:pointer} #${PANEL_ID} .ot-body{flex:1;min-height:0;padding:8px 7px 46px;overflow:auto}
      #${PANEL_ID} .ot-selected,#${PANEL_ID} .ot-card{padding:8px 9px;margin-bottom:7px;border:1px solid #d2d7de;border-radius:10px;background:#fff} #${PANEL_ID} .ot-selected{white-space:nowrap;overflow:hidden;text-overflow:ellipsis} #${PANEL_ID} .ot-selected strong{color:#0e8f7c}
      #${PANEL_ID} .ot-grid{display:grid;grid-template-columns:1fr 1fr;gap:7px} #${PANEL_ID} .ot-field{display:flex;flex-direction:column;gap:4px;font-size:11px;font-weight:700;color:#596674} #${PANEL_ID} .ot-field.wide{grid-column:1/-1}
      #${PANEL_ID} input[type=text],#${PANEL_ID} input[type=number],#${PANEL_ID} select{width:100%;padding:7px;border:1px solid #b9c3ce;border-radius:6px;background:#fff;color:#263442;font:13px Arial,sans-serif} #${PANEL_ID} input[type=checkbox]{accent-color:#0e9c86}
      #${PANEL_ID} .ot-check{display:flex;align-items:center;gap:7px;padding:7px;border:1px solid #d5dbe2;border-radius:7px;background:#f8fafb;font-size:11px} #${PANEL_ID} .ot-checks{display:grid;grid-template-columns:1fr 1fr;gap:7px}
      #${PANEL_ID} .ot-muted{color:#74808d;font-size:11px;line-height:1.35} #${PANEL_ID} details{margin-top:7px;border:1px solid #d2d7de;border-radius:10px;background:#fff;overflow:hidden} #${PANEL_ID} summary{padding:8px 9px;cursor:pointer;font-size:11px;letter-spacing:.08em;text-transform:uppercase;font-weight:700;color:#66717e}
      #${PANEL_ID} .ot-code{padding:9px} #${PANEL_ID} textarea{width:100%;min-height:135px;resize:vertical;padding:8px;border:1px solid #aeb8c4;border-radius:6px;background:#17212b;color:#e7f5f2;font:12px/1.45 Consolas,monospace} #${PANEL_ID} .ot-code-tools{display:flex;justify-content:flex-end;margin-top:6px}
      #${PANEL_ID} .ot-actions{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:10px} #${PANEL_ID} .ot-btn{padding:7px;border:1px solid #b8c0ca;border-radius:8px;background:#eceff2;color:#4b5766;font-size:10px;cursor:pointer} #${PANEL_ID} .ot-btn:hover{border-color:#0e9c86} #${PANEL_ID} .ot-btn.primary{border-color:#0e9c86;background:#0e9c86;color:#fff;font-weight:700} #${PANEL_ID} .ot-btn:disabled{opacity:.45;cursor:not-allowed}
      #${PANEL_ID} .ot-status{position:absolute;left:7px;right:7px;bottom:7px;min-height:26px;max-height:42px;overflow:auto;border:1px solid #cbd2da;border-radius:8px;background:rgba(255,255,255,.97);padding:8px 10px;font-size:10px;color:#596573} #${PANEL_ID} .ot-status.good{border-color:#0e9c86;color:#117d6d} #${PANEL_ID} .ot-status.bad{border-color:#c66a6a;color:#a33d3d} #${PANEL_ID} .ot-status.warn{border-color:#d4a45c;color:#8a5a12}
      @media(max-width:560px){#${PANEL_ID}{left:8px;top:8px;width:calc(100vw - 16px);height:calc(100vh - 16px)}}`;
    document.head.appendChild(style);
  }
  const panel=document.createElement("section");panel.id=PANEL_ID;panel.className="open";panel.innerHTML=`
    <div class="ot-head" data-floating-panel-drag><img src="${ICON.replace(/"/g,"&quot;")}" alt=""><div class="ot-title"><strong>Old Timer</strong><span>Countdown and countup timer</span></div><button class="ot-close" type="button" title="Close">×</button></div>
    <div class="ot-body"><div class="ot-selected"><strong>Selected:</strong> <span data-ot-selected>No Clip selected</span></div>
      <p class="ot-muted">Select a Clip containing text. Old Timer updates that text without requiring an object name and publishes the numeric timer value for game code.</p>
      <div class="ot-card ot-grid">
        <label class="ot-field"><span>Mode</span><select data-ot-mode><option value="countdown">Countdown</option><option value="countup">Count up</option></select></label>
        <label class="ot-field"><span>Display</span><select data-ot-display><option value="seconds">Minutes : seconds</option><option value="tenths">Tenths</option><option value="hundredths">Hundredths</option></select></label>
        <label class="ot-field"><span>Start seconds</span><input data-ot-start type="number" min="0" value="30" step="0.1"></label>
        <label class="ot-field"><span>End seconds</span><input data-ot-end type="number" min="0" value="0" step="0.1"></label>
        <label class="ot-field wide"><span>Text object</span><select data-ot-text><option value="">Select a Clip containing text</option></select></label>
        <label class="ot-field"><span>Timer variable</span><input data-ot-variable type="text" value="LT_Time"></label>
        <label class="ot-field"><span>Font</span><input data-ot-font type="text" value="Poppins" list="lukeanimate-oldtimer-fonts"><datalist id="lukeanimate-oldtimer-fonts"><option>Poppins</option><option>Montserrat</option><option>Roboto</option><option>Open Sans</option><option>Inter</option><option>Orbitron</option><option>Press Start 2P</option></datalist></label>
        <label class="ot-field wide"><span>Go to Clip frame when complete (blank = none)</span><input data-ot-goto type="number" min="1" placeholder="No frame change"></label>
      </div>
      <div class="ot-card ot-checks"><label class="ot-check"><input data-ot-loop type="checkbox"> Loop timer</label><label class="ot-check"><input data-ot-stop type="checkbox" checked> Stop Clip when complete</label><label class="ot-check"><input data-ot-console type="checkbox"> Console logs</label><label class="ot-check"><input data-ot-character-die type="checkbox"> Set Character_Die on complete</label></div>
      <div class="ot-card ot-grid">
        <label class="ot-field wide"><span>Affected Side Runner Clip</span><select data-ot-side-runner><option value="">Select a Side Runner Clip</option></select></label>
        <label class="ot-field"><span>Character_Die number</span><input data-ot-character-die-value type="number" value="1" step="1"></label>
        <label class="ot-field"><span>Then Side Runner does</span><select data-ot-death-action><option value="goto_frame">Go to Clip frame</option><option value="destroy">Destroy / hide sprite</option><option value="reappear">Destroy, then reappear</option><option value="reset_position">Reset to starting position</option></select></label>
        <label class="ot-field" data-ot-death-frame-field><span>Side Runner Clip frame</span><input data-ot-death-frame type="number" min="1" value="1" step="1"></label>
        <label class="ot-field" data-ot-reappear-delay-field><span>Reappear after (s)</span><input data-ot-reappear-delay type="number" min="0" value="1" step="0.1"></label>
        <label class="ot-check wide" data-ot-reset-position-field><input data-ot-reset-position type="checkbox" checked> Return Side Runner to its starting position</label>
        <p class="ot-muted wide">Old Timer writes the chosen number directly into this Side Runner's live loop. It resets Character_Die to 0 when playback or a timer loop restarts.</p>
      </div>
      <details><summary>View / edit code</summary><div class="ot-code"><p class="ot-muted">Return value, text, fontFamily and/or characterDie. The context provides config, value, done, format, time, readValue and writeValue.</p><textarea data-ot-code spellcheck="false"></textarea><div class="ot-code-tools"><button class="ot-btn" data-ot-reset type="button">Reset code</button></div></div></details>
      <div class="ot-actions"><button class="ot-btn" data-ot-remove type="button">Remove</button><button class="ot-btn primary" data-ot-apply type="button">Apply</button></div><div class="ot-status" data-ot-status></div>
    </div>`;
  document.body.appendChild(panel);LukeAnimate.registerFloatingPanel(panel);LukeAnimate.bringFloatingPanelToFront(panel);LukeAnimate.clampFloatingPanelToViewport(panel);selectTool(true);
  const get=q=>panel.querySelector(q),status=get('[data-ot-status]'),apply=get('[data-ot-apply]'),remove=get('[data-ot-remove]');let selected=null,key='';
  const setStatus=(message,kind)=>{status.textContent=String(message||'');status.className='ot-status'+(kind?' '+kind:'');};
  function selectedClip(){const list=LukeAnimate.getSelectedDrawObjects()||[],clips=LukeAnimate.getClips?LukeAnimate.getClips():{};return list.length===1&&list[0]&&list[0].type==='clipinstance'&&clips[list[0].clipId]?list[0]:null;}
  function nameOf(object){const clips=LukeAnimate.getClips?LukeAnimate.getClips():{};return object?String(object.name||(clips[object.clipId]&&clips[object.clipId].name)||'Clip'):'No Clip selected';}
  function textObjects(object){const clips=LukeAnimate.getClips?LukeAnimate.getClips():{},clip=object&&clips[object.clipId],found=[],seen=new Set(),ids=new Set();function visit(value){if(!value||typeof value!=='object'||seen.has(value))return;seen.add(value);if(value.type==='text'){const id=String(value.id||'');if(id&&!ids.has(id)){ids.add(id);const preview=String(value.text||'').replace(/\s+/g,' ').trim().slice(0,42)||'Untitled text';found.push({id,object:value,label:'Text '+(found.length+1)+' — '+preview});}}if(Array.isArray(value.children))value.children.forEach(visit);}if(clip){(clip.drawObjects||[]).forEach(visit);(clip.sceneFrames||[]).forEach(frame=>(frame&&frame.objects||[]).forEach(visit));(clip.drawingLayers||[]).forEach(layer=>(layer&&layer.frames||[]).forEach(frame=>visit(frame&&frame.object)));}return found;}
  function fillTextObjects(object){const choices=textObjects(object),select=get('[data-ot-text]');select.innerHTML=choices.length?choices.map(choice=>`<option value="${choice.id.replace(/"/g,'&quot;')}">${choice.label.replace(/&/g,'&amp;').replace(/</g,'&lt;')}</option>`).join(''):'<option value="">Select a Clip containing text</option>';select.disabled=!choices.length;return choices;}
  function sideRunnerChoices(owner){return(LukeAnimate.getDrawObjects?LukeAnimate.getDrawObjects():[]).filter(object=>object&&object!==owner&&object.type==='clipinstance'&&LukeAnimate.getTypeTool(object,'siderunner')).map(object=>({id:String(object.id||''),object,label:nameOf(object)})).filter(choice=>choice.id);}
  function deathFrameFor(object){const labels=LukeAnimate.getClipFrameLabels?LukeAnimate.getClipFrameLabels(object&&object.clipId):[];const death=(labels||[]).find(label=>String(label&&label.name||'').toLowerCase()==='death');return death?Math.max(1,Number(death.frame)||1):1;}
  function fillSideRunners(owner){const choices=sideRunnerChoices(owner),select=get('[data-ot-side-runner]');select.innerHTML='<option value="">Select a Side Runner Clip</option>'+choices.map(choice=>`<option value="${choice.id.replace(/"/g,'&quot;')}">${choice.label.replace(/&/g,'&amp;').replace(/</g,'&lt;')}</option>`).join('');select.disabled=!choices.length;return choices;}
  function updateDeathFields(){const action=get('[data-ot-death-action]').value;get('[data-ot-death-frame-field]').style.display=action==='goto_frame'?'flex':'none';get('[data-ot-reappear-delay-field]').style.display=action==='reappear'?'flex':'none';get('[data-ot-reset-position-field]').style.display=action==='reappear'?'flex':'none';}
  function load(settings,choices,runners){const c=settings||{},available=choices||[],sideRunners=runners||[],hasSettings=!!settings;get('[data-ot-mode]').value=String(c.mode||'countdown');get('[data-ot-display]').value=String(c.displayMode||'seconds');get('[data-ot-start]').value=String(c.startSeconds===undefined?30:c.startSeconds);get('[data-ot-end]').value=String(c.endSeconds===undefined?0:c.endSeconds);let textId=String(c.textObjectId||'');if(!available.some(choice=>choice.id===textId))textId=available.length?available[0].id:'';get('[data-ot-text]').value=textId;get('[data-ot-variable]').value=String(c.varName||'LT_Time');const chosen=available.find(choice=>choice.id===textId);get('[data-ot-font]').value=String(c.fontFamily||(chosen&&chosen.object.fontFamily)||'Poppins');get('[data-ot-goto]').value=c.gotoFrameOnComplete===null||c.gotoFrameOnComplete===undefined?'':String(c.gotoFrameOnComplete);get('[data-ot-loop]').checked=!!c.loop;get('[data-ot-stop]').checked=c.stopOnComplete!==false;get('[data-ot-console]').checked=!!c.showConsole;let runnerId=String(c.targetSideRunnerId||'');if(!sideRunners.some(choice=>choice.id===runnerId)&&sideRunners.length===1)runnerId=sideRunners[0].id;get('[data-ot-side-runner]').value=runnerId;get('[data-ot-character-die]').checked=hasSettings?!!c.setCharacterDie:!!runnerId;get('[data-ot-character-die-value]').value=String(c.characterDieValue===undefined?1:c.characterDieValue);get('[data-ot-death-action]').value=String(c.targetDeathAction||'goto_frame');const runnerChoice=sideRunners.find(choice=>choice.id===runnerId);get('[data-ot-death-frame]').value=String(c.targetDeathFrame||(runnerChoice?deathFrameFor(runnerChoice.object):1));get('[data-ot-reappear-delay]').value=String(c.targetReappearDelay===undefined?1:c.targetReappearDelay);get('[data-ot-reset-position]').checked=c.targetResetPosition!==false;updateDeathFields();get('[data-ot-code]').value=String(c.code||DEFAULT_CODE);}
  function refresh(force){const next=selectedClip(),runners=sideRunnerChoices(next),nextKey=(next?String(next.id||next.clipId):'')+'|'+runners.map(choice=>choice.id).join(',');if(!force&&nextKey===key){if(next){const error=LukeAnimate.getTypeToolError(next,TOOL_ID);if(error)setStatus('Code error: '+error,'bad');}return;}selected=next;key=nextKey;get('[data-ot-selected]').textContent=nameOf(next);const choices=fillTextObjects(next);fillSideRunners(next);const settings=next?LukeAnimate.getTypeTool(next,TOOL_ID):null;apply.disabled=!next||!choices.length;remove.disabled=!settings;load(settings,choices,runners);setStatus(next?(settings?'Old Timer is attached to this Clip.':'Ready to attach Old Timer.'):'Select one Clip instance on the stage.',next?'good':'');}
  function settings(){const gotoValue=String(get('[data-ot-goto]').value||'').trim(),textChoice=textObjects(selected).find(item=>item.id===get('[data-ot-text]').value),textObject=textChoice&&textChoice.object;return{enabled:true,mode:get('[data-ot-mode]').value,displayMode:get('[data-ot-display]').value,startSeconds:Number(get('[data-ot-start]').value),endSeconds:Number(get('[data-ot-end]').value),textObjectId:get('[data-ot-text]').value,textLayerId:String(textObject&&textObject.timelineLayerId||''),textName:String(textObject&&textObject.name||''),varName:get('[data-ot-variable]').value,fontFamily:get('[data-ot-font]').value,gotoFrameOnComplete:gotoValue===''?null:Number(gotoValue),loop:get('[data-ot-loop]').checked,stopOnComplete:get('[data-ot-stop]').checked,showConsole:get('[data-ot-console]').checked,setCharacterDie:get('[data-ot-character-die]').checked,characterDieValue:Number(get('[data-ot-character-die-value]').value),targetSideRunnerId:get('[data-ot-side-runner]').value,targetDeathAction:get('[data-ot-death-action]').value,targetDeathFrame:Number(get('[data-ot-death-frame]').value),targetReappearDelay:Number(get('[data-ot-reappear-delay]').value),targetResetPosition:get('[data-ot-reset-position]').checked,code:get('[data-ot-code]').value};}
  get('[data-ot-text]').addEventListener('change',()=>{const choice=textObjects(selected).find(item=>item.id===get('[data-ot-text]').value);if(choice&&choice.object.fontFamily)get('[data-ot-font]').value=choice.object.fontFamily;});
  get('[data-ot-death-action]').addEventListener('change',updateDeathFields);
  get('[data-ot-side-runner]').addEventListener('change',()=>{const choice=sideRunnerChoices(selected).find(item=>item.id===get('[data-ot-side-runner]').value);if(!choice)return;get('[data-ot-character-die]').checked=true;const runner=LukeAnimate.getTypeTool(choice.object,'siderunner')||{};get('[data-ot-character-die-value]').value=String(runner.deathValue===undefined?1:runner.deathValue);get('[data-ot-death-action]').value=String(runner.deathAction||'goto_frame');get('[data-ot-death-frame]').value=String(runner.deathFrame||deathFrameFor(choice.object));get('[data-ot-reappear-delay]').value=String(runner.reappearDelay===undefined?1:runner.reappearDelay);get('[data-ot-reset-position]').checked=runner.resetPositionOnReappear!==false;updateDeathFields();});
  get('[data-ot-reset]').addEventListener('click',()=>{get('[data-ot-code]').value=DEFAULT_CODE;setStatus('Old Timer code reset. Click Apply to save it.','good');});
  apply.addEventListener('click',()=>{refresh(false);if(!selected){setStatus('Select one Clip first.','bad');return;}const value=settings();if(!value.textObjectId){setStatus('Choose a text object inside the Clip.','bad');return;}const target=sideRunnerChoices(selected).find(choice=>choice.id===value.targetSideRunnerId);if(target){if(LukeAnimate.ensureClipStateFrames)LukeAnimate.ensureClipStateFrames(target.object.clipId,SIDE_RUNNER_STATE_FRAMES);if(value.targetDeathAction==='goto_frame'&&value.targetDeathFrame<=1)value.targetDeathFrame=deathFrameFor(target.object);const runner=LukeAnimate.getTypeTool(target.object,'siderunner')||{};Object.assign(runner,{deathEnabled:value.setCharacterDie,deathVariable:'Character_Die',deathValue:value.characterDieValue,deathAction:value.targetDeathAction,deathFrame:value.targetDeathFrame,reappearDelay:value.targetReappearDelay,resetPositionOnReappear:value.targetResetPosition});LukeAnimate.setTypeTool(target.object,'siderunner',runner);}LukeAnimate.setTypeTool(selected,TOOL_ID,value);remove.disabled=false;if(value.setCharacterDie&&!target)setStatus('Timer applied and text is live. Choose a Side Runner Clip to use Character_Die.','warn');else setStatus(target?'Old Timer linked to '+target.label+'. Press Play to test it.':'Old Timer applied. Press Play to test it.','good');});
  remove.addEventListener('click',()=>{refresh(false);if(!selected||!LukeAnimate.removeTypeTool(selected,TOOL_ID)){setStatus('The selected Clip does not have Old Timer.','bad');return;}remove.disabled=true;const runners=fillSideRunners(selected);load(null,textObjects(selected),runners);setStatus('Old Timer removed.','good');});
  const timer=setInterval(()=>refresh(false),250);get('.ot-close').addEventListener('click',()=>{clearInterval(timer);panel.remove();selectTool(false);});refresh(true);
})();
