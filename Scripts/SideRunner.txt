/* Luke Animate Side Runner Type Tool v1.2 */
(function(){
  "use strict";
  const PANEL_ID="lukeanimate-siderunner-panel",STYLE_ID="lukeanimate-siderunner-style",TOOL_ID="siderunner";
  const GROUP_ID=String(group&&group.id?group.id:"type"),ITEM_ID=String(item&&(item.id||item.label)?(item.id||item.label):"Side_Runner");
  const ICON=String(item&&item.iconUrl?item.iconUrl:"https://lukeo25.github.io/LukeAnimate/SVGs/GameCharacter.svg");
  const DEFAULT_CODE=`const { velocity } = context;
return { vx: velocity.x, vy: velocity.y };`;
  const DEFAULT_STATE_FRAMES=['Idle_Right','Idle_Left','Run_Right','Run_Left','Jump_Right','Jump_Left','Fire_Right','Fire_Left','Fight_Right','Fight_Left','Death'];
  if(!LukeAnimate||Number(LukeAnimate.version||0)<124)throw new Error("Side Runner requires Luke Animate v124 or later.");
  const selectTool=active=>{if(LukeAnimate.setConfiguredToolActive)LukeAnimate.setConfiguredToolActive(GROUP_ID,ITEM_ID,!!active);};
  const old=document.getElementById(PANEL_ID);
  if(old){old.style.display="flex";old.classList.add("open");selectTool(true);LukeAnimate.bringFloatingPanelToFront(old);LukeAnimate.clampFloatingPanelToViewport(old);return;}
  if(!document.getElementById(STYLE_ID)){
    const style=document.createElement("style");style.id=STYLE_ID;style.textContent=`
      #${PANEL_ID}{position:fixed;z-index:10000;left:28px;top:18px;width:390px;max-width:calc(100vw - 16px);height:min(720px,calc(100vh - 36px));display:flex;flex-direction:column;background:#f1f3f6;border:1px solid #bfc6cf;border-radius:14px;box-shadow:0 20px 60px rgba(0,0,0,.3);color:#27313d;font:13px Inter,-apple-system,BlinkMacSystemFont,Segoe UI,sans-serif;overflow:hidden}
      #${PANEL_ID} *{box-sizing:border-box} #${PANEL_ID} .sr-head{display:flex;align-items:center;gap:9px;padding:8px 10px;background:#fff;border-bottom:1px solid #d3d8df;cursor:move;user-select:none}
      #${PANEL_ID} .sr-head img{width:27px;height:27px;object-fit:contain;filter:grayscale(1) brightness(.72)} #${PANEL_ID} .sr-title{flex:1} #${PANEL_ID} .sr-title strong{display:block;font-size:14px;color:#29434a} #${PANEL_ID} .sr-title span{font-size:9px;letter-spacing:.11em;text-transform:uppercase;color:#7b8491}
      #${PANEL_ID} .sr-close{width:32px;height:32px;border:1px solid #bbc3cd;border-radius:8px;background:#fff;font-size:20px;color:#566270;cursor:pointer} #${PANEL_ID} .sr-body{flex:1;min-height:0;padding:8px 7px 46px;overflow:auto}
      #${PANEL_ID} .sr-selected,#${PANEL_ID} .sr-card{padding:8px 9px;margin-bottom:7px;border:1px solid #d2d7de;border-radius:10px;background:#fff} #${PANEL_ID} .sr-selected{white-space:nowrap;overflow:hidden;text-overflow:ellipsis} #${PANEL_ID} .sr-selected strong{color:#0e8f7c}
      #${PANEL_ID} .sr-field{display:grid;grid-template-columns:112px 1fr 66px;gap:7px;align-items:center;margin:7px 0} #${PANEL_ID} .sr-field label{font-size:11px;font-weight:700;color:#596674} #${PANEL_ID} input[type=range]{width:100%;accent-color:#0e9c86} #${PANEL_ID} input[type=number],#${PANEL_ID} input[type=text],#${PANEL_ID} select{width:100%;padding:6px;border:1px solid #b9c3ce;border-radius:6px;background:#fff;color:#263442}
      #${PANEL_ID} .sr-death-grid{display:grid;grid-template-columns:1fr 1fr;gap:7px;margin-top:8px} #${PANEL_ID} .sr-death-field{display:flex;flex-direction:column;gap:4px;font-size:11px;font-weight:700;color:#596674} #${PANEL_ID} .sr-death-grid .wide{grid-column:1/-1}
      #${PANEL_ID} .sr-checks{display:grid;grid-template-columns:1fr 1fr;gap:7px} #${PANEL_ID} .sr-check{display:flex;align-items:center;gap:7px;padding:7px;border:1px solid #d5dbe2;border-radius:7px;background:#f8fafb;font-size:11px} #${PANEL_ID} input[type=checkbox],#${PANEL_ID} input[type=radio]{accent-color:#0e9c86}
      #${PANEL_ID} .sr-facing{display:flex;align-items:center;gap:10px;margin-bottom:8px;font-size:11px} #${PANEL_ID} .sr-muted{color:#74808d;font-size:11px;line-height:1.35}
      #${PANEL_ID} details{margin-top:7px;border:1px solid #d2d7de;border-radius:10px;background:#fff;overflow:hidden} #${PANEL_ID} summary{padding:8px 9px;cursor:pointer;font-size:11px;letter-spacing:.08em;text-transform:uppercase;font-weight:700;color:#66717e}
      #${PANEL_ID} .sr-code{padding:9px} #${PANEL_ID} textarea{width:100%;min-height:120px;resize:vertical;padding:8px;border:1px solid #aeb8c4;border-radius:6px;background:#17212b;color:#e7f5f2;font:12px/1.45 Consolas,monospace} #${PANEL_ID} .sr-code-tools{display:flex;justify-content:flex-end;margin-top:6px}
      #${PANEL_ID} .sr-actions{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:10px} #${PANEL_ID} .sr-btn{padding:7px;border:1px solid #b8c0ca;border-radius:8px;background:#eceff2;color:#4b5766;font-size:10px;cursor:pointer} #${PANEL_ID} .sr-btn:hover{border-color:#0e9c86} #${PANEL_ID} .sr-btn.primary{border-color:#0e9c86;background:#0e9c86;color:#fff;font-weight:700} #${PANEL_ID} .sr-btn:disabled{opacity:.45;cursor:not-allowed}
      #${PANEL_ID} .sr-status{position:absolute;left:7px;right:7px;bottom:7px;min-height:26px;max-height:42px;overflow:auto;border:1px solid #cbd2da;border-radius:8px;background:rgba(255,255,255,.97);padding:8px 10px;font-size:10px;color:#596573} #${PANEL_ID} .sr-status.good{border-color:#0e9c86;color:#117d6d} #${PANEL_ID} .sr-status.bad{border-color:#c66a6a;color:#a33d3d}
      @media(max-width:560px){#${PANEL_ID}{left:8px;top:8px;width:calc(100vw - 16px);height:calc(100vh - 16px)}}`;
    document.head.appendChild(style);
  }
  const fields=[['speed','Move speed',0,100,.25,3],['acceleration','Acceleration',.05,10,.05,.35],['friction','Slide friction',.01,2,.01,.12],['airControl','Air control',0,1,.05,.35],['jumpStrength','Jump strength',0,100,.5,9],['gravity','Gravity',0,100,.1,.6],['riseGravity','Rise gravity',.05,5,.05,.65],['fallGravity','Fall gravity',.05,5,.05,1.35],['maxFall','Max fall speed',0,100,.5,12]];
  const panel=document.createElement("section");panel.id=PANEL_ID;panel.className="open";
  panel.innerHTML=`<div class="sr-head" data-floating-panel-drag><img src="${ICON.replace(/"/g,"&quot;")}" alt=""><div class="sr-title"><strong>Side Runner</strong><span>Platform character controller</span></div><button class="sr-close" type="button" title="Close">×</button></div><div class="sr-body">
    <div class="sr-selected"><strong>Selected:</strong> <span data-sr-selected>No Clip selected</span></div>
    <div class="sr-card"><div class="sr-facing"><strong>Selected artwork faces</strong><label><input type="radio" name="sr-facing" value="right" checked> Right</label><label><input type="radio" name="sr-facing" value="left"> Left</label></div>${fields.map(f=>`<div class="sr-field"><label>${f[1]}</label><input data-sr-range="${f[0]}" type="range" min="${f[2]}" max="${f[3]}" step="${f[4]}" value="${f[5]}"><input data-sr-number="${f[0]}" type="number" min="${f[2]}" max="${f[3]}" step="${f[4]}" value="${f[5]}"></div>`).join('')}</div>
    <div class="sr-card sr-checks"><label class="sr-check"><input data-sr-check="enableLR" type="checkbox" checked> Left/right movement</label><label class="sr-check"><input data-sr-check="enableJump" type="checkbox" checked> Jump</label><label class="sr-check"><input data-sr-check="enableGravity" type="checkbox" checked> Gravity and falling</label><label class="sr-check"><input data-sr-check="animateStates" type="checkbox" checked> Use named state frames</label></div>
    <p class="sr-muted">Arrow keys or A/D move. Space, W or Up jumps. Applying Side Runner adds editable named frames for idle, run, jump, fire, fight and death in both directions while preserving existing frames.</p>
    <div class="sr-card"><label class="sr-check"><input data-sr-death-enabled type="checkbox"> Respond to a death value</label><div class="sr-death-grid">
      <label class="sr-death-field"><span>If variable</span><input data-sr-death-variable type="text" value="Character_Die"></label>
      <label class="sr-death-field"><span>equals number</span><input data-sr-death-value type="number" value="1" step="1"></label>
      <label class="sr-death-field wide"><span>Then do</span><select data-sr-death-action><option value="goto_frame">Go to Clip frame</option><option value="destroy">Destroy / hide sprite</option><option value="reappear">Destroy, then reappear</option><option value="reset_position">Reset to starting position</option></select></label>
      <label class="sr-death-field" data-sr-frame-field><span>Clip frame</span><input data-sr-death-frame type="number" min="1" value="1" step="1"></label>
      <label class="sr-death-field" data-sr-delay-field><span>Reappear after (s)</span><input data-sr-reappear-delay type="number" min="0" value="1" step="0.1"></label>
      <label class="sr-check wide" data-sr-reset-position-field><input data-sr-reset-position type="checkbox" checked> Return to starting position when reappearing</label>
    </div></div>
    <details><summary>View / edit code</summary><div class="sr-code"><p class="sr-muted">This optional hook runs after built-in input and gravity. Return <code>{ vx, vy }</code> to alter velocity.</p><textarea data-sr-code spellcheck="false"></textarea><div class="sr-code-tools"><button class="sr-btn" data-sr-reset type="button">Reset code</button></div></div></details>
    <div class="sr-actions"><button class="sr-btn" data-sr-remove type="button">Remove</button><button class="sr-btn primary" data-sr-apply type="button">Apply</button></div><div class="sr-status" data-sr-status></div></div>`;
  document.body.appendChild(panel);LukeAnimate.registerFloatingPanel(panel);LukeAnimate.bringFloatingPanelToFront(panel);LukeAnimate.clampFloatingPanelToViewport(panel);selectTool(true);
  const get=q=>panel.querySelector(q),status=get('[data-sr-status]'),apply=get('[data-sr-apply]'),remove=get('[data-sr-remove]');let selected=null,key='';
  const setStatus=(m,k)=>{status.textContent=String(m||'');status.className='sr-status'+(k?' '+k:'');};
  fields.forEach(f=>{const range=get(`[data-sr-range="${f[0]}"]`),number=get(`[data-sr-number="${f[0]}"]`),sync=value=>{const n=Math.max(f[2],Math.min(f[3],Number(value)));range.value=number.value=String(Number.isFinite(n)?n:f[5]);};range.addEventListener('input',()=>sync(range.value));number.addEventListener('input',()=>sync(number.value));});
  function selectedClip(){const list=LukeAnimate.getSelectedDrawObjects()||[],clips=LukeAnimate.getClips?LukeAnimate.getClips():{};return list.length===1&&list[0]&&list[0].type==='clipinstance'&&clips[list[0].clipId]?list[0]:null;}
  function nameOf(obj){const clips=LukeAnimate.getClips?LukeAnimate.getClips():{};return obj?String(obj.name||(clips[obj.clipId]&&clips[obj.clipId].name)||'Clip'):'No Clip selected';}
  function updateDeathFields(){const action=get('[data-sr-death-action]').value;get('[data-sr-frame-field]').style.display=action==='goto_frame'?'flex':'none';get('[data-sr-delay-field]').style.display=action==='reappear'?'flex':'none';get('[data-sr-reset-position-field]').style.display=action==='reappear'?'flex':'none';}
  function load(settings){const c=settings||{};fields.forEach(f=>{const value=c[f[0]]===undefined?f[5]:c[f[0]];get(`[data-sr-range="${f[0]}"]`).value=get(`[data-sr-number="${f[0]}"]`).value=String(value);});['enableLR','enableJump','enableGravity','animateStates'].forEach(id=>get(`[data-sr-check="${id}"]`).checked=c[id]!==false);const facing=c.selectedFacing==='left'?'left':'right';get(`input[name="sr-facing"][value="${facing}"]`).checked=true;get('[data-sr-death-enabled]').checked=!!c.deathEnabled;get('[data-sr-death-variable]').value=String(c.deathVariable||'Character_Die');get('[data-sr-death-value]').value=String(c.deathValue===undefined?1:c.deathValue);get('[data-sr-death-action]').value=String(c.deathAction||'goto_frame');get('[data-sr-death-frame]').value=String(c.deathFrame||1);get('[data-sr-reappear-delay]').value=String(c.reappearDelay===undefined?1:c.reappearDelay);get('[data-sr-reset-position]').checked=c.resetPositionOnReappear!==false;updateDeathFields();get('[data-sr-code]').value=String(c.code||DEFAULT_CODE);}
  function refresh(force){const next=selectedClip(),nextKey=next?String(next.id||next.clipId):'';if(!force&&nextKey===key){if(next){const e=LukeAnimate.getTypeToolError(next,TOOL_ID);if(e)setStatus('Code error: '+e,'bad');}return;}selected=next;key=nextKey;get('[data-sr-selected]').textContent=nameOf(next);apply.disabled=!next;const settings=next?LukeAnimate.getTypeTool(next,TOOL_ID):null;remove.disabled=!settings;load(settings);setStatus(next?(settings?'Side Runner is attached to this Clip.':'Ready to attach Side Runner.'):'Select one Clip instance on the stage.',next?'good':'');}
  function settings(){const out={enabled:true,selectedFacing:get('input[name="sr-facing"]:checked').value,deathEnabled:get('[data-sr-death-enabled]').checked,deathVariable:get('[data-sr-death-variable]').value,deathValue:Number(get('[data-sr-death-value]').value),deathAction:get('[data-sr-death-action]').value,deathFrame:Number(get('[data-sr-death-frame]').value),reappearDelay:Number(get('[data-sr-reappear-delay]').value),resetPositionOnReappear:get('[data-sr-reset-position]').checked,code:get('[data-sr-code]').value};fields.forEach(f=>out[f[0]]=Number(get(`[data-sr-number="${f[0]}"]`).value));['enableLR','enableJump','enableGravity','animateStates'].forEach(id=>out[id]=get(`[data-sr-check="${id}"]`).checked);return out;}
  get('[data-sr-death-action]').addEventListener('change',updateDeathFields);
  get('[data-sr-reset]').addEventListener('click',()=>{get('[data-sr-code]').value=DEFAULT_CODE;setStatus('Code reset. Click Apply to save it.','good');});
  apply.addEventListener('click',()=>{refresh(false);if(!selected){setStatus('Select one Clip first.','bad');return;}if(LukeAnimate.ensureClipStateFrames)LukeAnimate.ensureClipStateFrames(selected.clipId,DEFAULT_STATE_FRAMES);LukeAnimate.setTypeTool(selected,TOOL_ID,settings());remove.disabled=false;setStatus('Side Runner applied with idle, run, jump, fire, fight and death frames. Press Play to test it.','good');});
  remove.addEventListener('click',()=>{refresh(false);if(!selected||!LukeAnimate.removeTypeTool(selected,TOOL_ID)){setStatus('The selected Clip does not have Side Runner.','bad');return;}remove.disabled=true;load(null);setStatus('Side Runner removed.','good');});
  const timer=setInterval(()=>refresh(false),250);get('.sr-close').addEventListener('click',()=>{clearInterval(timer);panel.remove();selectTool(false);});refresh(true);
})();
