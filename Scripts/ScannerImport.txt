/* Luke Animate Scanner Import v1.2 — Windows WIA bridge */
(function(){
  "use strict";
  const PANEL_ID="lukeanimate-scanner-panel",STYLE_ID="lukeanimate-scanner-style",BRIDGE="http://127.0.0.1:8765";
  const GROUP_ID=String(group&&group.id?group.id:"import"),ITEM_ID=String(item&&(item.id||item.label)?(item.id||item.label):"Scanner_Import");
  const ICON=String(item&&item.iconUrl?item.iconUrl:"https://lukeo25.github.io/LukeAnimate/SVGs/Scanner.svg");
  document.getElementById(PANEL_ID)?.remove();
  if(!document.getElementById(STYLE_ID)){
    const style=document.createElement("style");style.id=STYLE_ID;style.textContent=`
      #${PANEL_ID}{position:fixed;z-index:10000;left:32px;top:28px;width:min(430px,calc(100vw - 24px));height:min(690px,calc(100vh - 24px));display:flex;flex-direction:column;overflow:hidden;background:#eef1f4;border:1px solid #b9c2cc;border-radius:14px;box-shadow:0 22px 70px rgba(0,0,0,.3);color:#273442;font:13px Inter,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}
      #${PANEL_ID} *{box-sizing:border-box} #${PANEL_ID} button,#${PANEL_ID} input,#${PANEL_ID} select{font:inherit}
      #${PANEL_ID} .si-head{height:55px;flex:0 0 55px;display:flex;align-items:center;gap:10px;padding:8px 11px;background:#fff;border-bottom:1px solid #cfd5dc;cursor:move;user-select:none}
      #${PANEL_ID} .si-head img{width:32px;height:32px;padding:4px;border-radius:8px;background:#66717c} #${PANEL_ID} .si-title{flex:1;min-width:0} #${PANEL_ID} .si-title strong{display:block;font-size:16px;color:#28414a} #${PANEL_ID} .si-title span{display:block;font-size:9px;letter-spacing:.13em;text-transform:uppercase;color:#7c8793}
      #${PANEL_ID} .si-close{width:34px;height:34px;border:1px solid #bbc4ce;border-radius:9px;background:#fff;color:#536170;font-size:19px;cursor:pointer}
      #${PANEL_ID} .si-body{flex:1;min-height:0;overflow:auto;padding:11px} #${PANEL_ID} .si-card{margin-bottom:10px;padding:11px;border:1px solid #cbd3db;border-radius:11px;background:#fff} #${PANEL_ID} .si-card h4{margin:0 0 11px;color:#65727e;font:800 10px Consolas,monospace;letter-spacing:.1em;text-transform:uppercase}
      #${PANEL_ID} .si-row{display:grid;grid-template-columns:105px minmax(0,1fr);align-items:center;gap:9px;margin-bottom:9px;color:#64717d;font-size:12px} #${PANEL_ID} .si-row:last-child{margin-bottom:0} #${PANEL_ID} .si-row input,#${PANEL_ID} .si-row select{width:100%;height:35px;padding:0 9px;border:1px solid #c3ccd5;border-radius:8px;background:#f7f9fa;color:#293843}
      #${PANEL_ID} .si-check{display:flex;align-items:center;gap:7px;margin:9px 0;color:#5d6b78} #${PANEL_ID} .si-check input{accent-color:#0d9c86}
      #${PANEL_ID} .si-actions{display:grid;grid-template-columns:1fr 1fr;gap:8px} #${PANEL_ID} .si-btn{height:37px;border:1px solid #bdc6d0;border-radius:8px;background:#fff;color:#536170;cursor:pointer} #${PANEL_ID} .si-btn.primary{border-color:#0d9c86;background:#0d9c86;color:#fff;font-weight:750} #${PANEL_ID} .si-btn:disabled{opacity:.45;cursor:not-allowed}
      #${PANEL_ID} .si-preview{display:flex;align-items:center;justify-content:center;min-height:150px;max-height:230px;overflow:hidden;border:1px dashed #bcc7cf;border-radius:9px;background:#f7f9fa} #${PANEL_ID} .si-preview img{display:block;max-width:100%;max-height:228px;object-fit:contain} #${PANEL_ID} .si-empty{padding:24px;color:#87939d;text-align:center;font-size:11px}
      #${PANEL_ID} .si-help{margin:0;color:#71808e;font-size:10.5px;line-height:1.45} #${PANEL_ID} .si-help a{color:#0b8c79;font-weight:700} #${PANEL_ID} .si-status{flex:0 0 auto;margin:0 10px 10px;padding:8px 9px;min-height:31px;border:1px solid #cbd3db;border-radius:8px;background:#fff;color:#64717d;font-size:10.5px} #${PANEL_ID} .si-status.good{border-color:#0d9c86;color:#0b806e} #${PANEL_ID} .si-status.bad{border-color:#c56d6d;color:#a34141}`;document.head.appendChild(style);
  }
  const panel=document.createElement("section");panel.id=PANEL_ID;panel.innerHTML=`<div class="si-head" data-floating-panel-drag><img src="${ICON}" alt=""><div class="si-title"><strong>Scanner Import</strong><span>Windows WIA · image capture</span></div><button class="si-close" type="button" title="Close">×</button></div><div class="si-body"><div class="si-card"><h4>Scanner</h4><div class="si-row"><span>Device</span><select data-device><option value="">Start the bridge first</option></select></div><div class="si-row"><span>Resolution</span><select data-dpi><option>150</option><option selected>300</option><option>600</option><option>1200</option></select></div><div class="si-row"><span>Colour</span><select data-colour><option value="color">Colour</option><option value="grayscale">Greyscale</option><option value="text">Black &amp; white</option></select></div><div class="si-row"><span>Orientation</span><select data-rotation><option value="-90" selected>Rotate 90° left</option><option value="90">Rotate 90° right</option><option value="0">No rotation</option><option value="180">Rotate 180°</option></select></div><div class="si-row"><span>Placement</span><select data-fit><option value="contain">Fit inside stage</option><option value="native">Original pixel size</option><option value="cover">Fill stage</option></select></div><label class="si-check"><input data-next-frame type="checkbox"> Put each new scan on the next frame</label><div class="si-actions"><button class="si-btn" data-refresh type="button">Refresh scanners</button><button class="si-btn primary" data-scan type="button" disabled>Scan page</button></div></div><div class="si-card"><h4>Preview</h4><div class="si-preview" data-preview><div class="si-empty">Your scanned page will appear here.</div></div></div><div class="si-card"><h4>Windows helper</h4><p class="si-help">Luke Animate runs in a browser, so Windows provides scanner access through a small local helper. <a href="https://github.com/lukeo25/LukeAnimate/raw/main/Downloads/ScannerBridge.exe" target="_blank" rel="noopener">Download ScannerBridge.exe</a>, open it once, then click Refresh scanners. It stays in the Windows notification area and does not use PowerShell.</p></div></div><div class="si-status" data-status>Looking for the local scanner bridge…</div>`;
  document.body.appendChild(panel);LukeAnimate.registerFloatingPanel(panel);LukeAnimate.bringFloatingPanelToFront(panel);LukeAnimate.clampFloatingPanelToViewport(panel);LukeAnimate.setConfiguredToolActive(GROUP_ID,ITEM_ID,true);
  const q=s=>panel.querySelector(s),device=q("[data-device]"),scan=q("[data-scan]"),status=q("[data-status]");let lastDataUrl="",scanCount=0;
  const setStatus=(message,kind="")=>{status.textContent=message;status.className="si-status"+(kind?" "+kind:"");};
  async function bridge(path,options){const response=await fetch(BRIDGE+path,options);const data=await response.json();if(!response.ok||data.error)throw new Error(data.error||("Scanner bridge returned "+response.status));return data;}
  async function refresh(){scan.disabled=true;setStatus("Looking for scanners…");try{const data=await bridge("/scanners");device.innerHTML="";(data.scanners||[]).forEach(entry=>{const option=document.createElement("option");option.value=entry.id;option.textContent=entry.name;device.appendChild(option);});scan.disabled=!device.value;setStatus(device.value?(data.scanners.length+" scanner"+(data.scanners.length===1?"":"s")+" found."):"No WIA scanners were found.",device.value?"good":"bad");}catch(error){device.innerHTML='<option value="">Bridge not running</option>';setStatus("Open ScannerBridge.exe, then click Refresh scanners.","bad");}}
  async function orientScan(dataUrl,degrees){const rotation=((Number(degrees)||0)%360+360)%360;if(!rotation)return dataUrl;const image=await new Promise((resolve,reject)=>{const element=new Image();element.onload=()=>resolve(element);element.onerror=()=>reject(new Error("The scanned image could not be rotated."));element.src=dataUrl;});const quarterTurn=rotation===90||rotation===270,canvas=document.createElement("canvas");canvas.width=quarterTurn?image.naturalHeight:image.naturalWidth;canvas.height=quarterTurn?image.naturalWidth:image.naturalHeight;const context=canvas.getContext("2d");context.translate(canvas.width/2,canvas.height/2);context.rotate(rotation*Math.PI/180);context.drawImage(image,-image.naturalWidth/2,-image.naturalHeight/2);return canvas.toDataURL("image/png");}
  async function importScan(dataUrl,name){const image=await new Promise((resolve,reject)=>{const element=new Image();element.onload=()=>resolve(element);element.onerror=()=>reject(new Error("The scanned image could not be decoded."));element.src=dataUrl;});if(q("[data-next-frame]").checked&&scanCount>0){const next=LukeAnimate.getCurrentFrame()+1;LukeAnimate.setFrame(next);LukeAnimate.insertBlankFrame(next);}const fit=q("[data-fit]").value,nativeWidth=image.naturalWidth||image.width,nativeHeight=image.naturalHeight||image.height;let width=nativeWidth,height=nativeHeight;if(fit!=="native"){const scale=fit==="cover"?Math.max(LukeAnimate.width/nativeWidth,LukeAnimate.height/nativeHeight):Math.min(LukeAnimate.width/nativeWidth,LukeAnimate.height/nativeHeight);width=nativeWidth*scale;height=nativeHeight*scale;}const object=LukeAnimate.createImageObject({src:dataUrl,name},{name,src:dataUrl,naturalWidth:nativeWidth,naturalHeight:nativeHeight,width,height,x:LukeAnimate.width/2,y:LukeAnimate.height/2,mediaRole:"overlay"});LukeAnimate.addDrawObject(object,"animate");scanCount++;return object;}
  scan.onclick=async()=>{scan.disabled=true;setStatus("Waiting for the scanner…");try{const query=new URLSearchParams({id:device.value,dpi:q("[data-dpi]").value,colour:q("[data-colour]").value});const data=await bridge("/scan?"+query);setStatus("Correcting scan orientation…");lastDataUrl=await orientScan(data.dataUrl,q("[data-rotation]").value);q("[data-preview]").innerHTML=`<img src="${lastDataUrl}" alt="Scanned page">`;await importScan(lastDataUrl,data.name||("Scan "+(scanCount+1)+".png"));setStatus("Scan imported into Luke Animate.","good");}catch(error){setStatus(error.message||String(error),"bad");}finally{scan.disabled=!device.value;}};
  q("[data-refresh]").onclick=refresh;q(".si-close").onclick=()=>{panel.remove();LukeAnimate.setConfiguredToolActive(GROUP_ID,ITEM_ID,false);};refresh();
})();
