/* Luke Animate Drag & Drop Behaviour v1.1 */
(function(){
  "use strict";
  const PANEL_ID="lukeanimate-dragdrop-panel",STYLE_ID="lukeanimate-dragdrop-style",TOOL_ID="dragdrop";
  const GROUP_ID=String(group&&group.id?group.id:"behaviours"),ITEM_ID=String(item&&(item.id||item.label)?(item.id||item.label):"Drag_Drop");
  const ICON=String(item&&item.iconUrl?item.iconUrl:"https://lukeo25.github.io/LukeAnimate/SVGs/Drag.svg");
  const DEFAULT_CODE=`const { result, target } = context;
// result is "correct" or "wrong". Add optional game logic here.
return { result, target };`;
  if(!LukeAnimate||Number(LukeAnimate.version||0)<129)throw new Error("Drag & Drop requires Luke Animate v255 or later.");
  const selectTool=active=>{if(LukeAnimate.setConfiguredToolActive)LukeAnimate.setConfiguredToolActive(GROUP_ID,ITEM_ID,!!active);};
  const old=document.getElementById(PANEL_ID);
  if(old){old.style.display="flex";old.classList.add("open");selectTool(true);LukeAnimate.bringFloatingPanelToFront(old);LukeAnimate.clampFloatingPanelToViewport(old);return;}
  if(!document.getElementById(STYLE_ID)){
    const style=document.createElement("style");style.id=STYLE_ID;style.textContent=`
      #${PANEL_ID}{position:fixed;z-index:10000;left:30px;top:18px;width:410px;max-width:calc(100vw - 16px);height:min(760px,calc(100vh - 36px));display:flex;flex-direction:column;background:#f1f3f6;border:1px solid #bfc6cf;border-radius:14px;box-shadow:0 20px 60px rgba(0,0,0,.3);color:#27313d;font:13px Inter,-apple-system,BlinkMacSystemFont,Segoe UI,sans-serif;overflow:hidden}
      #${PANEL_ID} *{box-sizing:border-box} #${PANEL_ID} .dd-head{display:flex;align-items:center;gap:9px;padding:8px 10px;background:#fff;border-bottom:1px solid #d3d8df;cursor:move;user-select:none}
      #${PANEL_ID} .dd-head img{width:27px;height:27px;object-fit:contain;filter:grayscale(1) brightness(.72)} #${PANEL_ID} .dd-title{flex:1} #${PANEL_ID} .dd-title strong{display:block;font-size:14px;color:#29434a} #${PANEL_ID} .dd-title span{font-size:9px;letter-spacing:.11em;text-transform:uppercase;color:#7b8491}
      #${PANEL_ID} .dd-close{width:32px;height:32px;border:1px solid #bbc3cd;border-radius:8px;background:#fff;font-size:20px;color:#566270;cursor:pointer} #${PANEL_ID} .dd-body{flex:1;min-height:0;padding:8px 7px 48px;overflow:auto}
      #${PANEL_ID} .dd-selected,#${PANEL_ID} .dd-card{padding:9px;margin-bottom:7px;border:1px solid #d2d7de;border-radius:10px;background:#fff} #${PANEL_ID} .dd-selected{white-space:nowrap;overflow:hidden;text-overflow:ellipsis} #${PANEL_ID} .dd-selected strong{color:#0e8f7c}
      #${PANEL_ID} h3{margin:0 0 8px;font-size:12px;color:#3e4c5a} #${PANEL_ID} .dd-grid{display:grid;grid-template-columns:1fr 1fr;gap:7px} #${PANEL_ID} .dd-wide{grid-column:1/-1}
      #${PANEL_ID} label.dd-field{display:flex;flex-direction:column;gap:4px;font-size:11px;font-weight:700;color:#596674} #${PANEL_ID} input[type=text],#${PANEL_ID} input[type=number],#${PANEL_ID} select{width:100%;padding:6px;border:1px solid #b9c3ce;border-radius:6px;background:#fff;color:#263442}
      #${PANEL_ID} .dd-check{display:flex;align-items:center;gap:7px;padding:7px;border:1px solid #d5dbe2;border-radius:7px;background:#f8fafb;font-size:11px} #${PANEL_ID} input[type=checkbox]{accent-color:#0e9c86}
      #${PANEL_ID} .dd-extra{margin-top:9px;padding-top:9px;border-top:1px solid #e0e4e9} #${PANEL_ID} .dd-extra-title{grid-column:1/-1;font-size:10px;font-weight:800;letter-spacing:.08em;text-transform:uppercase;color:#71808c}
      #${PANEL_ID} .dd-effects{display:flex;flex-direction:column;gap:5px;margin-top:7px} #${PANEL_ID} .dd-effect{display:grid;grid-template-columns:1fr 76px 28px;gap:5px} #${PANEL_ID} .dd-effect button{border:1px solid #c5ccd4;border-radius:6px;background:#f5f6f8;color:#8a3d3d;cursor:pointer}
      #${PANEL_ID} .dd-add{margin-top:6px;padding:6px 9px;border:1px solid #b9c3ce;border-radius:7px;background:#f2f5f6;color:#485563;cursor:pointer} #${PANEL_ID} .dd-muted{color:#74808d;font-size:11px;line-height:1.35}
      #${PANEL_ID} details{margin-top:7px;border:1px solid #d2d7de;border-radius:10px;background:#fff;overflow:hidden} #${PANEL_ID} summary{padding:8px 9px;cursor:pointer;font-size:11px;letter-spacing:.08em;text-transform:uppercase;font-weight:700;color:#66717e}
      #${PANEL_ID} .dd-code{padding:9px} #${PANEL_ID} textarea{width:100%;min-height:110px;resize:vertical;padding:8px;border:1px solid #aeb8c4;border-radius:6px;background:#17212b;color:#e7f5f2;font:12px/1.45 Consolas,monospace}
      #${PANEL_ID} .dd-actions{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:10px} #${PANEL_ID} .dd-btn{padding:8px;border:1px solid #b8c0ca;border-radius:8px;background:#eceff2;color:#4b5766;cursor:pointer} #${PANEL_ID} .dd-btn.primary{border-color:#0e9c86;background:#0e9c86;color:#fff;font-weight:700} #${PANEL_ID} .dd-btn:disabled{opacity:.45;cursor:not-allowed}
      #${PANEL_ID} .dd-status{position:absolute;left:7px;right:7px;bottom:7px;min-height:28px;max-height:42px;overflow:auto;border:1px solid #cbd2da;border-radius:8px;background:rgba(255,255,255,.97);padding:8px 10px;font-size:10px;color:#596573} #${PANEL_ID} .dd-status.good{border-color:#0e9c86;color:#117d6d} #${PANEL_ID} .dd-status.bad{border-color:#c66a6a;color:#a33d3d}
      @media(max-width:560px){#${PANEL_ID}{left:8px;top:8px;width:calc(100vw - 16px);height:calc(100vh - 16px)}}`;
    document.head.appendChild(style);
  }
  const panel=document.createElement("section");panel.id=PANEL_ID;panel.className="open";
  panel.innerHTML=`<div class="dd-head" data-floating-panel-drag><img src="${ICON.replace(/"/g,"&quot;")}" alt=""><div class="dd-title"><strong>Drag & Drop</strong><span>Clip behaviour</span></div><button class="dd-close" type="button" title="Close">×</button></div><div class="dd-body">
    <div class="dd-selected"><strong>Selected:</strong> <span data-dd-selected>No Clip selected</span></div>
    <div class="dd-card dd-grid"><label class="dd-field dd-wide"><span>Correct drop target</span><select data-dd-target></select></label><label class="dd-check dd-wide"><input data-dd-grab type="checkbox" checked> Keep the exact point where the Clip was grabbed</label></div>
    <div class="dd-card"><h3>Correct drop</h3><div class="dd-grid"><label class="dd-field"><span>Score variable</span><input data-dd-correct-var type="text" value="LT_score"></label><label class="dd-field"><span>Add to score</span><input data-dd-correct-score type="number" value="10" step="1"></label><label class="dd-field"><span>Go to frame (optional)</span><input data-dd-correct-frame type="text" placeholder="Number or frame name"></label><label class="dd-field"><span>On Clip</span><select data-dd-correct-goto><option value="self">Dragged Clip</option><option value="drop">Drop target</option></select></label></div><div class="dd-effects" data-dd-correct-effects></div><button class="dd-add" data-dd-add="correct" type="button">+ Add effect</button><div class="dd-extra dd-grid"><div class="dd-extra-title">Also do at the same time</div><label class="dd-field"><span>Send another Clip</span><select data-dd-correct-action-clip></select></label><label class="dd-field"><span>To frame</span><input data-dd-correct-action-frame type="text" placeholder="Number or frame name"></label><label class="dd-field"><span>Set variable</span><input data-dd-correct-action-var type="text" placeholder="Variable name"></label><label class="dd-field"><span>To value</span><input data-dd-correct-action-value type="text" placeholder="Number, text, true or false"></label></div></div>
    <div class="dd-card"><h3>Wrong drop</h3><div class="dd-grid"><label class="dd-field"><span>Score variable</span><input data-dd-wrong-var type="text" value="LT_score"></label><label class="dd-field"><span>Subtract from score</span><input data-dd-wrong-score type="number" value="5" min="0" step="1"></label><label class="dd-field"><span>Go to frame (optional)</span><input data-dd-wrong-frame type="text" placeholder="Number or frame name"></label><label class="dd-field"><span>On Clip</span><select data-dd-wrong-goto><option value="self">Dragged Clip</option><option value="drop">Drop target</option></select></label></div><div class="dd-effects" data-dd-wrong-effects></div><button class="dd-add" data-dd-add="wrong" type="button">+ Add effect</button><div class="dd-extra dd-grid"><div class="dd-extra-title">Also do at the same time</div><label class="dd-field"><span>Send another Clip</span><select data-dd-wrong-action-clip></select></label><label class="dd-field"><span>To frame</span><input data-dd-wrong-action-frame type="text" placeholder="Number or frame name"></label><label class="dd-field"><span>Set variable</span><input data-dd-wrong-action-var type="text" placeholder="Variable name"></label><label class="dd-field"><span>To value</span><input data-dd-wrong-action-value type="text" placeholder="Number, text, true or false"></label></div></div>
    <p class="dd-muted">Effects run from top to bottom. Dragging works during Play and the score variable is available to Life, Old Timer and Node Coder.</p>
    <details><summary>View / edit code</summary><div class="dd-code"><p class="dd-muted">This optional hook runs after a drop. <code>result</code> is “correct” or “wrong”.</p><textarea data-dd-code spellcheck="false"></textarea><button class="dd-add" data-dd-reset type="button">Reset code</button></div></details>
    <div class="dd-actions"><button class="dd-btn" data-dd-remove type="button">Remove</button><button class="dd-btn primary" data-dd-apply type="button">Apply</button></div><div class="dd-status" data-dd-status></div></div>`;
  document.body.appendChild(panel);LukeAnimate.registerFloatingPanel(panel);LukeAnimate.bringFloatingPanelToFront(panel);LukeAnimate.clampFloatingPanelToViewport(panel);selectTool(true);
  const get=q=>panel.querySelector(q),status=get("[data-dd-status]"),apply=get("[data-dd-apply]"),remove=get("[data-dd-remove]"),effectOptions=["fade","shrink","shake","return","disappear"];let selected=null,key="",loadedTargets={};
  const setStatus=(message,kind)=>{status.textContent=String(message||"");status.className="dd-status"+(kind?" "+kind:"");};
  function selectedClip(){const list=LukeAnimate.getSelectedDrawObjects()||[];return list.length===1&&list[0]&&list[0].type==="clipinstance"?list[0]:null;}
  function clipName(object){const clips=LukeAnimate.getClips?LukeAnimate.getClips():{};return object?String(object.name||(clips[object.clipId]&&clips[object.clipId].name)||"Clip"):"No Clip selected";}
  function availableTargets(){return (LukeAnimate.getDrawObjects?LukeAnimate.getDrawObjects():[]).filter(object=>object&&object.type==="clipinstance"&&(!selected||object!==selected));}
  function fillClipSelect(selector,preferred,placeholder){const select=get(selector),targets=availableTargets(),wanted=String(preferred||select.value||"");select.innerHTML='<option value="">'+placeholder+'</option>';targets.forEach(object=>{const option=document.createElement("option");option.value=String(object.id||"");option.textContent=clipName(object);select.appendChild(option);});if(wanted&&!targets.some(object=>String(object.id||"")===wanted)){const option=document.createElement("option");option.value=wanted;option.textContent="Saved Clip (not on this frame)";select.appendChild(option);}select.value=wanted;}
  function refreshTargets(preferred){const explicit=preferred&&typeof preferred==="object",values=explicit?preferred:{};fillClipSelect("[data-dd-target]",explicit?values.targetId:"","Choose a Clip…");fillClipSelect("[data-dd-correct-action-clip]",explicit?values.correctId:"","No additional Clip");fillClipSelect("[data-dd-wrong-action-clip]",explicit?values.wrongId:"","No additional Clip");}
  function renderEffects(kind,effects){const host=get(`[data-dd-${kind}-effects]`);host.innerHTML="";(effects||[]).forEach(entry=>{const row=document.createElement("div");row.className="dd-effect";row.innerHTML=`<select>${effectOptions.map(name=>`<option value="${name}">${name[0].toUpperCase()+name.slice(1)}</option>`).join("")}</select><input type="number" min=".05" max="10" step=".05" value="${Number(entry.duration)||.5}"><button type="button" title="Remove">×</button>`;row.querySelector("select").value=effectOptions.includes(entry.effect)?entry.effect:"fade";row.querySelector("button").onclick=()=>row.remove();host.appendChild(row);});}
  function readEffects(kind){return Array.from(get(`[data-dd-${kind}-effects]`).children).map(row=>({effect:row.querySelector("select").value,duration:Number(row.querySelector("input").value)||.5}));}
  function load(settings){const value=settings||{},ca=value.correctAction||{},wa=value.wrongAction||{};loadedTargets={targetId:String(value.targetId||""),correctId:String(ca.clipId||""),wrongId:String(wa.clipId||"")};refreshTargets(loadedTargets);get("[data-dd-grab]").checked=value.keepGrabOffset!==false;get("[data-dd-correct-var]").value=String(value.scoreVar||"LT_score");get("[data-dd-correct-score]").value=String(value.correctScore===undefined?10:value.correctScore);get("[data-dd-wrong-var]").value=String(value.wrongScoreVar||value.scoreVar||"LT_score");get("[data-dd-wrong-score]").value=String(value.wrongScore===undefined?5:value.wrongScore);const cg=value.correctGoto||{},wg=value.wrongGoto||{};get("[data-dd-correct-frame]").value=String(cg.frame||"");get("[data-dd-correct-goto]").value=cg.target==="drop"?"drop":"self";get("[data-dd-wrong-frame]").value=String(wg.frame||"");get("[data-dd-wrong-goto]").value=wg.target==="drop"?"drop":"self";["correct","wrong"].forEach(kind=>{const action=kind==="correct"?ca:wa;get(`[data-dd-${kind}-action-frame]`).value=String(action.frame||"");get(`[data-dd-${kind}-action-var]`).value=String(action.variable||"");get(`[data-dd-${kind}-action-value]`).value=String(action.value===undefined?"":action.value);});renderEffects("correct",value.correctEffects||[{effect:"shrink",duration:.5},{effect:"fade",duration:.5}]);renderEffects("wrong",value.wrongEffects||[{effect:"shake",duration:.5},{effect:"return",duration:.5}]);get("[data-dd-code]").value=String(value.code||DEFAULT_CODE);}
  function settings(){const targets=availableTargets(),targetId=get("[data-dd-target]").value,target=targets.find(object=>String(object.id||"")===targetId),action=kind=>{const clipId=get(`[data-dd-${kind}-action-clip]`).value,clip=targets.find(object=>String(object.id||"")===clipId);return {clipId,clipName:clip?clipName(clip):"",frame:get(`[data-dd-${kind}-action-frame]`).value,variable:get(`[data-dd-${kind}-action-var]`).value,value:get(`[data-dd-${kind}-action-value]`).value};};return {enabled:true,targetId,targetName:target?clipName(target):"",keepGrabOffset:get("[data-dd-grab]").checked,scoreVar:get("[data-dd-correct-var]").value,correctScore:Number(get("[data-dd-correct-score]").value),wrongScoreVar:get("[data-dd-wrong-var]").value,wrongScore:Math.abs(Number(get("[data-dd-wrong-score]").value)||0),correctEffects:readEffects("correct"),wrongEffects:readEffects("wrong"),correctGoto:{target:get("[data-dd-correct-goto]").value,frame:get("[data-dd-correct-frame]").value},wrongGoto:{target:get("[data-dd-wrong-goto]").value,frame:get("[data-dd-wrong-frame]").value},correctAction:action("correct"),wrongAction:action("wrong"),code:get("[data-dd-code]").value};}
  function refresh(force){const next=selectedClip(),nextKey=next?String(next.id||next.clipId):"";if(!force&&nextKey===key){refreshTargets();return;}selected=next;key=nextKey;get("[data-dd-selected]").textContent=clipName(next);apply.disabled=!next;const value=next?LukeAnimate.getTypeTool(next,TOOL_ID):null;remove.disabled=!value;load(value);setStatus(next?(value?"Drag & Drop is attached to this Clip.":"Choose its target and actions, then click Apply."):"Select one Clip instance on the stage.",next?"good":"");}
  panel.querySelectorAll("[data-dd-add]").forEach(button=>button.onclick=()=>{const kind=button.dataset.ddAdd;const current=readEffects(kind);current.push({effect:"fade",duration:.5});renderEffects(kind,current);});
  get("[data-dd-reset]").onclick=()=>{get("[data-dd-code]").value=DEFAULT_CODE;setStatus("Code reset. Click Apply to save it.","good");};
  apply.onclick=()=>{refresh(false);if(!selected){setStatus("Select one Clip first.","bad");return;}if(!get("[data-dd-target]").value){setStatus("Choose the correct drop target Clip.","bad");return;}LukeAnimate.setTypeTool(selected,TOOL_ID,settings());remove.disabled=false;setStatus("Drag & Drop applied. Press Play, then drag the Clip to test it.","good");};
  remove.onclick=()=>{refresh(false);if(!selected||!LukeAnimate.removeTypeTool(selected,TOOL_ID)){setStatus("The selected Clip does not have Drag & Drop.","bad");return;}remove.disabled=true;load(null);setStatus("Drag & Drop removed.","good");};
  const timer=setInterval(()=>refresh(false),250);get(".dd-close").onclick=()=>{clearInterval(timer);panel.remove();selectTool(false);};refresh(true);
})();
