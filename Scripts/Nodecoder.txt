/* Luke Animate Node Coder v1.0 */
(function(){
  "use strict";
  const PANEL_ID="lukeanimate-nodecoder-panel",STYLE_ID="lukeanimate-nodecoder-style";
  const GROUP_ID=String(group&&group.id?group.id:"tools"),ITEM_ID=String(item&&(item.id||item.label)?(item.id||item.label):"Node_Coder");
  const ICON=String(item&&item.iconUrl?item.iconUrl:"https://lukeo25.github.io/LukeAnimate/SVGs/Node.svg");
  if(!LukeAnimate||Number(LukeAnimate.version||0)<125||typeof LukeAnimate.getNodeCodeSlots!=="function")throw new Error("Node Coder requires Luke Animate v250 or later.");
  const setActive=active=>{if(LukeAnimate.setConfiguredToolActive)LukeAnimate.setConfiguredToolActive(GROUP_ID,ITEM_ID,!!active);};
  const previous=document.getElementById(PANEL_ID);
  if(previous){previous.style.display="flex";previous.classList.add("open");setActive(true);LukeAnimate.bringFloatingPanelToFront(previous);return;}
  if(!document.getElementById(STYLE_ID)){
    const style=document.createElement("style");style.id=STYLE_ID;style.textContent=`
      #${PANEL_ID}{position:fixed;z-index:10000;left:42px;top:34px;width:min(1120px,calc(100vw - 24px));height:min(720px,calc(100vh - 24px));display:flex;flex-direction:column;background:#eef1f4;border:1px solid #b9c2cc;border-radius:14px;box-shadow:0 22px 70px rgba(0,0,0,.32);color:#273442;font:13px Inter,-apple-system,BlinkMacSystemFont,Segoe UI,sans-serif;overflow:hidden}
      #${PANEL_ID} *{box-sizing:border-box} #${PANEL_ID} button,#${PANEL_ID} input,#${PANEL_ID} select,#${PANEL_ID} textarea{font:inherit}
      #${PANEL_ID} .nc-head{height:52px;flex:0 0 52px;display:flex;align-items:center;gap:10px;padding:8px 11px;background:#fff;border-bottom:1px solid #cfd5dc;cursor:move;user-select:none}
      #${PANEL_ID} .nc-head img{width:31px;height:31px;object-fit:contain} #${PANEL_ID} .nc-title{flex:1} #${PANEL_ID} .nc-title strong{display:block;font-size:16px;color:#28414a} #${PANEL_ID} .nc-title span{display:block;font-size:9px;letter-spacing:.13em;text-transform:uppercase;color:#7c8793}
      #${PANEL_ID} .nc-close{width:34px;height:34px;border:1px solid #bbc4ce;border-radius:9px;background:#fff;color:#536170;font-size:21px;cursor:pointer}
      #${PANEL_ID} .nc-main{flex:1;min-height:0;display:grid;grid-template-columns:220px minmax(360px,1fr) 365px}
      #${PANEL_ID} .nc-library{min-width:0;border-right:1px solid #cbd2da;background:#f7f9fb;display:flex;flex-direction:column;overflow:hidden}
      #${PANEL_ID} .nc-pane-title{padding:10px 12px;border-bottom:1px solid #d5dbe2;background:#fff;font-size:11px;font-weight:800;letter-spacing:.09em;text-transform:uppercase;color:#667481}
      #${PANEL_ID} .nc-selected{margin:10px;padding:10px;border:1px solid #cdd5dd;border-radius:10px;background:#fff;line-height:1.4;overflow:hidden;text-overflow:ellipsis}
      #${PANEL_ID} .nc-selected strong{color:#0d927e} #${PANEL_ID} .nc-add{margin:0 10px 8px;padding:9px;border:1px solid #0d9c86;border-radius:9px;background:#0d9c86;color:#fff;font-weight:800;cursor:pointer}
      #${PANEL_ID} .nc-slots{flex:1;overflow:auto;padding:0 10px 12px} #${PANEL_ID} .nc-slot-row{width:100%;margin:0 0 7px;padding:8px 9px;border:1px solid #ccd4dc;border-radius:9px;background:#fff;text-align:left;cursor:pointer;color:#374553}
      #${PANEL_ID} .nc-slot-row.active{border-color:#0d9c86;box-shadow:0 0 0 2px rgba(13,156,134,.14)} #${PANEL_ID} .nc-slot-row small{display:block;margin-top:3px;color:#85909b;text-transform:uppercase;letter-spacing:.08em;font-size:9px}
      #${PANEL_ID} .nc-managed{display:inline-block;margin-left:5px;padding:1px 5px;border-radius:99px;background:#e8ddff;color:#6f4ba8;font-size:8px;vertical-align:1px}
      #${PANEL_ID} .nc-graph-wrap{position:relative;min-width:0;overflow:hidden;background-color:#202831;background-image:radial-gradient(circle at 1px 1px,rgba(255,255,255,.17) 1px,transparent 0);background-size:24px 24px}
      #${PANEL_ID} .nc-graph{position:absolute;inset:0;overflow:auto} #${PANEL_ID} .nc-links{position:absolute;left:0;top:0;width:1400px;height:1000px;pointer-events:none;overflow:visible}
      #${PANEL_ID} .nc-node{position:absolute;width:190px;border:1px solid #4a5865;border-radius:12px;background:#10171e;color:#dce5eb;box-shadow:0 10px 26px rgba(0,0,0,.34);overflow:hidden;cursor:grab;user-select:none}
      #${PANEL_ID} .nc-node.active{border-color:#53d5bd;box-shadow:0 0 0 2px rgba(83,213,189,.22),0 10px 26px rgba(0,0,0,.34)} #${PANEL_ID} .nc-node-head{padding:8px 10px;background:#18232d;border-bottom:1px solid #34414d;font-weight:800;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
      #${PANEL_ID} .nc-clip-node{left:34px;top:88px;width:220px;cursor:default} #${PANEL_ID} .nc-port-row{height:35px;display:flex;align-items:center;gap:8px;padding:0 11px;color:#bac7d0;border-top:1px solid rgba(255,255,255,.04)}
      #${PANEL_ID} .nc-port{width:12px;height:12px;border-radius:50%;background:#8d9aa6;box-shadow:0 0 0 3px rgba(255,255,255,.06)} #${PANEL_ID} .nc-port.load{background:#a77deb} #${PANEL_ID} .nc-port.update{background:#e66868} #${PANEL_ID} .nc-port.unload{background:#4ed1ae}
      #${PANEL_ID} .nc-code-body{padding:9px 10px;color:#93a3af;font-size:11px;line-height:1.35} #${PANEL_ID} .nc-event-pill{display:inline-block;margin-top:7px;padding:2px 7px;border-radius:99px;background:#273746;color:#9fd9cf;text-transform:uppercase;font-size:9px;letter-spacing:.08em}
      #${PANEL_ID} .nc-editor{min-width:0;border-left:1px solid #cbd2da;background:#f7f9fb;display:flex;flex-direction:column;overflow:hidden} #${PANEL_ID} .nc-editor-body{flex:1;min-height:0;overflow:auto;padding:10px}
      #${PANEL_ID} .nc-field{display:flex;flex-direction:column;gap:4px;margin-bottom:9px;color:#60707e;font-size:11px;font-weight:750} #${PANEL_ID} .nc-field input,#${PANEL_ID} .nc-field select{width:100%;padding:8px;border:1px solid #bfc9d3;border-radius:8px;background:#fff;color:#263542}
      #${PANEL_ID} .nc-check{display:flex;align-items:center;gap:7px;margin-bottom:9px} #${PANEL_ID} .nc-check input{accent-color:#0d9c86}
      #${PANEL_ID} .nc-code{width:100%;height:310px;resize:vertical;padding:10px;border:1px solid #222f3a;border-radius:9px;background:#111a22;color:#dbf5ef;font:12px/1.48 Consolas,monospace;tab-size:2}
      #${PANEL_ID} .nc-help{margin:7px 0;color:#71808e;font-size:10px;line-height:1.45} #${PANEL_ID} .nc-actions{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:9px} #${PANEL_ID} .nc-btn{padding:9px;border:1px solid #bdc6d0;border-radius:9px;background:#fff;color:#536170;cursor:pointer} #${PANEL_ID} .nc-btn.primary{border-color:#0d9c86;background:#0d9c86;color:#fff;font-weight:800} #${PANEL_ID} .nc-btn.danger{color:#a34646}
      #${PANEL_ID} .nc-status{flex:0 0 auto;margin:0 10px 10px;padding:8px 9px;border:1px solid #cbd3db;border-radius:8px;background:#fff;color:#64717d;font-size:10px;min-height:30px} #${PANEL_ID} .nc-status.good{border-color:#0d9c86;color:#0b806e} #${PANEL_ID} .nc-status.bad{border-color:#c56d6d;color:#a34141}
      #${PANEL_ID} .nc-empty{padding:20px;color:#7b8996;text-align:center;line-height:1.5}
      @media(max-width:850px){#${PANEL_ID} .nc-main{grid-template-columns:180px 1fr}#${PANEL_ID} .nc-editor{position:absolute;right:0;top:52px;bottom:0;width:min(365px,80vw);z-index:4;box-shadow:-12px 0 30px rgba(0,0,0,.25)}}`;
    document.head.appendChild(style);
  }
  const panel=document.createElement("section");panel.id=PANEL_ID;panel.innerHTML=`
    <div class="nc-head" data-floating-panel-drag><img src="${ICON.replace(/"/g,"&quot;")}" alt=""><div class="nc-title"><strong>Node Coder</strong><span>Visual code slots for Luke Animate Clips</span></div><button class="nc-close" type="button">×</button></div>
    <div class="nc-main"><aside class="nc-library"><div class="nc-pane-title">Code Slots</div><div class="nc-selected"><strong>Selected:</strong><br><span data-nc-selected>No Clip selected</span></div><button class="nc-add" data-nc-add type="button">+ Add Code Slot</button><div class="nc-slots" data-nc-slots></div></aside>
      <main class="nc-graph-wrap"><div class="nc-graph" data-nc-graph><svg class="nc-links" data-nc-links></svg><div class="nc-empty" data-nc-empty>Select one Clip instance on the stage.<br>Its code slots will appear as nodes here.</div></div></main>
      <aside class="nc-editor"><div class="nc-pane-title">Slot Editor</div><div class="nc-editor-body" data-nc-editor><div class="nc-empty">Select or add a code slot.</div></div><div class="nc-status" data-nc-status>Ready.</div></aside>
    </div>`;
  document.body.appendChild(panel);LukeAnimate.registerFloatingPanel(panel);LukeAnimate.bringFloatingPanelToFront(panel);LukeAnimate.clampFloatingPanelToViewport(panel);setActive(true);
  const q=selector=>panel.querySelector(selector),slotsEl=q('[data-nc-slots]'),graph=q('[data-nc-graph]'),links=q('[data-nc-links]'),editor=q('[data-nc-editor]'),status=q('[data-nc-status]'),empty=q('[data-nc-empty]');
  let selected=null,selectedSlotId='',selectionKey='',drag=null;
  const setStatus=(message,kind)=>{status.textContent=String(message||'');status.className='nc-status'+(kind?' '+kind:'');};
  function selectedClip(){const list=LukeAnimate.getSelectedDrawObjects?LukeAnimate.getSelectedDrawObjects():[];return list.length===1&&list[0]&&list[0].type==='clipinstance'?list[0]:null;}
  function clipName(object){const clips=LukeAnimate.getClips?LukeAnimate.getClips():{};return object?String(object.name||(clips[object.clipId]&&clips[object.clipId].name)||'Clip'):'No Clip selected';}
  function slots(){return selected?LukeAnimate.getNodeCodeSlots(selected):[];}
  function slotById(id){return slots().find(slot=>slot.id===id)||null;}
  function escapeHtml(value){return String(value||'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/"/g,'&quot;');}
  function eventY(eventName){return eventName==='load'?132:(eventName==='unload'?202:167);}
  function renderLibrary(list){slotsEl.innerHTML=list.length?list.map(slot=>`<button class="nc-slot-row${slot.id===selectedSlotId?' active':''}" data-slot-id="${escapeHtml(slot.id)}" type="button">${escapeHtml(slot.name)}${slot.source==='type-tool'?'<span class="nc-managed">managed</span>':''}<small>${escapeHtml(slot.event==='tool'?'Type Tool':slot.event)}</small></button>`).join(''):'<div class="nc-empty">No code slots yet.</div>';slotsEl.querySelectorAll('[data-slot-id]').forEach(button=>button.addEventListener('click',()=>{selectedSlotId=button.dataset.slotId;renderAll();}));}
  function renderGraph(list){graph.querySelectorAll('.nc-node').forEach(node=>node.remove());links.innerHTML='';empty.style.display=selected?'none':'block';if(!selected)return;
    const clip=document.createElement('div');clip.className='nc-node nc-clip-node';clip.innerHTML=`<div class="nc-node-head">${escapeHtml(clipName(selected))}</div><div class="nc-port-row"><i class="nc-port load"></i>Load</div><div class="nc-port-row"><i class="nc-port update"></i>Update</div><div class="nc-port-row"><i class="nc-port unload"></i>Unload</div>`;graph.appendChild(clip);
    list.forEach((slot,index)=>{const node=document.createElement('div');node.className='nc-node'+(slot.id===selectedSlotId?' active':'');node.dataset.slotId=slot.id;node.style.left=(Number(slot.x)||340)+'px';node.style.top=(Number(slot.y)||70+index*120)+'px';node.innerHTML=`<div class="nc-node-head">${escapeHtml(slot.name)}${slot.source==='type-tool'?' · managed':''}</div><div class="nc-code-body">${escapeHtml((slot.code||'').replace(/\s+/g,' ').slice(0,95)||'Empty code slot')}<br><span class="nc-event-pill">${escapeHtml(slot.event)}</span></div>`;node.addEventListener('click',event=>{if(drag)return;event.stopPropagation();selectedSlotId=slot.id;renderAll();});node.addEventListener('pointerdown',event=>{if(event.button!==0)return;drag={node,slot,startX:event.clientX,startY:event.clientY,left:parseFloat(node.style.left)||0,top:parseFloat(node.style.top)||0,moved:false};node.setPointerCapture(event.pointerId);});node.addEventListener('pointermove',event=>{if(!drag||drag.node!==node)return;const dx=event.clientX-drag.startX,dy=event.clientY-drag.startY;if(Math.abs(dx)+Math.abs(dy)>3)drag.moved=true;node.style.left=Math.max(280,drag.left+dx)+'px';node.style.top=Math.max(18,drag.top+dy)+'px';drawLinks(list);});node.addEventListener('pointerup',event=>{if(!drag||drag.node!==node)return;node.releasePointerCapture(event.pointerId);if(drag.moved){const moved=Object.assign({},drag.slot,{x:parseFloat(node.style.left)||drag.slot.x,y:parseFloat(node.style.top)||drag.slot.y});LukeAnimate.setNodeCodeSlot(selected,moved);setStatus('Node position saved.','good');}drag=null;});graph.appendChild(node);});drawLinks(list);}
  function drawLinks(list){links.innerHTML='';list.forEach(slot=>{if(slot.event==='tool')return;const node=graph.querySelector(`.nc-node[data-slot-id="${CSS.escape(slot.id)}"]`);if(!node)return;const x1=parseFloat(node.style.left)||340,y1=(parseFloat(node.style.top)||70)+42,x2=34+220,y2=eventY(slot.event);const path=document.createElementNS('http://www.w3.org/2000/svg','path');const bend=Math.max(55,Math.abs(x1-x2)*.45);path.setAttribute('d',`M ${x1} ${y1} C ${x1-bend} ${y1}, ${x2+bend} ${y2}, ${x2} ${y2}`);path.setAttribute('fill','none');path.setAttribute('stroke',slot.event==='load'?'#a77deb':slot.event==='unload'?'#4ed1ae':'#e66868');path.setAttribute('stroke-width','3');path.setAttribute('opacity','.82');links.appendChild(path);});}
  function renderEditor(slot){if(!slot){editor.innerHTML='<div class="nc-empty">Select or add a code slot.</div>';return;}const managed=slot.source==='type-tool',nativeManaged=managed&&/^\/\/ .* uses Luke Animate native runtime code\./.test(String(slot.code||'').trim());editor.innerHTML=`<label class="nc-field"><span>Name</span><input data-nc-name value="${escapeHtml(slot.name)}" ${managed?'readonly':''}></label><label class="nc-field"><span>Runs on</span><select data-nc-event ${managed?'disabled':''}><option value="load">Load once</option><option value="update">Every update</option><option value="unload">Unload / stop</option><option value="tool">Type Tool managed code</option></select></label><label class="nc-check"><input data-nc-enabled type="checkbox" ${slot.enabled?'checked':''}> Enabled</label><label class="nc-field"><span>JavaScript</span><textarea class="nc-code" data-nc-code spellcheck="false" ${nativeManaged?'readonly':''}>${escapeHtml(slot.code)}</textarea></label><p class="nc-help">${nativeManaged?'This tool uses Luke Animate’s native runtime. Configure its options in the original Type Tool panel; its dedicated slot remains visible here.':'Custom slots receive <b>context</b>: object, state, values, event, delta, time, api, readValue and writeValue. Return transform values such as <code>{ x, y, rotation, scaleX, scaleY, opacity }</code>. Managed slots mirror code created by Type Tools, so code updates always keep their own slot.'}</p><div class="nc-actions"><button class="nc-btn danger" data-nc-delete type="button">${managed?'Remove Tool':'Delete Slot'}</button><button class="nc-btn primary" data-nc-save type="button" ${nativeManaged?'disabled':''}>Save Code</button></div>`;q('[data-nc-event]').value=slot.event;
    q('[data-nc-save]').addEventListener('click',()=>saveEditor(slot));q('[data-nc-delete]').addEventListener('click',()=>deleteSlot(slot));const error=LukeAnimate.getNodeCodeError(selected,slot.id);if(error)setStatus('Code error: '+error,'bad');}
  function saveEditor(slot){if(!selected)return;const next=Object.assign({},slot,{name:q('[data-nc-name]').value.trim()||slot.name,event:q('[data-nc-event]').value,enabled:q('[data-nc-enabled]').checked,code:q('[data-nc-code]').value});if(slot.source==='type-tool'){const tool=LukeAnimate.getTypeTool(selected,slot.toolId);if(!tool){setStatus('That Type Tool is no longer attached.','bad');return;}tool.enabled=next.enabled;tool.code=next.code;LukeAnimate.setTypeTool(selected,slot.toolId,tool);}else LukeAnimate.setNodeCodeSlot(selected,next);selectedSlotId=slot.id;renderAll();setStatus('Code saved in its '+(next.event==='tool'?'managed':next.event)+' slot.','good');}
  function deleteSlot(slot){if(!selected)return;if(slot.source==='type-tool'){if(!LukeAnimate.removeTypeTool(selected,slot.toolId)){setStatus('Could not remove the managed Type Tool.','bad');return;}}else LukeAnimate.removeNodeCodeSlot(selected,slot.id);selectedSlotId='';renderAll();setStatus(slot.source==='type-tool'?'Type Tool and its managed slot removed.':'Code slot removed.','good');}
  function addSlot(){if(!selected){setStatus('Select one Clip instance first.','bad');return;}const now=Date.now().toString(36),slot={id:'custom-'+now,name:'Code '+(slots().filter(entry=>entry.source==='custom').length+1),event:'update',enabled:true,code:'// Runs every playback update.\n// Keep persistent values in context.state.\nreturn {};',source:'custom',x:360,y:70+slots().length*110};LukeAnimate.setNodeCodeSlot(selected,slot);selectedSlotId=slot.id;renderAll();setStatus('New Update code slot added.','good');}
  function renderAll(){const list=slots();if(selectedSlotId&&!list.some(slot=>slot.id===selectedSlotId))selectedSlotId='';if(!selectedSlotId&&list.length)selectedSlotId=list[0].id;q('[data-nc-selected]').textContent=clipName(selected);q('[data-nc-add]').disabled=!selected;renderLibrary(list);renderGraph(list);renderEditor(list.find(slot=>slot.id===selectedSlotId)||null);}
  function refresh(){const next=selectedClip(),nextKey=next?String(next.id||next.clipId):'';if(nextKey!==selectionKey){selected=next;selectionKey=nextKey;selectedSlotId='';renderAll();setStatus(next?'Clip loaded. Add or select a code slot.':'Select one Clip instance on the stage.',next?'good':'');return;}if(selected&&selectedSlotId){const error=LukeAnimate.getNodeCodeError(selected,selectedSlotId);if(error)setStatus('Code error: '+error,'bad');}}
  q('[data-nc-add]').addEventListener('click',addSlot);q('.nc-close').addEventListener('click',()=>{clearInterval(refreshTimer);panel.remove();setActive(false);});const refreshTimer=setInterval(refresh,300);refresh();
})();
