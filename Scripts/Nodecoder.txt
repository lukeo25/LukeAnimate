/* Luke Animate Node Coder v3.1 — ActionScript-style mouse events and Clip playback actions */
(function(){
  "use strict";
  const PANEL_ID="lukeanimate-nodecoder-panel",STYLE_ID="lukeanimate-nodecoder-style";
  const GROUP_ID=String(group&&group.id?group.id:"tools"),ITEM_ID=String(item&&(item.id||item.label)?(item.id||item.label):"Node_Coder");
  const ICON=String(item&&item.iconUrl?item.iconUrl:"https://lukeo25.github.io/LukeAnimate/SVGs/Node.svg");
  if(!LukeAnimate||Number(LukeAnimate.version||0)<127||typeof LukeAnimate.getNodeCodeSlots!=="function")throw new Error("Node Coder requires Luke Animate v253 or later.");
  const setActive=active=>{if(LukeAnimate.setConfiguredToolActive)LukeAnimate.setConfiguredToolActive(GROUP_ID,ITEM_ID,!!active);};
  const previous=document.getElementById(PANEL_ID);if(previous)previous.remove();
  if(!document.getElementById(STYLE_ID)){
    const style=document.createElement("style");style.id=STYLE_ID;style.textContent=`
      #${PANEL_ID}{position:fixed;z-index:10000;left:24px;top:24px;width:min(1240px,calc(100vw - 36px));height:min(760px,calc(100vh - 36px));min-width:760px;min-height:470px;display:flex;flex-direction:column;overflow:hidden;background:#eef1f4;border:1px solid #b9c2cc;border-radius:14px;box-shadow:0 22px 70px rgba(0,0,0,.3);color:#273442;font:13px Inter,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}
      #${PANEL_ID} *{box-sizing:border-box} #${PANEL_ID} button,#${PANEL_ID} input,#${PANEL_ID} select,#${PANEL_ID} textarea{font:inherit}
      #${PANEL_ID} .nc-head{height:52px;flex:0 0 52px;display:flex;align-items:center;gap:10px;padding:8px 11px;background:#fff;border-bottom:1px solid #cfd5dc;cursor:move;user-select:none} #${PANEL_ID} .nc-head img{width:31px;height:31px;object-fit:contain} #${PANEL_ID} .nc-title{flex:1;min-width:0} #${PANEL_ID} .nc-title strong{display:block;font-size:16px;color:#28414a} #${PANEL_ID} .nc-title span{display:block;font-size:9px;letter-spacing:.13em;text-transform:uppercase;color:#7c8793;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
      #${PANEL_ID} .nc-window-btn{width:34px;height:34px;border:1px solid #bbc4ce;border-radius:9px;background:#fff;color:#536170;font-size:18px;line-height:1;cursor:pointer} #${PANEL_ID} .nc-window-btn:hover{background:#f2f5f7} #${PANEL_ID}.nc-minimized{height:52px!important;min-height:52px!important} #${PANEL_ID}.nc-minimized .nc-toolbar,#${PANEL_ID}.nc-minimized .nc-main{display:none} #${PANEL_ID}.nc-maximized{border-radius:10px}
      #${PANEL_ID} .nc-toolbar{height:48px;flex:0 0 48px;display:flex;align-items:center;gap:7px;padding:7px 9px;background:#f7f9fb;border-bottom:1px solid #cbd2da;overflow-x:auto;overflow-y:hidden} #${PANEL_ID} .nc-spacer{flex:1 1 auto;min-width:10px}
      #${PANEL_ID} .nc-btn,#${PANEL_ID} .nc-select{height:32px;border:1px solid #bdc6d0;border-radius:8px;background:#fff;color:#536170;padding:0 10px;white-space:nowrap} #${PANEL_ID} .nc-btn{cursor:pointer} #${PANEL_ID} .nc-btn:hover{background:#edf2f5} #${PANEL_ID} .nc-btn.primary{border-color:#0d9c86;background:#0d9c86;color:#fff;font-weight:750} #${PANEL_ID} .nc-btn.primary:hover{background:#0b8c79} #${PANEL_ID} .nc-zoom-label{min-width:44px;text-align:center;color:#6c7985;font-size:11px;font-variant-numeric:tabular-nums}
      #${PANEL_ID} .nc-main{flex:1;min-height:0;display:grid;grid-template-columns:240px minmax(360px,1fr) 350px;position:relative} #${PANEL_ID} .nc-sidebar,#${PANEL_ID} .nc-inspector{min-width:0;background:#f7f9fb;display:flex;flex-direction:column;overflow:hidden} #${PANEL_ID} .nc-sidebar{border-right:1px solid #cbd2da} #${PANEL_ID} .nc-inspector{border-left:1px solid #cbd2da}
      #${PANEL_ID} .nc-pane-title{height:39px;flex:0 0 39px;display:flex;align-items:center;gap:7px;padding:0 11px;border-bottom:1px solid #d5dbe2;background:#fff;font-size:11px;font-weight:800;letter-spacing:.09em;text-transform:uppercase;color:#667481} #${PANEL_ID} .nc-count{margin-left:auto;padding:2px 7px;border-radius:99px;background:#e7ecef;color:#65727e;font-size:9px;letter-spacing:0}
      #${PANEL_ID} .nc-search{margin:10px 10px 7px;width:calc(100% - 20px);height:34px;padding:0 10px;border:1px solid #bfc9d3;border-radius:8px;background:#fff;color:#263542;outline:none} #${PANEL_ID} .nc-search:focus{border-color:#0d9c86;box-shadow:0 0 0 2px rgba(13,156,134,.12)}
      #${PANEL_ID} .nc-clip-list{flex:1;min-height:0;overflow:auto;padding:3px 10px 12px} #${PANEL_ID} .nc-clip-row{display:grid;grid-template-columns:34px minmax(0,1fr) 28px;align-items:center;gap:8px;width:100%;margin:0 0 7px;padding:8px;border:1px solid #ccd4dc;border-radius:9px;background:#fff;color:#374553;text-align:left;cursor:grab} #${PANEL_ID} .nc-clip-row:hover,#${PANEL_ID} .nc-clip-row.active{border-color:#0d9c86;box-shadow:0 0 0 2px rgba(13,156,134,.1)} #${PANEL_ID} .nc-clip-row img{width:30px;height:30px;padding:5px;border-radius:7px;background:#eaf7f4} #${PANEL_ID} .nc-clip-row strong{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:12px} #${PANEL_ID} .nc-clip-row small{display:block;margin-top:2px;color:#87939e;font-size:9px} #${PANEL_ID} .nc-clip-add{width:27px;height:27px;border:1px solid #bfcbd3;border-radius:7px;background:#f7fafb;color:#0d8f7c;cursor:pointer;font-size:17px;line-height:1}
      #${PANEL_ID} .nc-library-hint{padding:9px 11px;border-top:1px solid #d5dbe2;background:#fff;color:#7b8792;font-size:10px;line-height:1.4}
      #${PANEL_ID} .nc-canvas-wrap{position:relative;min-width:0;min-height:0;overflow:hidden;background:#edf1f4} #${PANEL_ID} .nc-viewport{position:absolute;inset:0;overflow:hidden;cursor:grab;background-color:#edf1f4;background-image:radial-gradient(circle at 1px 1px,#bdc7d0 1.15px,transparent 0);background-size:26px 26px} #${PANEL_ID} .nc-viewport.panning{cursor:grabbing} #${PANEL_ID} .nc-world{position:absolute;left:0;top:0;width:1px;height:1px;transform-origin:0 0} #${PANEL_ID} .nc-links{position:absolute;left:0;top:0;width:1px;height:1px;overflow:visible;pointer-events:none} #${PANEL_ID} .nc-nodes{position:absolute;left:0;top:0}
      #${PANEL_ID} .nc-canvas-hint{position:absolute;left:14px;bottom:12px;max-width:440px;padding:7px 9px;border:1px solid rgba(188,199,208,.85);border-radius:8px;background:rgba(255,255,255,.83);color:#798691;font-size:10px;line-height:1.4;pointer-events:none}
      #${PANEL_ID} .nc-node{position:absolute;border:1px solid #b9c5ce;border-radius:12px;background:#fff;color:#354451;box-shadow:0 8px 22px rgba(38,55,68,.17);user-select:none} #${PANEL_ID} .nc-node.active{border-color:#0d9c86;box-shadow:0 0 0 2px rgba(13,156,134,.15),0 8px 22px rgba(38,55,68,.17)} #${PANEL_ID} .nc-node-head{height:39px;display:flex;align-items:center;gap:7px;padding:0 10px;border-bottom:1px solid #d3dae0;border-radius:11px 11px 0 0;background:#f5f8fa;cursor:grab;font-weight:800} #${PANEL_ID} .nc-node-head span{min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap} #${PANEL_ID} .nc-node-badge{flex:0 0 auto;padding:2px 6px;border-radius:99px;background:#dff3ee;color:#0b806e;font-size:8px;text-transform:uppercase;letter-spacing:.07em} #${PANEL_ID} .nc-node-remove{margin-left:auto;width:22px;height:22px;border:0;border-radius:6px;background:transparent;color:#7d8993;cursor:pointer} #${PANEL_ID} .nc-node-remove:hover{background:#e5eaee;color:#a34c4c}
      #${PANEL_ID} .nc-clip-node{width:230px;overflow:hidden} #${PANEL_ID} .nc-event-row{position:relative;height:34px;display:flex;align-items:center;padding:0 13px 0 29px;border-top:1px solid #eef1f3;color:#60707d;font-size:11px} #${PANEL_ID} .nc-event-row small{margin-left:auto;color:#96a1aa;font-size:8px;text-transform:uppercase} #${PANEL_ID} .nc-code-node{width:230px;min-height:105px} #${PANEL_ID} .nc-code-preview{height:64px;overflow:hidden;padding:9px 12px;color:#74818c;font:10px/1.42 Consolas,monospace;white-space:pre-wrap} #${PANEL_ID} .nc-managed{background:#eee6fb;color:#7453a4}
      #${PANEL_ID} .nc-pip{position:absolute;width:14px;height:14px;border:2px solid #fff;border-radius:50%;box-shadow:0 0 0 1px #8e9ba6;cursor:crosshair;padding:0} #${PANEL_ID} .nc-pip.input{left:8px;top:10px;background:#8b98a3} #${PANEL_ID} .nc-pip.output{right:-7px;top:67px;background:#0d9c86} #${PANEL_ID} .nc-event-row[data-event="load"] .nc-pip{background:#9a73d5} #${PANEL_ID} .nc-event-row[data-event="update"] .nc-pip{background:#df6969} #${PANEL_ID} .nc-event-row[data-event="unload"] .nc-pip{background:#3bb99f} #${PANEL_ID} .nc-event-row[data-event="tool"] .nc-pip{background:#8a69b8;cursor:default}
      #${PANEL_ID} .nc-inspector-body{flex:1;min-height:0;overflow:auto;padding:11px} #${PANEL_ID} .nc-field{display:flex;flex-direction:column;gap:4px;margin-bottom:9px;color:#60707e;font-size:11px;font-weight:750} #${PANEL_ID} .nc-field input,#${PANEL_ID} .nc-field select{width:100%;height:34px;padding:0 8px;border:1px solid #bfc9d3;border-radius:8px;background:#fff;color:#263542} #${PANEL_ID} .nc-check{display:flex;align-items:center;gap:7px;margin-bottom:9px;color:#536572} #${PANEL_ID} .nc-check input{accent-color:#0d9c86} #${PANEL_ID} .nc-code{width:100%;height:300px;resize:vertical;padding:10px;border:1px solid #34434e;border-radius:9px;background:#1d2932;color:#e0f2ee;font:12px/1.48 Consolas,monospace;tab-size:2} #${PANEL_ID} .nc-code:read-only{background:#e9eef1;color:#697681;border-color:#c4cdd4}
      #${PANEL_ID} .nc-simple-card{margin:3px 0 11px;padding:11px;border:1px solid #c7d1d9;border-radius:10px;background:#fff} #${PANEL_ID} .nc-simple-title{display:flex;align-items:center;gap:7px;margin-bottom:10px;color:#334752;font-size:12px;font-weight:800} #${PANEL_ID} .nc-simple-title span{padding:3px 7px;border-radius:99px;background:#dff3ee;color:#0b806e;font-size:9px;text-transform:uppercase;letter-spacing:.07em}
      #${PANEL_ID} .nc-control{display:block;margin:0 0 10px;color:#60707e;font-size:11px;font-weight:700} #${PANEL_ID} .nc-control:last-child{margin-bottom:0} #${PANEL_ID} .nc-control-row{display:flex;align-items:center;gap:8px;margin-top:5px} #${PANEL_ID} .nc-control select,#${PANEL_ID} .nc-control input[type="text"],#${PANEL_ID} .nc-control input[type="number"]{width:100%;height:34px;padding:0 8px;border:1px solid #bfc9d3;border-radius:8px;background:#f9fbfc;color:#263542} #${PANEL_ID} .nc-control input[type="range"]{flex:1;min-width:0;accent-color:#0d9c86} #${PANEL_ID} .nc-control output{min-width:58px;padding:4px 6px;border:1px solid #d1d9df;border-radius:6px;background:#f4f7f9;color:#42535f;text-align:center;font-size:10px;font-variant-numeric:tabular-nums}
      #${PANEL_ID} .nc-code-details{margin-top:9px;border:1px solid #cbd4db;border-radius:9px;background:#fff;overflow:hidden} #${PANEL_ID} .nc-code-details summary{padding:10px 11px;cursor:pointer;color:#536572;font-size:11px;font-weight:800;text-transform:uppercase;letter-spacing:.06em} #${PANEL_ID} .nc-code-details[open] summary{border-bottom:1px solid #d8dee3} #${PANEL_ID} .nc-code-details .nc-code-wrap{padding:9px} #${PANEL_ID} .nc-generated-note{margin:0 0 8px;color:#7b8792;font-size:10px;line-height:1.4}
      #${PANEL_ID} .nc-help{margin:7px 0;color:#71808e;font-size:10px;line-height:1.45} #${PANEL_ID} .nc-actions{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:9px} #${PANEL_ID} .nc-actions .danger{color:#a34646} #${PANEL_ID} .nc-status{flex:0 0 auto;margin:0 10px 10px;padding:8px 9px;border:1px solid #cbd3db;border-radius:8px;background:#fff;color:#64717d;font-size:10px;min-height:30px} #${PANEL_ID} .nc-status.good{border-color:#0d9c86;color:#0b806e} #${PANEL_ID} .nc-status.bad{border-color:#c56d6d;color:#a34141} #${PANEL_ID} .nc-empty{padding:22px 14px;color:#7b8996;text-align:center;line-height:1.5}
      #${PANEL_ID} .nc-editor-card{padding:12px;border:1px solid #c7d1d9;border-radius:10px;background:#fff} #${PANEL_ID} .nc-editor-card strong{display:block;margin-bottom:5px;color:#334752} #${PANEL_ID} .nc-editor-card p{margin:0 0 10px;color:#71808e;font-size:10px;line-height:1.45} #${PANEL_ID} .nc-timeline-row{display:grid;grid-template-columns:105px minmax(0,1fr);align-items:center;gap:9px;margin-bottom:10px;color:#687584;font-size:12px} #${PANEL_ID} .nc-timeline-row input,#${PANEL_ID} .nc-timeline-row select{width:100%;height:36px;padding:0 10px;border:1px solid #c7d0d9;border-radius:9px;background:#f3f5f7;color:#303d49} #${PANEL_ID} .nc-timeline-card{margin-top:10px;padding:11px;border:1px solid #cbd4dc;border-radius:11px;background:#fff} #${PANEL_ID} .nc-timeline-card h4{margin:0 0 13px;color:#677483;font:800 10px Consolas,monospace;letter-spacing:.1em;text-transform:uppercase} #${PANEL_ID} .nc-timeline-help{margin:2px 0 0;color:#71808e;font-size:10.5px;line-height:1.45} #${PANEL_ID} .nc-timeline-delete{width:100%;height:36px;margin-top:12px;border:1px solid #ccd4dc;border-radius:8px;background:#f6f7f9;color:#474f58;cursor:pointer} .nc-code-popup{position:fixed;z-index:10020;left:max(16px,calc(50vw - 360px));top:max(16px,calc(50vh - 280px));width:min(720px,calc(100vw - 32px));height:min(560px,calc(100vh - 32px));display:flex;flex-direction:column;overflow:hidden;border:1px solid #aeb9c3;border-radius:13px;background:#eef1f4;box-shadow:0 24px 80px rgba(0,0,0,.38);font:13px Inter,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif} .nc-code-popup *{box-sizing:border-box}.nc-code-popup-head{height:50px;flex:0 0 50px;display:flex;align-items:center;gap:9px;padding:8px 11px;border-bottom:1px solid #cbd3da;background:#fff;cursor:move}.nc-code-popup-head strong{flex:1;color:#29434a}.nc-code-popup-head button{width:32px;height:32px;border:1px solid #bbc4ce;border-radius:8px;background:#fff;color:#536170;font-size:19px;cursor:pointer}.nc-code-popup textarea{flex:1;min-height:0;margin:10px;resize:none;padding:12px;border:1px solid #34434e;border-radius:9px;background:#17242d;color:#e0f2ee;font:13px/1.5 Consolas,monospace;tab-size:2}.nc-code-popup-actions{display:flex;justify-content:flex-end;gap:8px;padding:0 10px 10px}.nc-code-popup-actions button{height:34px;padding:0 14px;border:1px solid #bdc6d0;border-radius:8px;background:#fff;color:#536170;cursor:pointer}.nc-code-popup-actions .primary{border-color:#0d9c86;background:#0d9c86;color:#fff;font-weight:750}
      #${PANEL_ID} .nc-inspector-body.nc-has-docked{padding:0;overflow:hidden} #${PANEL_ID} .nc-docked-editor{position:static!important;inset:auto!important;width:100%!important;max-width:none!important;height:100%!important;min-height:0!important;border:0!important;border-radius:0!important;box-shadow:none!important} #${PANEL_ID} .nc-docked-editor>[data-floating-panel-drag],#${PANEL_ID} .nc-docked-editor>.dd-head{display:none!important} #${PANEL_ID} .nc-docked-editor>.dd-body{padding-top:8px!important}
      @media(max-width:950px){#${PANEL_ID} .nc-main{grid-template-columns:200px 1fr}#${PANEL_ID} .nc-inspector{position:absolute;right:0;top:0;bottom:0;width:min(350px,70vw);z-index:4;box-shadow:-12px 0 30px rgba(0,0,0,.2)}}`;
    document.head.appendChild(style);
  }
  const panel=document.createElement("section");panel.id=PANEL_ID;panel.innerHTML=`<div class="nc-head" data-floating-panel-drag><img src="${ICON.replace(/"/g,"&quot;")}" alt=""><div class="nc-title"><strong>Node Coder</strong><span>Clip and Timeline nodes, connected actions and live code</span></div><button class="nc-window-btn nc-minimize" type="button" title="Minimize" aria-label="Minimize">−</button><button class="nc-window-btn nc-maximize" type="button" title="Maximize" aria-label="Maximize">□</button><button class="nc-window-btn nc-close" type="button" title="Close" aria-label="Close">×</button></div><div class="nc-toolbar"><button class="nc-btn" data-action="refresh" type="button">Refresh</button><button class="nc-btn" data-action="fit" type="button">Fit</button><button class="nc-btn" data-action="save-layout" type="button">Save</button><button class="nc-btn" data-action="load-layout" type="button">Load</button><input data-layout-file type="file" accept="application/json,.json" hidden><span class="nc-spacer"></span><select class="nc-select" data-template><optgroup label="Clip"><option value="blank">Code</option><option value="drag">Drag &amp; Drop</option><option value="move">Move</option><option value="rotate">Rotate</option><option value="opacity">Pulse Opacity</option><option value="variable">Set Variable</option><option value="condition">If Variable</option><option value="rando">Rando</option></optgroup><optgroup label="Timeline"><option value="timelineStop">Stop</option><option value="timelineGotoStop">Go To and Stop</option><option value="timelinePlay">Play</option><option value="timelineGotoPlay">Go To and Play</option><option value="timelineRando">Rando</option></optgroup></select><select class="nc-select" data-event><option value="update">Update</option><option value="load">Load</option><option value="unload">Unload</option><option value="click">Mouse Click</option><option value="pointerdown">Pointer Down</option><option value="pointerup">Pointer Up</option><option value="dragstart">Drag Start</option><option value="drag">While Dragging</option><option value="drop">Drop</option><option value="correctDrop">Correct Drop</option><option value="incorrectDrop">Incorrect Drop</option><option value="frame">Timeline Frame</option><option value="tool">Type Tools</option></select><button class="nc-btn primary" data-action="add-node" type="button">+ Add Node</button><button class="nc-btn" data-action="zoom-out" type="button">−</button><span class="nc-zoom-label" data-zoom>85%</span><button class="nc-btn" data-action="zoom-in" type="button">+</button></div><div class="nc-main"><aside class="nc-sidebar"><div class="nc-pane-title">Library <span class="nc-count" data-clip-count>0</span></div><input class="nc-search" data-search type="search" placeholder="Search Timeline or Clips"><div class="nc-clip-list" data-clip-list></div><div class="nc-library-hint">Timeline controls frame-action nodes. Drag a Timeline or Clip item onto the grid, double-click it, or use +.</div></aside><main class="nc-canvas-wrap"><div class="nc-viewport" data-viewport><div class="nc-world" data-world><svg class="nc-links" data-links></svg><div class="nc-nodes" data-nodes></div></div><div class="nc-canvas-hint">Drag the empty grid to pan · Wheel to zoom · Add Timeline actions or connect Clip code</div></div></main><aside class="nc-inspector"><div class="nc-pane-title">Node Inspector</div><div class="nc-inspector-body" data-editor><div class="nc-empty">Select a node to edit it.</div></div><div class="nc-status" data-status>Ready.</div></aside></div>`;
  document.body.appendChild(panel);LukeAnimate.registerFloatingPanel(panel);LukeAnimate.bringFloatingPanelToFront(panel);LukeAnimate.clampFloatingPanelToViewport(panel);setActive(true);
  const q=s=>panel.querySelector(s),viewport=q("[data-viewport]"),world=q("[data-world]"),nodesLayer=q("[data-nodes]"),links=q("[data-links]"),clipList=q("[data-clip-list]"),editor=q("[data-editor]"),status=q("[data-status]"),search=q("[data-search]"),layoutFile=q("[data-layout-file]");
  q("[data-template] optgroup[label='Clip']").insertAdjacentHTML("beforeend",'<option value="clipcontrol">Clip Playback</option>');
  q("[data-event]").innerHTML='<option value="update">Update</option><option value="load">Load</option><option value="unload">Unload</option><option value="mouseover">Roll Over</option><option value="mouseout">Roll Out</option><option value="hover">Hover</option><option value="click">Mouse Click</option><option value="pointerdown">Mouse Down</option><option value="pointerup">Mouse Up</option><option value="releaseoutside">Release Outside</option><option value="dragstart">Drag Start</option><option value="drag">While Dragging</option><option value="drop">Drop</option><option value="correctDrop">Correct Drop</option><option value="incorrectDrop">Incorrect Drop</option><option value="frame">Timeline Frame</option><option value="tool">Type Tools</option>';
  const state={clips:[],graph:new Map(),activeKey:"",selectedOwnerKey:"",selectedSlotId:"",scale:.85,panX:70,panY:65,panning:null,nodeDrag:null,linking:null,sceneFingerprint:"",lastStageSelectionKey:""};
  const escapeHtml=v=>String(v??"").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;"),clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
  let normalPanelRect=null,minimizedFromMaximized=false;
  function rememberPanelRect(){const r=panel.getBoundingClientRect();normalPanelRect={left:r.left,top:r.top,width:r.width,height:r.height};}
  function clampPanel(){
    if(panel.classList.contains("nc-maximized")){panel.style.left="8px";panel.style.top="8px";panel.style.width=Math.max(320,window.innerWidth-16)+"px";panel.style.height=Math.max(52,window.innerHeight-16)+"px";return;}
    const r=panel.getBoundingClientRect(),left=clamp(r.left,0,Math.max(0,window.innerWidth-Math.min(r.width,window.innerWidth))),top=clamp(r.top,0,Math.max(0,window.innerHeight-52));
    panel.style.left=left+"px";panel.style.top=top+"px";
  }
  function toggleMinimize(){
    const minimizing=!panel.classList.contains("nc-minimized");
    if(minimizing){minimizedFromMaximized=panel.classList.contains("nc-maximized");if(!minimizedFromMaximized)rememberPanelRect();panel.classList.remove("nc-maximized");panel.classList.add("nc-minimized");q(".nc-minimize").textContent="□";q(".nc-minimize").title="Restore";}
    else{panel.classList.remove("nc-minimized");q(".nc-minimize").textContent="−";q(".nc-minimize").title="Minimize";if(minimizedFromMaximized){panel.classList.add("nc-maximized");clampPanel();}else if(normalPanelRect){Object.assign(panel.style,{left:normalPanelRect.left+"px",top:normalPanelRect.top+"px",width:normalPanelRect.width+"px",height:normalPanelRect.height+"px"});}}
    clampPanel();
  }
  function toggleMaximize(){
    if(panel.classList.contains("nc-minimized")){panel.classList.remove("nc-minimized");q(".nc-minimize").textContent="−";q(".nc-minimize").title="Minimize";}
    const maximizing=!panel.classList.contains("nc-maximized");
    if(maximizing){rememberPanelRect();panel.classList.add("nc-maximized");q(".nc-maximize").textContent="❐";q(".nc-maximize").title="Restore";clampPanel();}
    else{panel.classList.remove("nc-maximized");q(".nc-maximize").textContent="□";q(".nc-maximize").title="Maximize";if(normalPanelRect)Object.assign(panel.style,{left:normalPanelRect.left+"px",top:normalPanelRect.top+"px",width:normalPanelRect.width+"px",height:normalPanelRect.height+"px"});clampPanel();}
  }
  const setStatus=(m,k="")=>{status.textContent=String(m||"");status.className="nc-status"+(k?" "+k:"");};
  const TIMELINE_KEY="__lukeanimate_timeline__",TIMELINE_ICON="https://lukeo25.github.io/LukeAnimate/SVGs/Animate.svg";
  function objectKey(o,i=0){return String(o&&(o.id||o.timelineLayerId)||((o&&o.clipId)||"clip")+"-"+i);}
  function clipLabel(o){const defs=LukeAnimate.getClips?LukeAnimate.getClips():{};return String(o&&(o.name||(defs[o.clipId]&&defs[o.clipId].name))||"Clip");}
  function sceneClipEntries(){const objects=LukeAnimate.getDrawObjects?LukeAnimate.getDrawObjects():[];return [{object:null,key:TIMELINE_KEY,name:"Timeline",index:-1,timeline:true}].concat(objects.map((object,index)=>({object,index})).filter(e=>e.object&&e.object.type==="clipinstance").map(e=>({object:e.object,key:objectKey(e.object,e.index),name:clipLabel(e.object),index:e.index,timeline:false})));}
  function selectedStageClip(){const list=LukeAnimate.getSelectedDrawObjects?LukeAnimate.getSelectedDrawObjects():[];return list.length===1&&list[0]&&list[0].type==="clipinstance"?list[0]:null;}
  function timelineSlots(){return (LukeAnimate.getTimelineScriptMarkers?LukeAnimate.getTimelineScriptMarkers():[]).map(marker=>({id:marker.id,name:(marker.type==="rando"?"Rando":"Stop")+" · Frame "+(Number(marker.frame)+1),event:"timeline",enabled:true,source:"timeline-script",layerId:marker.layerId,markerType:marker.type,frame:marker.frame,config:marker.config||{},x:Number.isFinite(Number(marker.nodeX))?Number(marker.nodeX):70,y:Number.isFinite(Number(marker.nodeY))?Number(marker.nodeY):90+Number(marker.frame||0)*3,code:marker.type==="rando"?"Choose a random value and run matching rules.":"Control Timeline or Clip playback."}));}
  const activeEntry=()=>state.graph.get(state.activeKey)||null,entryForObject=o=>state.clips.find(e=>e.object===o)||null,slotsFor=e=>e?(e.timeline?timelineSlots():LukeAnimate.getNodeCodeSlots(e.object)):[];
  function slotEntry(id,ownerKey=state.selectedOwnerKey){for(const entry of state.graph.values()){if(ownerKey&&entry.key!==ownerKey)continue;const slot=slotsFor(entry).find(s=>s.id===id);if(slot)return {entry,slot};}return null;}
  function screenToWorld(x,y){const r=viewport.getBoundingClientRect();return {x:(x-r.left-state.panX)/state.scale,y:(y-r.top-state.panY)/state.scale};}
  function applyTransform(){world.style.transform=`translate(${state.panX}px,${state.panY}px) scale(${state.scale})`;q("[data-zoom]").textContent=Math.round(state.scale*100)+"%";}
  function addGraphClip(entry,position){if(!entry)return;if(!state.graph.has(entry.key)){const saved=entry.object&&entry.object._lukeAnimateNodeGraph||{},fallback={x:360,y:55+state.graph.size*260};state.graph.set(entry.key,Object.assign({},entry,{x:Number((position&&position.x)??saved.x??fallback.x),y:Number((position&&position.y)??saved.y??fallback.y)}));}if(state.activeKey!==entry.key){state.selectedOwnerKey="";state.selectedSlotId="";}state.activeKey=entry.key;renderAll();}
  function removeGraphClip(key){state.graph.delete(key);if(state.activeKey===key)state.activeKey=state.graph.size?state.graph.keys().next().value:"";state.selectedOwnerKey="";state.selectedSlotId="";renderAll();}
  function refreshClipLibrary(){
    state.clips=sceneClipEntries();const byKey=new Map(state.clips.map(e=>[e.key,e]));for(const [key,old] of state.graph){const now=byKey.get(key);if(now){old.object=now.object;old.name=now.name;}else state.graph.delete(key);}
    const filter=search.value.trim().toLowerCase(),visible=state.clips.filter(e=>!filter||e.name.toLowerCase().includes(filter)||e.key.toLowerCase().includes(filter));q("[data-clip-count]").textContent=String(state.clips.length);
    clipList.innerHTML=visible.length?visible.map(e=>`<div class="nc-clip-row${state.activeKey===e.key?" active":""}" draggable="true" data-clip-key="${escapeHtml(e.key)}"><img src="${escapeHtml(e.timeline?TIMELINE_ICON:ICON)}" alt=""><span><strong>${escapeHtml(e.name)}</strong><small>${e.timeline?"Frame actions":escapeHtml(e.key)}</small></span><button class="nc-clip-add" data-add-clip="${escapeHtml(e.key)}" type="button">+</button></div>`).join(""):'<div class="nc-empty">No library items.</div>';
    clipList.querySelectorAll("[data-clip-key]").forEach(row=>{const key=row.dataset.clipKey;row.addEventListener("dragstart",ev=>{ev.dataTransfer.setData("text/x-la-clip",key);ev.dataTransfer.effectAllowed="copy";});row.addEventListener("dblclick",()=>addGraphClip(state.clips.find(e=>e.key===key)));row.addEventListener("click",ev=>{if(ev.target.closest("button"))return;if(state.graph.has(key)){state.activeKey=key;state.selectedOwnerKey="";state.selectedSlotId="";renderAll();}});});clipList.querySelectorAll("[data-add-clip]").forEach(b=>b.addEventListener("click",()=>addGraphClip(state.clips.find(e=>e.key===b.dataset.addClip))));
  }
  const portOrder=["load","update","unload","mouseover","mouseout","hover","click","pointerdown","pointerup","releaseoutside","dragstart","drag","drop","correctDrop","incorrectDrop","frame","tool"],portLabel={load:"Load",update:"Update",unload:"Unload",mouseover:"Roll Over",mouseout:"Roll Out",hover:"Hover",click:"Mouse Click",pointerdown:"Mouse Down",pointerup:"Mouse Up",releaseoutside:"Release Outside",dragstart:"Drag Start",drag:"While Dragging",drop:"Drop",correctDrop:"Correct Drop",incorrectDrop:"Incorrect Drop",frame:"Timeline Frame",tool:"Type Tools",timeline:"Frame Actions"},portY=e=>39+portOrder.indexOf(e)*34+17;
  const uiDefaults={
    drag:{button:"primary",axis:"both",grab:"offset",release:"stay"},
    move:{direction:"right",speed:120},
    rotate:{direction:"clockwise",speed:90},
    opacity:{minimum:10,maximum:100,speed:1},
    variable:{variable:"score",type:"number",value:"1"},
    condition:{variable:"score",operator:">=",value:10,trueOpacity:100,falseOpacity:40},
    rando:{variable:"rng",minimum:0,maximum:10,integer:"yes"},
    clipcontrol:{action:"gotoStop",frame:"1"}
  };
  function slotUi(slot){
    const source=slot&&slot.ui&&typeof slot.ui==="object"?slot.ui:null,kind=source&&uiDefaults[source.kind]?source.kind:"";
    if(!kind)return null;
    return {kind,values:Object.assign({},uiDefaults[kind],source.values&&typeof source.values==="object"?source.values:{})};
  }
  function safeStateKey(slotId,suffix){return "nc_"+String(slotId||"node").replace(/[^a-z0-9_]/gi,"_")+"_"+suffix;}
  function generatedCode(kind,values,slotId){
    if(kind==="drag"){
      const button={primary:0,middle:1,secondary:2}[values.button]??0,axis=["both","horizontal","vertical"].includes(values.axis)?values.axis:"both",keepOffset=values.grab!=="centre",returnHome=values.release==="return",key=safeStateKey(slotId,"drag");
      return `const pointer = context.pointer;\nconst key = ${JSON.stringify(key)};\nif (!context.state[key]) context.state[key] = { dragging:false, x:null, y:null, startX:0, startY:0, offsetX:0, offsetY:0 };\nconst drag = context.state[key];\nif (pointer.pressed && pointer.button === ${button} && pointer.over) {\n  const currentX = Number(context.effective.x) || 0;\n  const currentY = Number(context.effective.y) || 0;\n  drag.dragging = true;\n  drag.startX = currentX; drag.startY = currentY;\n  drag.x = currentX; drag.y = currentY;\n  drag.offsetX = ${keepOffset?"currentX - pointer.x":"0"};\n  drag.offsetY = ${keepOffset?"currentY - pointer.y":"0"};\n}\nif (drag.dragging && pointer.down) {\n  ${axis!=="vertical"?"drag.x = pointer.x + drag.offsetX;":""}\n  ${axis!=="horizontal"?"drag.y = pointer.y + drag.offsetY;":""}\n}\nif (drag.dragging && pointer.released) {\n  drag.dragging = false;\n  ${returnHome?"drag.x = drag.startX; drag.y = drag.startY;":""}\n}\nif (Number.isFinite(drag.x) && Number.isFinite(drag.y)) return { x:drag.x, y:drag.y };\nreturn {};`;
    }
    if(kind==="move"){
      const direction=["left","right","up","down"].includes(values.direction)?values.direction:"right",axis=(direction==="left"||direction==="right")?"x":"y",sign=(direction==="left"||direction==="up")?-1:1,speed=Math.max(0,Number(values.speed)||0),key=safeStateKey(slotId,"position");
      return `if (!Number.isFinite(context.state[${JSON.stringify(key)}])) context.state[${JSON.stringify(key)}] = Number(context.object.${axis}) || 0;\ncontext.state[${JSON.stringify(key)}] += ${sign*speed} * context.delta;\nreturn { ${axis}: context.state[${JSON.stringify(key)}] };`;
    }
    if(kind==="rotate"){
      const sign=values.direction==="counterclockwise"?-1:1,speed=Math.max(0,Number(values.speed)||0),key=safeStateKey(slotId,"rotation");
      return `if (!Number.isFinite(context.state[${JSON.stringify(key)}])) context.state[${JSON.stringify(key)}] = Number(context.object.rotation) || 0;\ncontext.state[${JSON.stringify(key)}] += ${sign*speed} * context.delta;\nreturn { rotation: context.state[${JSON.stringify(key)}] };`;
    }
    if(kind==="opacity"){
      const low=clamp(Number(values.minimum)||0,0,100)/100,high=clamp(Number(values.maximum)||100,0,100)/100,min=Math.min(low,high),max=Math.max(low,high),speed=Math.max(.05,Number(values.speed)||1),mid=(min+max)/2,amp=(max-min)/2;
      return `return { opacity: ${mid} + Math.sin(context.time * ${speed} * Math.PI * 2) * ${amp} };`;
    }
    if(kind==="variable"){
      const name=String(values.variable||"score").trim()||"score",type=String(values.type||"number"),raw=String(values.value??"");let value;
      if(type==="boolean")value=/^(true|1|yes|on)$/i.test(raw);else if(type==="text")value=raw;else value=Number(raw)||0;
      return `context.writeValue(${JSON.stringify(name)}, ${JSON.stringify(value)});\nreturn {};`;
    }
    if(kind==="rando"){
      const name=String(values.variable||"rng").trim()||"rng",minimum=Number(values.minimum)||0,maximum=Number(values.maximum)||10,integer=values.integer!=="no";
      return `const minimum = ${Math.min(minimum,maximum)};\nconst maximum = ${Math.max(minimum,maximum)};\nlet value = minimum + Math.random() * (maximum - minimum);\n${integer?"value = Math.round(value);":""}\ncontext.writeValue(${JSON.stringify(name)}, value);\nreturn {};`;
    }
    if(kind==="clipcontrol"){
      const action=["stop","play","gotoStop","gotoPlay"].includes(values.action)?values.action:"gotoStop",frame=String(values.frame||"1").trim()||"1";
      return `context.api.runTimelineAction(context.object.id, ${JSON.stringify(action)}, ${JSON.stringify(frame)});\nreturn {};`;
    }
    if(kind==="condition"){
      const name=String(values.variable||"score").trim()||"score",operator=[">=",">","===","<=","<","!=="].includes(values.operator)?values.operator:">=",value=Number(values.value)||0,on=clamp(Number(values.trueOpacity)||0,0,100)/100,off=clamp(Number(values.falseOpacity)||0,0,100)/100;
      return `if (Number(context.readValue(${JSON.stringify(name)}, 0)) ${operator} ${value}) {\n  return { opacity: ${on} };\n}\nreturn { opacity: ${off} };`;
    }
    return "return {};";
  }
  const rangeControl=(key,label,value,min,max,step,unit)=>`<label class="nc-control">${label}<span class="nc-control-row"><input data-ui-key="${key}" type="range" min="${min}" max="${max}" step="${step}" value="${escapeHtml(value)}"><output data-ui-output="${key}" data-unit="${escapeHtml(unit||"")}">${escapeHtml(value)}${escapeHtml(unit||"")}</output></span></label>`;
  function simpleControls(slot){
    const ui=slotUi(slot);if(!ui)return "";const v=ui.values;
    if(ui.kind==="drag")return `<div class="nc-simple-card" data-simple-kind="drag"><div class="nc-simple-title"><span>Drag &amp; Drop</span>This Clip</div><label class="nc-control">Mouse button<select data-ui-key="button"><option value="primary">Primary (left)</option><option value="middle">Middle</option><option value="secondary">Secondary (right)</option></select></label><label class="nc-control">Movement<select data-ui-key="axis"><option value="both">Anywhere</option><option value="horizontal">Left and right only</option><option value="vertical">Up and down only</option></select></label><label class="nc-control">When grabbed<select data-ui-key="grab"><option value="offset">Keep the point I grabbed</option><option value="centre">Snap the Clip centre to the pointer</option></select></label><label class="nc-control">When released<select data-ui-key="release"><option value="stay">Stay where dropped</option><option value="return">Return to the starting position</option></select></label></div>`;
    if(ui.kind==="move")return `<div class="nc-simple-card" data-simple-kind="move"><div class="nc-simple-title"><span>Move</span>Visual controls</div><label class="nc-control">Direction<select data-ui-key="direction"><option value="right">Right</option><option value="left">Left</option><option value="up">Up</option><option value="down">Down</option></select></label>${rangeControl("speed","Speed",v.speed,0,1000,5," px/s")}</div>`;
    if(ui.kind==="rotate")return `<div class="nc-simple-card" data-simple-kind="rotate"><div class="nc-simple-title"><span>Rotate</span>Visual controls</div><label class="nc-control">Direction<select data-ui-key="direction"><option value="clockwise">Clockwise</option><option value="counterclockwise">Counter-clockwise</option></select></label>${rangeControl("speed","Rotation speed",v.speed,0,720,5,"°/s")}</div>`;
    if(ui.kind==="opacity")return `<div class="nc-simple-card" data-simple-kind="opacity"><div class="nc-simple-title"><span>Opacity</span>Pulse controls</div>${rangeControl("minimum","Minimum opacity",v.minimum,0,100,1,"%")}${rangeControl("maximum","Maximum opacity",v.maximum,0,100,1,"%")}${rangeControl("speed","Pulse speed",v.speed,.1,10,.1,"/s")}</div>`;
    if(ui.kind==="variable")return `<div class="nc-simple-card" data-simple-kind="variable"><div class="nc-simple-title"><span>Variable</span>Set a value</div><label class="nc-control">Variable name<input data-ui-key="variable" type="text" value="${escapeHtml(v.variable)}"></label><label class="nc-control">Value type<select data-ui-key="type"><option value="number">Number</option><option value="text">Text</option><option value="boolean">True / False</option></select></label><label class="nc-control">Value<input data-ui-key="value" type="text" value="${escapeHtml(v.value)}"></label></div>`;
    if(ui.kind==="rando")return `<div class="nc-simple-card" data-simple-kind="rando"><div class="nc-simple-title"><span>Rando</span>Choose a random value</div><label class="nc-control">Variable name<input data-ui-key="variable" type="text" value="${escapeHtml(v.variable)}"></label><label class="nc-control">Minimum<input data-ui-key="minimum" type="number" step="any" value="${escapeHtml(v.minimum)}"></label><label class="nc-control">Maximum<input data-ui-key="maximum" type="number" step="any" value="${escapeHtml(v.maximum)}"></label><label class="nc-control">Number type<select data-ui-key="integer"><option value="yes">Whole number</option><option value="no">Decimal</option></select></label></div>`;
    if(ui.kind==="clipcontrol")return `<div class="nc-simple-card" data-simple-kind="clipcontrol"><div class="nc-simple-title"><span>Clip Playback</span>ActionScript-style timeline control</div><label class="nc-control">Action<select data-ui-key="action"><option value="gotoStop">Go to frame and stop</option><option value="gotoPlay">Go to frame and play</option><option value="stop">Stop this Clip</option><option value="play">Play this Clip</option></select></label><label class="nc-control">Frame number or label<input data-ui-key="frame" type="text" value="${escapeHtml(v.frame)}"></label></div>`;
    return `<div class="nc-simple-card" data-simple-kind="condition"><div class="nc-simple-title"><span>If</span>Variable condition</div><label class="nc-control">Variable name<input data-ui-key="variable" type="text" value="${escapeHtml(v.variable)}"></label><label class="nc-control">Comparison<select data-ui-key="operator"><option value=">=">At least (≥)</option><option value=">">Greater than (&gt;)</option><option value="===">Equal to (=)</option><option value="&lt;=">At most (≤)</option><option value="&lt;">Less than (&lt;)</option><option value="!==">Not equal (≠)</option></select></label><label class="nc-control">Compare with<input data-ui-key="value" type="number" value="${escapeHtml(v.value)}"></label>${rangeControl("trueOpacity","Opacity when true",v.trueOpacity,0,100,1,"%")}${rangeControl("falseOpacity","Opacity when false",v.falseOpacity,0,100,1,"%")}</div>`;
  }
  function uiSummary(slot){const ui=slotUi(slot);if(!ui)return "";const v=ui.values;if(ui.kind==="drag")return `${v.axis==="both"?"Free":v.axis} · ${v.release==="return"?"returns home":"stays dropped"}`;if(ui.kind==="move")return `${String(v.direction).replace(/^./,c=>c.toUpperCase())} · ${v.speed} px/s`;if(ui.kind==="rotate")return `${v.direction==="counterclockwise"?"Counter-clockwise":"Clockwise"} · ${v.speed}°/s`;if(ui.kind==="opacity")return `${v.minimum}–${v.maximum}% · ${v.speed} pulses/s`;if(ui.kind==="variable")return `${v.variable} = ${v.value}`;if(ui.kind==="rando")return `${v.variable} · ${v.minimum} to ${v.maximum}`;if(ui.kind==="clipcontrol")return `${String(v.action).replace(/([A-Z])/g," $1")} · ${v.frame}`;return `${v.variable} ${v.operator} ${v.value}`;}
  function renderGraph(){
    nodesLayer.innerHTML="";
    for(const entry of state.graph.values()){
      const clipNode=document.createElement("article");clipNode.className="nc-node nc-clip-node"+(state.activeKey===entry.key&&!state.selectedSlotId?" active":"");clipNode.dataset.clipKey=entry.key;clipNode.style.left=entry.x+"px";clipNode.style.top=entry.y+"px";clipNode.innerHTML=`<div class="nc-node-head" data-node-drag><span class="nc-node-badge">Clip</span><span>${escapeHtml(entry.name)}</span><button class="nc-node-remove" type="button">×</button></div>${portOrder.map(e=>`<div class="nc-event-row" data-event="${e}"><button class="nc-pip input" data-input-event="${e}" data-input-clip="${escapeHtml(entry.key)}" type="button"></button>${portLabel[e]}<small>${e==='tool'?'managed':'event'}</small></div>`).join("")}`;clipNode.addEventListener("click",ev=>{if(ev.target.closest(".nc-node-remove"))return;state.activeKey=entry.key;state.selectedOwnerKey="";state.selectedSlotId="";renderAll();});clipNode.querySelector(".nc-node-remove").addEventListener("click",ev=>{ev.stopPropagation();removeGraphClip(entry.key);});installNodeDrag(clipNode,entry,null);nodesLayer.appendChild(clipNode);
      slotsFor(entry).forEach((slot,index)=>{const code=document.createElement("article");code.className="nc-node nc-code-node"+(state.selectedOwnerKey===entry.key&&state.selectedSlotId===slot.id?" active":"");code.dataset.slotId=slot.id;code.dataset.ownerKey=entry.key;const x=Number.isFinite(Number(slot.x))?Number(slot.x):Math.max(30,entry.x-300),y=Number.isFinite(Number(slot.y))?Number(slot.y):entry.y+index*125;code.style.left=x+"px";code.style.top=y+"px";const preview=uiSummary(slot)||String(slot.code||"").replace(/\s+/g," ").slice(0,128)||"Empty code node";const badge=slot.source==='timeline-script'?"Timeline":slot.source==='type-tool'?"Managed":"Code";code.innerHTML=`<div class="nc-node-head" data-node-drag><span class="nc-node-badge${slot.source==='type-tool'?" nc-managed":""}">${badge}</span><span>${escapeHtml(slot.name)}</span><button class="nc-node-remove" type="button">×</button></div><div class="nc-code-preview">${escapeHtml(preview)}</div>${slot.source==='timeline-script'?"":"<button class=\"nc-pip output\" data-output-slot=\""+escapeHtml(slot.id)+"\" type=\"button\"></button>"}`;code.addEventListener("click",ev=>{if(ev.target.closest(".nc-node-remove,.nc-pip"))return;state.activeKey=entry.key;state.selectedOwnerKey=entry.key;state.selectedSlotId=slot.id;renderAll();});code.querySelector(".nc-node-remove").addEventListener("click",ev=>{ev.stopPropagation();deleteSlot(entry,slot);});const pip=code.querySelector(".nc-pip");if(pip)pip.addEventListener("pointerdown",ev=>beginLink(ev,entry,slot));installNodeDrag(code,entry,slot);nodesLayer.appendChild(code);});
    }
    nodesLayer.querySelectorAll("[data-input-event]").forEach(input=>input.addEventListener("pointerup",ev=>finishLink(ev,input.dataset.inputClip,input.dataset.inputEvent)));drawLinks();
  }
  function selectNode(entry,slot,rerender=true){state.activeKey=entry.key;state.selectedOwnerKey=slot?entry.key:"";state.selectedSlotId=slot?slot.id:"";if(rerender)renderAll();}
  function installNodeDrag(node,entry,slot){const handle=node.querySelector("[data-node-drag]");if(!handle)return;handle.addEventListener("pointerdown",ev=>{if(ev.button!==0||ev.target.closest("button"))return;ev.stopPropagation();ev.preventDefault();selectNode(entry,slot,false);if(LukeAnimate.pushUndo)LukeAnimate.pushUndo();state.nodeDrag={node,entry,slot,startX:ev.clientX,startY:ev.clientY,left:parseFloat(node.style.left)||0,top:parseFloat(node.style.top)||0,pointerId:ev.pointerId};const move=moveEvent=>{const d=state.nodeDrag;if(!d||d.node!==node||d.pointerId!==moveEvent.pointerId)return;node.style.left=d.left+(moveEvent.clientX-d.startX)/state.scale+"px";node.style.top=d.top+(moveEvent.clientY-d.startY)/state.scale+"px";drawLinks();};const finish=finishEvent=>{const d=state.nodeDrag;if(!d||d.node!==node||d.pointerId!==finishEvent.pointerId)return;document.removeEventListener("pointermove",move,true);document.removeEventListener("pointerup",finish,true);document.removeEventListener("pointercancel",finish,true);const x=parseFloat(node.style.left)||d.left,y=parseFloat(node.style.top)||d.top,moved=Math.abs(finishEvent.clientX-d.startX)>2||Math.abs(finishEvent.clientY-d.startY)>2;if(slot&&slot.source==='timeline-script')LukeAnimate.setTimelineScriptMarker(slot.layerId,slot.id,{nodeX:x,nodeY:y});else if(slot)LukeAnimate.setNodeCodeSlot(entry.object,Object.assign({},slot,{x,y}));else{entry.x=x;entry.y=y;if(entry.object)entry.object._lukeAnimateNodeGraph={x,y};}state.nodeDrag=null;renderAll();setStatus(moved?"Node position saved.":slot?`Selected ${slot.name}.`:`Selected ${entry.name}.`,"good");};document.addEventListener("pointermove",move,true);document.addEventListener("pointerup",finish,true);document.addEventListener("pointercancel",finish,true);});}
  const wireColor=e=>e==="load"?"#9a73d5":e==="unload"?"#3bb99f":e==="tool"?"#8a69b8":"#df6969";
  function drawCurve(x1,y1,x2,y2,color,dashed=false){const p=document.createElementNS("http://www.w3.org/2000/svg","path"),b=Math.max(70,Math.abs(x2-x1)*.52);p.setAttribute("d",`M ${x1} ${y1} C ${x1+b} ${y1}, ${x2-b} ${y2}, ${x2} ${y2}`);p.setAttribute("fill","none");p.setAttribute("stroke",color);p.setAttribute("stroke-width","3");p.setAttribute("opacity",".82");if(dashed)p.setAttribute("stroke-dasharray","7 6");links.appendChild(p);}
  function drawLinks(){links.innerHTML="";for(const entry of state.graph.values()){const clip=nodesLayer.querySelector(`.nc-clip-node[data-clip-key="${CSS.escape(entry.key)}"]`);if(!clip)continue;const cx=parseFloat(clip.style.left)||0,cy=parseFloat(clip.style.top)||0;for(const slot of slotsFor(entry)){const code=nodesLayer.querySelector(`.nc-code-node[data-slot-id="${CSS.escape(slot.id)}"][data-owner-key="${CSS.escape(entry.key)}"]`);if(!code)continue;const event=portOrder.includes(slot.event)?slot.event:"update";drawCurve((parseFloat(code.style.left)||0)+230,(parseFloat(code.style.top)||0)+74,cx+8,cy+portY(event),wireColor(event));}}if(state.linking)drawCurve(state.linking.x,state.linking.y,state.linking.mouseX,state.linking.mouseY,"#7c8994",true);}
  function beginLink(ev,entry,slot){ev.stopPropagation();ev.preventDefault();selectNode(entry,slot,false);renderEditor();nodesLayer.querySelectorAll(".nc-node.active").forEach(n=>n.classList.remove("active"));ev.currentTarget.closest(".nc-code-node").classList.add("active");const node=ev.currentTarget.closest(".nc-code-node");state.linking={entry,slot,x:(parseFloat(node.style.left)||0)+230,y:(parseFloat(node.style.top)||0)+74,mouseX:(parseFloat(node.style.left)||0)+280,mouseY:(parseFloat(node.style.top)||0)+74};drawLinks();}
  function finishLink(ev,targetKey,event){if(!state.linking)return;ev.stopPropagation();ev.preventDefault();const source=state.linking,target=state.graph.get(targetKey);state.linking=null;if(!target)return renderAll();if(event==="tool"&&source.slot.source!=="type-tool"){setStatus("Only managed Type Tool nodes connect to the Type Tools port.","bad");return renderAll();}if(source.slot.source==="type-tool"&&(target!==source.entry||event!=="tool")){setStatus("Managed nodes remain connected to their own Clip’s Type Tools port.","bad");return renderAll();}if(target===source.entry){LukeAnimate.setNodeCodeSlot(target.object,Object.assign({},source.slot,{event}));state.selectedOwnerKey=target.key;state.selectedSlotId=source.slot.id;}else{const next=Object.assign({},source.slot,{id:source.slot.id+"-"+Date.now().toString(36),event});LukeAnimate.setNodeCodeSlot(target.object,next);LukeAnimate.removeNodeCodeSlot(source.entry.object,source.slot.id);state.selectedOwnerKey=target.key;state.selectedSlotId=next.id;state.activeKey=target.key;}renderAll();setStatus(`Connected “${source.slot.name}” to ${target.name} · ${portLabel[event]}.`,"good");}
  function readEditorUi(slot){const current=slotUi(slot);if(!current)return null;const values={};editor.querySelectorAll("[data-ui-key]").forEach(control=>{values[control.dataset.uiKey]=(control.type==="range"||control.type==="number")?Number(control.value):control.value;});return {kind:current.kind,values};}
  function syncSimpleOutputs(){editor.querySelectorAll('input[type="range"][data-ui-key]').forEach(control=>{const output=editor.querySelector(`[data-ui-output="${CSS.escape(control.dataset.uiKey)}"]`);if(output)output.textContent=control.value+String(output.dataset.unit||"");});}
  function bindSimpleControls(slot){const ui=slotUi(slot);if(!ui)return;Object.keys(ui.values).forEach(key=>{const control=editor.querySelector(`[data-ui-key="${CSS.escape(key)}"]`);if(control)control.value=String(ui.values[key]);});syncSimpleOutputs();const regenerate=()=>{syncSimpleOutputs();setStatus("Visual controls changed. Save Node to apply.");};editor.querySelectorAll("[data-ui-key]").forEach(control=>{control.addEventListener("input",regenerate);control.addEventListener("change",regenerate);});}
  const managedEditors={button:["behaviours","Button","lukeanimate-button-panel"],dragdrop:["type","Drag Drop","lukeanimate-dragdrop-panel"],siderunner:["type","Side_Runner","lukeanimate-siderunner-panel"],oldtimer:["type","Old Timer","lukeanimate-oldtimer-panel"],life:["type","Life","lukeanimate-life-panel"],directions:["type","Directions","lukeanimate-directions-panel"],car:["type","Car","lukeanimate-car-panel"]};let dockRequest=0;
  function closeDockedEditor(){dockRequest++;const docked=editor.querySelector(".nc-docked-editor");if(!docked)return;const close=docked.querySelector('[class*="close"]');if(close)close.click();else docked.remove();editor.classList.remove("nc-has-docked");}
  async function dockManagedEditor(entry,slot){const target=managedEditors[String(slot.toolId||"").toLowerCase()],key=entry.key+":"+slot.id,existing=editor.querySelector(".nc-docked-editor");if(existing&&existing.dataset.nodeKey===key)return;if(existing)closeDockedEditor();if(!target){editor.innerHTML=`<div class="nc-empty">The ${escapeHtml(slot.name)} editor is not configured.</div>`;return;}const request=++dockRequest;editor.innerHTML='<div class="nc-empty">Loading the shared editor…</div>';if(LukeAnimate.selectDrawObject)LukeAnimate.selectDrawObject(entry.object);const opened=await LukeAnimate.openConfiguredTool(target[0],target[1]);if(request!==dockRequest)return;const toolPanel=document.getElementById(target[2]);if(!opened||!toolPanel){editor.innerHTML=`<div class="nc-empty">Could not load the ${escapeHtml(slot.name)} editor.</div>`;return;}toolPanel.classList.add("nc-docked-editor");toolPanel.dataset.nodeKey=key;editor.innerHTML="";editor.classList.add("nc-has-docked");editor.appendChild(toolPanel);}
  function openCodePopup(entry,slot){
    const old=document.querySelector(".nc-code-popup");if(old)old.remove();
    const popup=document.createElement("section");popup.className="nc-code-popup";popup.innerHTML=`<div class="nc-code-popup-head" data-floating-panel-drag><strong>Freehand Code · ${escapeHtml(slot.name)}</strong><button type="button" data-code-close title="Close">×</button></div><textarea spellcheck="false" data-popup-code>${escapeHtml(slot.code||"")}</textarea><div class="nc-code-popup-actions"><button type="button" data-code-cancel>Cancel</button><button class="primary" type="button" data-code-save>Save Code</button></div>`;
    document.body.appendChild(popup);LukeAnimate.registerFloatingPanel(popup);LukeAnimate.bringFloatingPanelToFront(popup);LukeAnimate.clampFloatingPanelToViewport(popup);const close=()=>popup.remove();popup.querySelector("[data-code-close]").onclick=close;popup.querySelector("[data-code-cancel]").onclick=close;popup.querySelector("[data-code-save]").onclick=()=>{const current=slotEntry(slot.id,entry.key);if(!current){close();return setStatus("That node is no longer available.","bad");}LukeAnimate.setNodeCodeSlot(entry.object,Object.assign({},current.slot,{code:popup.querySelector("[data-popup-code]").value,ui:null}));close();renderAll();setStatus("Freehand code saved.","good");};popup.querySelector("textarea").focus();
  }
  function renderEditor(){
    const found=slotEntry(state.selectedSlotId);if(!found){if(editor.querySelector(".nc-docked-editor"))closeDockedEditor();editor.classList.remove("nc-has-docked");editor.innerHTML='<div class="nc-empty">Select a code node to edit it.<br><br>Add nodes from the toolbar or drag an output to another event port.</div>';return;}
    const {entry,slot}=found;if(slot.source==="timeline-script"){
      if(editor.querySelector(".nc-docked-editor"))closeDockedEditor();editor.classList.remove("nc-has-docked");
      const config=slot.config||{},frameNumber=Number(slot.frame)+1,layerName=slot.layerName||entry.name||"Script";
      let form=`<div class="nc-timeline-row"><span>Layer</span><input value="${escapeHtml(layerName)}" readonly></div><div class="nc-timeline-row"><span>Marker frame</span><input data-timeline-frame type="number" min="1" value="${frameNumber}"></div><div class="nc-timeline-row"><span>Behaviour</span><select data-timeline-type><option value="stop">Stop</option><option value="rando">Rando</option></select></div>`;
      if(slot.markerType==="rando"){
        form+=`<div class="nc-timeline-card"><h4>Rando</h4><div class="nc-timeline-row"><span>Variable</span><input data-timeline-var value="${escapeHtml(config.varName||"rng")}"></div><div class="nc-timeline-row"><span>Minimum</span><input data-timeline-min type="number" step="any" value="${Number(config.min)||0}"></div><div class="nc-timeline-row"><span>Maximum</span><input data-timeline-max type="number" step="any" value="${Number(config.max)||10}"></div><p class="nc-timeline-help">This marker chooses a random value when playback reaches Frame ${frameNumber}.</p></div>`;
      }else{
        const frameActions=["gotoStop","gotoPlay","clipGotoStop","clipGotoPlay"],clipActions=["clipStop","clipGotoStop","clipPlay","clipGotoPlay"],action=config.action||"stop";
        form+=`<div class="nc-timeline-card"><h4>Stop / Play</h4><div class="nc-timeline-row"><span>Do</span><select data-timeline-action><option value="stop">Stop this timeline</option><option value="gotoStop">Go to frame and stop</option><option value="play">Play this timeline</option><option value="gotoPlay">Go to frame and play</option><option value="clipStop">Tell Clip to stop</option><option value="clipGotoStop">Tell Clip: go to and stop</option><option value="clipPlay">Tell Clip to play</option><option value="clipGotoPlay">Tell Clip: go to and play</option></select></div>${clipActions.includes(action)?`<div class="nc-timeline-row"><span>Clip</span><input data-timeline-target value="${escapeHtml(config.target||"")}"></div>`:""}${frameActions.includes(action)?`<div class="nc-timeline-row"><span>Frame / label</span><input data-timeline-target-frame value="${escapeHtml(config.frame||"1")}"></div>`:""}<p class="nc-timeline-help">This marker runs when playback reaches Frame ${frameNumber}. Stop.svg is used automatically when that asset becomes available.</p></div>`;
      }
      form+='<button class="nc-timeline-delete" data-delete type="button">Delete marker</button>';editor.innerHTML=form;
      q("[data-timeline-type]").value=slot.markerType||"stop";if(slot.markerType!=="rando")q("[data-timeline-action]").value=config.action||"stop";
      const save=()=>{const next={frame:Number(q("[data-timeline-frame]").value)-1,type:q("[data-timeline-type]").value};if(next.type==="rando")next.config={varName:q("[data-timeline-var]")?.value||config.varName||"rng",min:Number(q("[data-timeline-min]")?.value??config.min??0),max:Number(q("[data-timeline-max]")?.value??config.max??10)};else next.config={action:q("[data-timeline-action]")?.value||config.action||"stop",target:q("[data-timeline-target]")?.value||config.target||"timeline",frame:q("[data-timeline-target-frame]")?.value||config.frame||"1"};LukeAnimate.setTimelineScriptMarker(slot.layerId,slot.id,next);renderAll();setStatus("Timeline action saved.","good");};
      editor.querySelectorAll("input,select").forEach(control=>control.addEventListener("change",save));q("[data-delete]").onclick=()=>deleteSlot(entry,slot);return;
    }
    const managed=slot.source==="type-tool",visual=!!slotUi(slot);
    if(managed){dockManagedEditor(entry,slot);return;}
    if(editor.querySelector(".nc-docked-editor"))closeDockedEditor();editor.classList.remove("nc-has-docked");
    editor.innerHTML=`<label class="nc-field"><span>Clip</span><input value="${escapeHtml(entry.name)}" readonly></label><label class="nc-field"><span>Node name</span><input data-edit-name value="${escapeHtml(slot.name)}"></label><label class="nc-field"><span>Connected event</span><select data-edit-event><option value="load">Load</option><option value="update">Update</option><option value="unload">Unload</option><option value="click">Mouse Click</option><option value="pointerdown">Pointer Down</option><option value="pointerup">Pointer Up</option><option value="dragstart">Drag Start</option><option value="drag">While Dragging</option><option value="drop">Drop</option><option value="correctDrop">Correct Drop</option><option value="incorrectDrop">Incorrect Drop</option><option value="frame">Timeline Frame</option></select></label><label class="nc-field" data-node-frame-field><span>Timeline frame</span><input data-edit-frame type="number" min="1" value="${Number(slot.frame??0)+1}"></label><label class="nc-check"><input data-edit-enabled type="checkbox" ${slot.enabled!==false?"checked":""}> Enabled</label>${simpleControls(slot)}<div class="nc-editor-card"><strong>Freehand JavaScript</strong><p>${visual?"Opening freehand code detaches this node from its visual controls when you save.":"Write or edit this node’s JavaScript in a separate coding window."}</p><button class="nc-btn" data-open-code type="button">Open Code Editor</button></div><p class="nc-help">Code receives context: object, effective, pointer, state, values, event, delta, time, api, readValue and writeValue.</p><div class="nc-actions"><button class="nc-btn danger" data-delete type="button">Delete Node</button><button class="nc-btn primary" data-save type="button">Save Node</button></div>`;
    const editEvent=q("[data-edit-event]");[{value:"mouseover",label:"Roll Over"},{value:"mouseout",label:"Roll Out"},{value:"hover",label:"Hover"},{value:"releaseoutside",label:"Release Outside"}].forEach(item=>{if(!editEvent.querySelector(`option[value="${item.value}"]`)){const option=document.createElement("option");option.value=item.value;option.textContent=item.label;editEvent.appendChild(option);}});editEvent.value=slot.event;const syncFrameField=()=>{const field=q("[data-node-frame-field]");if(field)field.style.display=editEvent.value==="frame"?"flex":"none";};editEvent.addEventListener("change",syncFrameField);syncFrameField();bindSimpleControls(slot);q("[data-open-code]").onclick=()=>openCodePopup(entry,slot);q("[data-save]").onclick=()=>saveEditor(entry,slot);q("[data-delete]").onclick=()=>deleteSlot(entry,slot);const error=LukeAnimate.getNodeCodeError(entry.object,slot.id);if(error)setStatus("Code error: "+error,"bad");
  }
  function saveEditor(entry,slot){const ui=readEditorUi(slot),event=q("[data-edit-event]").value,next=Object.assign({},slot,{name:q("[data-edit-name]").value.trim()||slot.name,event,frame:event==="frame"?Math.max(0,Number(q("[data-edit-frame]").value)-1):slot.frame,enabled:q("[data-edit-enabled]").checked,code:ui?generatedCode(ui.kind,ui.values,slot.id):slot.code,ui});LukeAnimate.setNodeCodeSlot(entry.object,next);state.activeKey=entry.key;state.selectedOwnerKey=entry.key;state.selectedSlotId=slot.id;renderAll();setStatus("Node saved.","good");}
  function deleteSlot(entry,slot){const ok=slot.source==="timeline-script"?LukeAnimate.removeTimelineScriptMarker(slot.layerId,slot.id):slot.source==="type-tool"?LukeAnimate.removeTypeTool(entry.object,slot.toolId):LukeAnimate.removeNodeCodeSlot(entry.object,slot.id);if(!ok)return setStatus("The node could not be removed.","bad");state.selectedOwnerKey="";state.selectedSlotId="";renderAll();setStatus(slot.source==="timeline-script"?"Timeline script removed.":slot.source==="type-tool"?"Type Tool and managed node removed.":"Code node removed.","good");}
  const templates={blank:{name:"Code",code:"// Runs on the connected Clip event.\n// Keep persistent values in context.state.\nreturn {};"},drag:{name:"Drag & Drop",kind:"drag"},move:{name:"Move",kind:"move"},rotate:{name:"Rotate",kind:"rotate"},opacity:{name:"Pulse Opacity",kind:"opacity"},variable:{name:"Set Variable",kind:"variable"},condition:{name:"If Variable",kind:"condition"},rando:{name:"Rando",kind:"rando"},clipcontrol:{name:"Clip Playback",kind:"clipcontrol"},timelineStop:{name:"Stop",timelineType:"stop",timelineAction:"stop"},timelineGotoStop:{name:"Go To and Stop",timelineType:"stop",timelineAction:"gotoStop"},timelinePlay:{name:"Play",timelineType:"stop",timelineAction:"play"},timelineGotoPlay:{name:"Go To and Play",timelineType:"stop",timelineAction:"gotoPlay"},timelineRando:{name:"Rando",timelineType:"rando"}};
  function addCodeNode(){let entry=activeEntry(),template=templates[q("[data-template]").value]||templates.blank;if(template.timelineType){entry=state.clips.find(e=>e.timeline);if(!entry)return setStatus("Timeline is unavailable.","bad");document.getElementById("script-behaviour-panel")?.classList.remove("open");state.graph.set(TIMELINE_KEY,Object.assign({},entry,{x:360,y:55}));state.activeKey=TIMELINE_KEY;const marker=LukeAnimate.addTimelineScriptMarker(template.timelineType,{config:template.timelineAction?{action:template.timelineAction}:null,openEditor:false});state.selectedOwnerKey=TIMELINE_KEY;state.selectedSlotId=marker.id;renderAll();setStatus(`${template.name} timeline node added.`,"good");return;}if(!entry||entry.timeline)return setStatus("Add or select a Clip node first.","bad");const event=template.kind==="drag"?"update":q("[data-event]").value,count=slotsFor(entry).filter(s=>s.source!=="type-tool").length,id="custom-"+Date.now().toString(36),ui=template.kind?{kind:template.kind,values:Object.assign({},uiDefaults[template.kind])}:null,slot={id,name:template.name+(count?" "+(count+1):""),event,enabled:true,code:template.kind?generatedCode(template.kind,ui.values,id):template.code,ui,source:"custom",x:Math.max(30,entry.x-300),y:entry.y+count*125};LukeAnimate.setNodeCodeSlot(entry.object,slot);state.selectedOwnerKey=entry.key;state.selectedSlotId=slot.id;renderAll();setStatus(`${template.name} node added to ${portLabel[event]}.`,"good");}
  function renderAll(){refreshClipLibrary();renderGraph();renderEditor();applyTransform();}
  function fitGraph(){const nodes=Array.from(nodesLayer.querySelectorAll(".nc-node"));if(!nodes.length){state.panX=60;state.panY=55;state.scale=.85;return applyTransform();}const b=nodes.reduce((box,n)=>{const x=parseFloat(n.style.left)||0,y=parseFloat(n.style.top)||0,w=n.offsetWidth||230,h=n.offsetHeight||160;return {x1:Math.min(box.x1,x),y1:Math.min(box.y1,y),x2:Math.max(box.x2,x+w),y2:Math.max(box.y2,y+h)};},{x1:Infinity,y1:Infinity,x2:-Infinity,y2:-Infinity}),r=viewport.getBoundingClientRect(),w=Math.max(1,b.x2-b.x1),h=Math.max(1,b.y2-b.y1);state.scale=clamp(Math.min((r.width-80)/w,(r.height-80)/h),.35,1.35);state.panX=40-b.x1*state.scale;state.panY=40-b.y1*state.scale;applyTransform();}
  function zoomAt(factor,cx,cy){const r=viewport.getBoundingClientRect(),x=cx??r.left+r.width/2,y=cy??r.top+r.height/2,before=screenToWorld(x,y);state.scale=clamp(state.scale*factor,.3,1.8);state.panX=(x-r.left)-before.x*state.scale;state.panY=(y-r.top)-before.y*state.scale;applyTransform();}
  function exportLayout(){const data={format:"Luke Animate Node Coder",version:2,view:{scale:state.scale,panX:state.panX,panY:state.panY},clips:Array.from(state.graph.values()).map(e=>({key:e.key,name:e.name,x:e.x,y:e.y,slots:slotsFor(e)}))},blob=new Blob([JSON.stringify(data,null,2)],{type:"application/json"}),url=URL.createObjectURL(blob),a=document.createElement("a");a.href=url;a.download="LukeAnimate-NodeCoder.json";a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);setStatus("Node layout and code saved.","good");}
  async function importLayout(file){try{const data=JSON.parse(await file.text());if(!data||!Array.isArray(data.clips))throw new Error("Not a Node Coder layout");state.graph.clear();for(const saved of data.clips){const entry=state.clips.find(e=>e.key===saved.key)||state.clips.find(e=>e.name===saved.name);if(!entry)continue;if(!state.graph.has(entry.key))state.graph.set(entry.key,Object.assign({},entry,{x:Number(saved.x)||480,y:Number(saved.y)||55}));for(const slot of saved.slots||[])LukeAnimate.setNodeCodeSlot(entry.object,slot);}if(data.view){state.scale=clamp(Number(data.view.scale)||.85,.3,1.8);state.panX=Number(data.view.panX)||60;state.panY=Number(data.view.panY)||55;}state.activeKey=state.graph.size?state.graph.keys().next().value:"";renderAll();setStatus("Node layout and code loaded.","good");}catch(error){setStatus("Could not load layout: "+String(error&&error.message||error),"bad");}}
  viewport.addEventListener("dragover",ev=>{ev.preventDefault();ev.dataTransfer.dropEffect="copy";});viewport.addEventListener("drop",ev=>{ev.preventDefault();const key=ev.dataTransfer.getData("text/x-la-clip"),entry=state.clips.find(e=>e.key===key);if(entry)addGraphClip(entry,screenToWorld(ev.clientX,ev.clientY));});viewport.addEventListener("wheel",ev=>{ev.preventDefault();zoomAt(ev.deltaY<0?1.1:.9,ev.clientX,ev.clientY);},{passive:false});viewport.addEventListener("pointerdown",ev=>{if(ev.button!==0||ev.target.closest(".nc-node"))return;state.panning={startX:ev.clientX,startY:ev.clientY,panX:state.panX,panY:state.panY};viewport.classList.add("panning");viewport.setPointerCapture(ev.pointerId);});viewport.addEventListener("pointermove",ev=>{if(state.panning){state.panX=state.panning.panX+ev.clientX-state.panning.startX;state.panY=state.panning.panY+ev.clientY-state.panning.startY;applyTransform();}if(state.linking){const p=screenToWorld(ev.clientX,ev.clientY);state.linking.mouseX=p.x;state.linking.mouseY=p.y;drawLinks();}});viewport.addEventListener("pointerup",ev=>{if(state.panning){state.panning=null;viewport.classList.remove("panning");try{viewport.releasePointerCapture(ev.pointerId);}catch(_){}}if(state.linking){state.linking=null;drawLinks();setStatus("Connection cancelled.");}});
  const keepPanelVisible=()=>clampPanel();
  window.addEventListener("resize",keepPanelVisible);window.addEventListener("pointerup",keepPanelVisible,true);
  search.addEventListener("input",refreshClipLibrary);q('[data-action="refresh"]').addEventListener("click",()=>{renderAll();setStatus("Clip library refreshed.","good");});q('[data-action="fit"]').addEventListener("click",fitGraph);q('[data-action="save-layout"]').addEventListener("click",exportLayout);q('[data-action="load-layout"]').addEventListener("click",()=>layoutFile.click());layoutFile.addEventListener("change",()=>{const file=layoutFile.files&&layoutFile.files[0];if(file)importLayout(file);layoutFile.value="";});q('[data-action="add-node"]').addEventListener("click",addCodeNode);q('[data-action="zoom-out"]').addEventListener("click",()=>zoomAt(.9));q('[data-action="zoom-in"]').addEventListener("click",()=>zoomAt(1.1));q(".nc-minimize").addEventListener("click",ev=>{ev.stopPropagation();toggleMinimize();});q(".nc-maximize").addEventListener("click",ev=>{ev.stopPropagation();toggleMaximize();});q(".nc-title").addEventListener("dblclick",ev=>{ev.stopPropagation();toggleMaximize();});q(".nc-close").addEventListener("click",()=>{clearInterval(refreshTimer);closeDockedEditor();window.removeEventListener("resize",keepPanelVisible);window.removeEventListener("pointerup",keepPanelVisible,true);panel.remove();setActive(false);});
  function pollScene(){const entries=sceneClipEntries(),fingerprint=entries.map(e=>e.key+":"+e.name).join("|"),selected=selectedStageClip(),selectedEntry=selected&&(entries.find(e=>e.object===selected)||null),selectedKey=selectedEntry?selectedEntry.key:"";if(fingerprint!==state.sceneFingerprint){state.sceneFingerprint=fingerprint;refreshClipLibrary();}if(selectedKey!==state.lastStageSelectionKey){state.lastStageSelectionKey=selectedKey;if(selectedEntry){addGraphClip(selectedEntry);setStatus(`Selected ${selectedEntry.name}.`,"good");}}const found=slotEntry(state.selectedSlotId);if(found){const error=LukeAnimate.getNodeCodeError(found.entry.object,found.slot.id);if(error)setStatus("Code error: "+error,"bad");}}
  const refreshTimer=setInterval(pollScene,350);refreshClipLibrary();const selected=selectedStageClip(),initial=selected&&(entryForObject(selected)||null);if(initial){state.lastStageSelectionKey=initial.key;addGraphClip(initial);}else{renderAll();setStatus(state.clips.length?"Drag a Clip onto the grid.":"Add a Clip to the current frame first.");}applyTransform();
  const focus=LukeAnimate.consumeNodeCoderTimelineFocus&&LukeAnimate.consumeNodeCoderTimelineFocus();if(focus){const timeline=state.clips.find(e=>e.timeline);if(timeline){addGraphClip(timeline);const target=timelineSlots().find(slot=>String(slot.layerId)===String(focus.layerId)&&String(slot.id)===String(focus.markerId));if(target){state.activeKey=TIMELINE_KEY;state.selectedOwnerKey=TIMELINE_KEY;state.selectedSlotId=target.id;renderAll();setStatus(`Selected ${target.name}.`,"good");}}}
})();
